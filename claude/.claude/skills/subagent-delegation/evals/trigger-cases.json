{
  "skill_name": "subagent-delegation",
  "method": "behavioral-dispatch",
  "cases": [
    {
      "id": "locate-importers-sweep",
      "query": "Find every file in this project that imports from the components module and list what each one uses.",
      "should_trigger": true,
      "note": "Broad locate sweep — real correction from session 84955fdf: 'don't grep. use an explore agent.' Unambiguous even cold: multi-file sweep, conclusion-only output needed. Expected to fire ≥50% under --warm-dispatch. Under cold default (--append-system-prompt), bounded-retrieval framing suppresses dispatch — cold 0-1/10 is expected, not a regression. Construct-validity caveat: the dispatch-project fixture was enlarged to 18+ importers and the case still under-fired cold — fixture scale alone does not overcome framing bias."
    },
    {
      "id": "explore-unfamiliar-area",
      "query": "Explore how the rendering pipeline is wired — what calls render_widget, where WidgetRenderer is instantiated, and how the call chain flows.",
      "should_trigger": true,
      "note": "Exploratory mapping of unfamiliar area. Real 'first explore the data' pattern. Expected to fire ≥50% under --warm-dispatch. Under cold default (--append-system-prompt), bounded-retrieval framing suppresses dispatch — cold 0-1/10 is expected, not a regression."
    },
    {
      "id": "relay-lookup",
      "query": "Show me the render_widget method and everywhere it's called.",
      "should_trigger": true,
      "note": "Relay lookup: the parent needs the answer reported back, not the output reasoned over. Reframed from 'known-target = inline' after user pushback dc0dc508 — relay is locate-and-report, not comprehension. Expected to fire ≥50% under --warm-dispatch. Under cold default (--append-system-prompt), bounded-retrieval framing suppresses dispatch — cold 0-1/10 is expected, not a regression. Construct-validity caveat: behavioral-dispatch cannot validate a skill-body fix (body is invisible to this eval — it only fires if the model self-triggers the Skill tool, which concrete task queries don't do)."
    },
    {
      "id": "reason-over-supplied-failure",
      "query": "This assertion is failing:\n\nAssertionError: expected widget_id='footer' to produce '<footer>' got '<widget id=footer>'\n\nWalk through why render_widget returns that format and what needs to change.",
      "should_trigger": false,
      "note": "Failure pasted inline — parent must reason line by line over the supplied output. Cold-context confounded: a cold turn inlines naturally regardless of skill quality, so this case is a weak signal. INLINE cases do not self-validate."
    },
    {
      "id": "single-confirmation-feeds-edit",
      "query": "Confirm that DEFAULT_TIMEOUT is only defined in theme.py and not duplicated elsewhere in this project, then I'll update it.",
      "should_trigger": false,
      "note": "Single targeted lookup feeding an edit the parent will make. Comprehension read whose conclusion feeds the parent's own write. Cold-context confounded: same caveat as reason-over-supplied-failure."
    }
  ]
}
