{
  "skill_name": "subagent-delegation",
  "method": "behavioral-dispatch",
  "cases": [
    {
      "id": "locate-importers-sweep",
      "query": "Find every file in this project that imports from the components module and list what each one uses.",
      "should_trigger": true,
      "note": "Broad locate sweep — real correction from session 84955fdf: 'don't grep. use an explore agent.' The fixture has 3 importers of components (renderer.py, layout.py, page.py). Measured 0/30 warm (claude-sonnet-4-6, K=30; confirmed 0/10 at K=10): task shape (bounded enumeration) is the suppressor — warmth does not flip native direct-retrieval propensity for a concrete locate query. Under cold default (--append-system-prompt), bounded-retrieval framing suppresses dispatch — cold 0-1/10 is expected, not a regression. Construct-validity caveat: behavioral-dispatch cannot measure concrete known-target relay/lookup queries — those queries elicit direct retrieval regardless of context warmth or priming."
    },
    {
      "id": "explore-unfamiliar-area",
      "query": "Explore how the rendering pipeline is wired — what calls render_widget, where WidgetRenderer is instantiated, and how the call chain flows.",
      "should_trigger": true,
      "note": "Exploratory mapping of unfamiliar area. Real 'first explore the data' pattern. Initial measurement: 10/10 warm (K=10, claude-sonnet-4-6). K=30 re-measurement: 13/30 (43%) — below the 50% formal harness threshold, indicating the small-sample K=10 result overstated the stable rate. The exploration shape is the only DELEGATE shape to elicit non-zero warm-dispatch propensity; concrete known-target queries (locate sweep, relay lookup) fire at 0/30. Under cold default (--append-system-prompt), bounded-retrieval framing suppresses dispatch — cold 0-1/10 is expected, not a regression."
    },
    {
      "id": "reason-over-supplied-failure",
      "query": "This assertion is failing:\n\nAssertionError: expected widget_id='footer' to produce '<footer>' got '<widget id=footer>'\n\nWalk through why render_widget returns that format and what needs to change.",
      "should_trigger": false,
      "note": "Failure pasted inline — parent must reason directly over the supplied output (data supplied, no multi-file gather needed, no delegation). Cold-context confounded: a cold turn inlines naturally regardless of skill quality, so this case is a weak signal. INLINE cases do not self-validate."
    },
    {
      "id": "single-confirmation-feeds-edit",
      "query": "Confirm that DEFAULT_TIMEOUT is only defined in theme.py and not duplicated elsewhere in this project, then I'll update it.",
      "should_trigger": false,
      "note": "Single targeted lookup feeding an edit the parent will make. Comprehension read whose conclusion feeds the parent's own write. Cold-context confounded: same caveat as reason-over-supplied-failure."
    }
  ]
}
