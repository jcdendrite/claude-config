# _config.sh: cut per-lookup schema re-reads and subprocess forks

## Context

Cut the per-call filesystem/subprocess cost of `claude/.claude/hooks/_config.sh`'s config-key resolution path without changing any resolution semantics, grammar, or Python parity. A single `_config_value`/`_config_enabled` call for one key today re-parses `config-keys.psv` up to seven times — `_config_value` calls `_config_schema_field` separately for `resolution` and `legacy-probe-on-resolution-failure`, and (for a `config-dir-or-home` union key with a diverged `$HOME`) also calls `_config_schema_known_keys` and three more `_config_schema_field` calls for `type`/`legacy-filename`/`legacy-polarity` — each a full re-read of the same 15-row schema file, plus `_config_line_key_value`/`_config_location_value` each fork a `tr` subprocess per state-file line for case-folding. This repo's `PreToolUse` hooks run this path on every `Edit`/`Write`/`MultiEdit` and every git write (11+ call sites), so the per-call overhead compounds against the documented <100ms-per-fire hook budget (`docs/config-file.md`'s Performance section measures ~22ms added per `_config_enabled` call). GitHub issue #998 prescribes three fixes, all confirmed against `docs/design-decisions/sentinel-config-consolidation.md`'s own performance paragraph as the design doc's intended remediation: (1) one schema pass per `_config_value` lookup instead of up to seven, (2) replace the `tr` subprocess fork with a bash-3.2-safe `case` statement, (3) per-process memoization so a repeated lookup of the same key within one process's lifetime skips re-resolution. This is primarily an implementation-efficiency change — the file format, config schema, and per-call (not per-session) resolution model are all explicitly out of scope per that design doc.

This is not purely an efficiency change, though: `_config_resolve_fail_safe`'s three internal-failure paths now uniformly check `_config_location_value`'s exit status and resolve to the key's schema `default` — or to `true` when `legacy_probe` is `true`, true only for `worktree_required` — rather than a hardcoded literal, on failure, for all five enforcement-critical keys (`worktree_required`, `autonomous_shipping`, `round_consult_gate`, `commit_stall_block`, `authorization_boundary_restore`). This is an intentional, tested fail-safe-resolution semantics change, not a side effect of the efficiency work. Ledger row 8 covers why `default` is now validated eagerly as a consequence.

## Approach

Collapse `_config.sh`'s per-lookup schema re-reads into one pass, gate the `tr` fork behind a `case` test so it never runs for an all-lowercase file, and memoize `_config_value`/`_config_enabled`'s final per-key result in a single delimited-string cache that `_config_set`/`_config_scaffold` reset. Most of the public contract — arity-guard texts, stderr wording, resolution order, and the exit codes for a schema-level failure — stays byte-identical, and the existing behavioral suites are the proof of that part. One piece does not: `_config_resolve_fail_safe`'s three internal-failure paths (the union fold and both single-location `_config_location_value` calls) — see Context above.

Three findings shift the design away from the issue's literal prescription, all verified this session:

**The `tr` fork does not need replacing — it needs a guard.** `_config_read_key_from_file` walks every state-file line, and `_config_line_key_value` forks one `tr` per line: ~15 forks per location read, up to ~30 for a `config-dir-or-home` union key. Wrapping the existing call in `case "$value" in *[A-Z]*) <existing tr> ;; esac` drops that to zero for every machine-written state file (`_config_set` writes `true`/`false`/a lowercase enum literal), and to at most two on a machine carrying a hand-authored `TRUE`. A hand-rolled 26-arm ASCII folder would buy those last two forks at the cost of ~35 lines of new string-manipulation code inside the file that decides whether worktree enforcement is armed — the heavier primitive for a rounding error. It is also the option CLAUDE.md's hand-rolled-logic rule argues against while `tr` remains available. Set aside; if the engineer wants the fork gone unconditionally, that is a follow-up with its own review surface.

The guard is fail-safe in either direction for the call site that already scopes its locale. `_config_line_key_value` already holds `local LC_ALL=C`, so `[A-Z]` there is exactly the 26 ASCII bytes, confirmed by reading the function — this call site needs no further change. `_config_location_value`'s `content-matches` branch (the only key using that legacy-polarity today is `pr_cost_disclosure`, not one of the five enforcement-critical keys) has no such `local` today, so its `[A-Z]` guard would otherwise rest on an unverified assumption about every collation on every platform this file targets — including macOS/Darwin bash 3.2, which this file's own header names as its binding constraint and which a same-machine probe in this session (glibc/Linux bash 5.2 only) cannot confirm. Rather than accept that assumption, this function gains its own `local LC_ALL=C` — the same one-line scoping `_config_line_key_value` already carries — so the guard's correctness no longer depends on an unverified cross-platform claim. With that scoping in place, over-matching still costs a fork (the `tr` branch keeps its own `LC_ALL=C` prefix and reproduces today's exact output) and under-matching cannot occur (ASCII `A`-`Z` is unconditionally matched by `[A-Z]` under `LC_ALL=C`, the same guarantee `_config_line_key_value`'s existing guard already relies on).

**The single-pass helper collapses seven passes to two, not to one.** A new `_config_schema_row KEY` does one `IFS='|' read` walk of `config-keys.psv`, populating `_CONFIG_ROW_TYPE`/`_DEFAULT`/`_RESOLUTION`/`_LEGACY_PROBE`/`_LEGACY_IMPORT`/`_LEGACY_FILENAME`/`_LEGACY_POLARITY`/`_HUMAN_NAME`/`_DOCS_ANCHOR`/`_PROMPT_DESCRIPTION` plus `_CONFIG_ROW_KNOWN_KEYS` (the `" k1 k2 "` membership list, free in the same pass). It follows `_config_trim`/`_config_quote_value_for_write`'s established global-result idiom rather than printing for `$(...)` capture, because a capture fork is the cost this change exists to remove and bash 3.2 has no `local -n`. It captures fields from the **first** matching row while scanning to end-of-file for `known_keys` — `test_config_parser_parity.py:444` pins schema-row first-match-wins against `_config_schema_field`'s in-loop `return`.

`_config_value` then calls `_config_schema_row` once and threads the result down through the already-existing 6-arg `_config_location_value` / 4-arg `_config_read_key_from_file` precomputed forms on **every** path, not just the union-active one. What remains is `_config_location_value`'s closing `_config_schema_field "$key" default`, one further pass, reached only when neither a state-file row nor a legacy file resolved the key. Threading `default` as a seventh argument would remove it, but `TestPrecomputedArgsArityGuard` pins the literal `"expected 2 or 6 args, got %d"` text with `got 3` and `got 5` assertions. Set aside: at most two passes per lookup (three for a union key falling through at both locations), down from seven-plus, is the whole win, and it costs no contract.

Truncation semantics forbid validating eagerly. `_config_schema_field` returns 4 only for the *specific* resolution-critical column a caller asked for, so `_config_value` must check `resolution` empty, then `legacy_probe` empty, in that order — that ordering is what makes `autonomous_shipping|bool` and `worktree_required|bool|false|config-dir-or-home` both return 4 at their two different pinned points. Validating all four resolution-critical columns inside `_config_schema_row` would name the wrong field in the warning and would turn a hand-corrupted-but-resolvable row (`key|bool||config-dir|...`, empty `default`, state-file row present) into a spurious exit 4. A one-line `_config_warn_truncated_row KEY FIELD` helper keeps the warning text in one place and is called at each consumption point.

`_config_schema_field` is rewritten to delegate to `_config_schema_row` and map FIELD onto the right global, preserving its name, signature, exit codes, and the ordering that makes an unrecognized FIELD return 1 only when KEY's row was found (and 4 otherwise). `_config_read_key_from_file`'s 2-arg self-deriving branch keeps calling `_config_schema_known_keys` **by name** and is deliberately left uncollapsed — that is the path `_migrate_process_key` uses, and `test_migrate_legacy_config.py:293` overrides that function as a bash stub. It is not on the hot path, because `_config_value` now always reaches the 4-arg form.

No caching at the schema-read level, per the session's resolved constraint: `config-get.sh:53-71` performs two deliberate independent schema reads to close a TOCTOU window, and a row cache shared across them would silently retire the second `[ -r ]` check. Each `_config_schema_row` call re-reads and re-checks readability, exactly as `_config_schema_field` does today.

**Memoization needs invalidation, and the repo has a real cache hit the earlier per-call-memoization analysis missed.** `advance-past-commit-stall.sh` resolves `autonomous_shipping` twice in one process: line 77 (`_lib_autonomous_shipping_sentinel_present`, the pre-`REPO_ROOT` fast path) and line 181 (`_lib_autonomous_shipping_active`, which calls the same helper again). Both are bare commands in the top-level shell, not `$(...)` captures, so a parent-frame memo hits. That second lookup is the most expensive shape in the repo — a `config-dir-or-home` union key walking two locations — and it lands on exactly the machines where autonomous shipping is on. `nudge-handoff-near-context-cap.sh` calls `_config_enabled handoff_nudge` at lines 396 and 508, but those are the `--check` and fire paths, mutually exclusive in one process.

The staleness hazard is in `install.sh`, not `_lib.sh`. `_prompt_sentinel_opt_in` reads `_config_value "$key"` (line 438), writes `_config_set "$key" false` (448), then re-reads `_config_value "$key"` (458) and *must* see the write — `test_install_sh_machine_level_opt_ins.py:459` asserts `"→ disabled"` on that second read. `report_sentinel_inventory`'s own header (install.sh:646-649) documents the same invariant across functions. Both reads happen inside `$(...)`, so a naive memo survives by luck. The design does not rely on that: `_config_set` and `_config_scaffold` each call a one-line `_config_memo_reset` before returning, closing the class by construction. `migrate-legacy-config.sh` is unaffected either way — it uses `_config_read_key_from_file`, never `_config_value`.

Memo shape: a single delimited string, `_CONFIG_MEMO_CACHE`, formatted `" key1=value1 key2=value2 "` and queried with the same space-padded `case " $list " in *" $key "*)` idiom `_config_schema_known_keys` (lines 409-445) already establishes in this file, extended one step further with parameter-expansion extraction of the value half. This reuses an already-precedented pattern rather than introducing a new one, and it has no two-structure desync failure mode the way a pair of parallel indexed arrays would (an append to one array with no matching append to the other, silently misaligning index-based lookups with no error). No delimiter-collision risk: a memoized key is always schema-declared snake_case (`config-keys.psv`'s own documented grammar), and — because only exit 0 is ever memoized (see below) — a memoized value is always drawn from `true`/`false`/`[a-z0-9_-]+`, the same grammar `_config_line_key_value` already enforces and which can never contain a space or `=`. Two alternatives were weighed and set aside:

- **Dynamic variable names via `printf -v` plus `${!name}` indirect expansion.** Two distinct hazards, only one of which is actually decisive. Indirect expansion of an unset target under `set -u` is not decisive: the guarded idiom `${!name:-SENTINEL}` sidesteps an unset indirect target without tripping `set -u` at all (confirmed on a bash 5.2 probe this session; not independently confirmed on bash 3.2, so this doesn't resolve whether the guarded form itself behaves identically there, but it does mean this hazard alone would not have ruled the approach out even if it had). The actually decisive hazard is `printf -v` rejecting a hyphenated variable name outright — every current `config-keys.psv` key is schema-documented snake_case, so no live key trips this today, but a future hyphenated key would silently break the scheme with no compile-time signal. Forward-compatibility hardening, not a live bug, but real enough to prefer the string-based approach below.
- **Two parallel indexed arrays (`_CONFIG_MEMO_KEYS`, `_CONFIG_MEMO_VALUES`) with a linear, `${#arr[@]}`-guarded scan.** Bash-3.2-safe and legitimate, but carries the desync failure mode the single-string design avoids, and duplicates a "is X a member of this list, and if so what's its paired value" query shape this file already solves once via `_config_schema_known_keys`'s idiom. Set aside in favor of reusing that idiom directly.

Three rules keep it correct:

- **Memoize only when `config_dir_override` is empty.** An override is an arbitrary filesystem path and would have to become part of the cache key; no bash caller passes one (only `_config.py`'s `config_dir_override` parameter, a separate reader), and every hook call site is zero-override. This dissolves the composite-key problem instead of defending it.
- **Memoize only exit 0.** Exits 2, 3, and 4 are transient-environment failures; re-resolving them costs nothing on a cold path and keeps every readability and resolution check live on each call.
- **`_config_value` and `_config_enabled` share one memo gate, `_config_lookup`, rather than checking or storing independently.** Both are thin wrappers that call `_config_lookup` bare (no `$(...)`); `_config_lookup` itself checks the memo first and, on a miss, calls `_config_resolve` and stores the result before returning. Neither wrapper forks a subshell internally, so a memo store from one is visible to a same-process call to the other — the only subshell in play is whichever one the *caller* of `_config_value`/`_config_enabled` introduces by wrapping the call in `$(...)`, exactly as `_config_lookup`'s own docstring states. Reading before storing means a duplicate entry cannot be appended.

Cost when the memo never hits (nine of the eleven call sites): one case-glob scan of the (typically empty or single-entry) memo string and one append. No forks, no file I/O.

**Global-return scoping needs an explicit invariant, not just correct call-site tracing.** The invariant: never declare a `local` sharing a `_CONFIG_ROW_*`/`_CONFIG_MEMO_*`/`_CONFIG_RESOLVED_*`/`_CONFIG_LOOKUP_*` name anywhere in this file. Bash `local` is dynamically, not lexically, scoped, so such a declaration *anywhere* up the call stack — now or in a future edit — would silently intercept the plain assignment and redirect it into that shadow, with no error. `_config_schema_row`'s ~10 `_CONFIG_ROW_*` globals and `_config_resolve`/`_config_lookup`'s own `_CONFIG_RESOLVED_VALUE`/`_CONFIG_RESOLVED_MEMOIZABLE`/`_CONFIG_LOOKUP_VALUE` globals are safe as designed today — tracing the call shape, each is copied into a plainly-named local (or consumed directly) immediately after being read and never re-read once a later call could have overwritten it. `_CONFIG_MEMO_CACHE`/`_CONFIG_MEMO_ENV_DIR`/`_CONFIG_MEMO_ENV_HOME`/`_CONFIG_MEMO_ENV_SET`/`_CONFIG_MEMO_LOOKUP_RESULT` are safe today for a different reason, not this read-once pattern: they persist by design across the entire process lifetime and are read/written directly by name on every lookup — they are safe only because no `local` currently declares one of these names anywhere in this file or its callers, a call-stack-wide absence rather than a read-then-relay guarantee. That is why this group needs the wider cross-file scan Critical files item 8 describes below. This is a materially larger surface than this file's two existing global-return helpers (`_config_trim`'s `_CONFIG_TRIM_RESULT`, `_config_quote_value_for_write`'s `_CONFIG_QUOTE_RESULT`), both single-name leaf functions with no nested calls, because the new design threads these names through a multi-level call chain (`_config_value`/`_config_enabled` → `_config_lookup` → `_config_resolve` → `_config_schema_row`). The invariant is stated as a comment on the global declarations and enforced by a static grep-based test (Critical files, below), closing the class by construction rather than by review discipline.

**The design doc's inline figure becomes a pointer.** `docs/design-decisions/sentinel-config-consolidation.md`:17 restates the `~22ms` figure that `docs/config-file.md`'s Performance section owns, so re-measuring in this PR would update one copy and leave the other stale. The figure is deleted there and the existing parenthetical pointer kept, per CLAUDE.md's single-source-of-truth rule for prose. Nothing else in that paragraph moves: its argument (the cost is implementation, not file format) and its conclusion (fixable inside `_config.sh` alone) are what this PR implements, so they are confirmed rather than revised. The two verbs that carried the deleted figure shift to past tense, because the same sentence's present-tense mechanism claim — seven schema passes, one `tr` fork per state-file line — stops describing `_config.sh` the moment this change lands.

### Assumption ledger

**Root:** one `_config_value`/`_config_enabled` lookup re-parses the 15-row `config-keys.psv` up to seven times and forks one `tr` per state-file line, so every `PreToolUse` gate in this repo pays ~22ms of avoidable work per fire against a documented <100ms budget.

**Givens:**

1. macOS system bash is 3.2 — no associative arrays, no `${var,,}`, no `local -n`. Vendor-imposed; `_config.sh`:19-23 and 121-135 already pin both consequences. `[verified: claude/.claude/hooks/_config.sh:19-23,121-135]`
2. Hook processes are independent, short-lived, and share no memory, so no cross-invocation cache exists to reach for. Owned by the Claude Code hook runtime. `[verified: docs/design-decisions/sentinel-config-consolidation.md:13-15]`
3. `config-get.sh`'s two independent schema reads are a deliberate TOCTOU defense that no lower-level cache may collapse. `config-get.sh` is a repo-owned file this plan could technically edit, but weakening its race-detection behavior is a separate, separately-decidable question (is the detection worth its current cost?) that this issue's own "no resolution-semantics change" scope doesn't authorize deciding here — the reason this counts as a given is that dissolving it needs a decision outside this plan, not that the file is unreachable. `[engineer-verified]`, corroborated at `claude/.claude/scripts/config-get.sh:53-71`
4. `_config_schema_known_keys` must stay dispatched by name from `_migrate_process_key`'s call path. `[engineer-verified]`, corroborated at `claude/.claude/scripts/tests/test_migrate_legacy_config.py:286-300`
5. `_config.py` needs no change: `schema()` already does one pass per call, folds with in-process `str.lower()`, and documents no-caching as deliberate. `[engineer-verified]`

**Mechanisms:**

6. `_config_schema_row` returns via `_CONFIG_ROW_*` globals rather than stdout — `anchors: root`. A `$(...)`-capture return would reintroduce the fork this change removes, and `_config_trim`/`_config_quote_value_for_write` already establish the idiom for the same reason. `[verified: claude/.claude/hooks/_config.sh:85-94,721-726]`
7. `_config_schema_row` captures the first matching row and scans to EOF for `known_keys` — `anchors: row6`. Schema-row first-match-wins is pinned across both readers. `[verified: claude/.claude/scripts/tests/test_config_parser_parity.py:444-465]`
8. Emptiness of `default`, `resolution`, and `legacy-probe-on-resolution-failure` is checked eagerly, right after `_config_schema_row` returns in `_config_resolve` — not lazily at whichever consumption point would have reached each column, and not inside `_config_schema_row` itself (which stays a pure populate-and-return, with no validation of its own). `_config_resolve_fail_safe`'s three internal-failure paths (see Approach) can all consume `default`, so there is no row shape where an empty `default` goes unread, and eager validation matches `_config.py:376-381`'s existing eager column validation. `[verified: claude/.claude/hooks/_config.sh:766-778; claude/.claude/hooks/tests/test_config_lib.py:344-362 (test_empty_default_column_returns_exit_4_not_a_silent_grant)]`
9. `_config_location_value` and `_config_read_key_from_file` keep their 2-or-6 and 2-or-4 arities and error texts — `anchors: root`. `[verified: claude/.claude/hooks/tests/test_config_lib.py:719-742]`
10. Per-process memoization is the mechanism for the repeated lookup, which is heavier than the task strictly requires — it adds cross-call mutable state to a file whose design is per-call resolution — so, `anchors: root`, three lighter primitives: **(a)** delete the redundant call in `advance-past-commit-stall.sh` — fails, that hook's own comment documents the fast path as a deliberate pre-filter placed before `REPO_ROOT` is known, specifically to avoid spawning `jq`; removing it re-orders the hook. **(b)** Hoist the result into a local in `_lib_autonomous_shipping_active` — fails, the two lookups originate from two separate top-level statements in the hook, not from one shared frame. **(c)** Ship fixes 1 and 2 only — fails, the second lookup still pays two schema passes and two full state-file walks for a union key. `[verified: claude/.claude/hooks/advance-past-commit-stall.sh:62-77,181; claude/.claude/hooks/_lib.sh:1796-1826]`
11. `_config_set`/`_config_scaffold` reset the memo — `anchors: row10`. `install.sh` reads a key, writes it, and re-reads it in one process, with `"→ disabled"` asserted on the second read. `[verified: install.sh:436-495,646-649; claude/.claude/hooks/tests/test_install_sh_machine_level_opt_ins.py:459-477]`
12. Only exit 0 is memoized — `anchors: row10`. A cached 3 or 4 would outlive the transient condition that produced it, weakening the same posture given 3 protects.
13. Memoization is skipped when `config_dir_override` is non-empty — `anchors: row10`. An arbitrary path in the cache key needs a delimiter or identifier encoding; no bash caller passes one.
14. A `case "$value" in *[A-Z]*)` guard, not a hand-rolled folder, replaces the fork on the hot path — `anchors: root`. Lighter primitives weighed: a 26-arm per-character `case` (rejected: ~35 lines of new string code in an enforcement-critical file, to remove at most two forks per process on a hand-authored uppercase value) and bash's `${var,,}` (rejected: bash 4.0+, excluded by given 1). `[verified: claude/.claude/hooks/_config.sh:164,523]`
15. The memo returns a stale value if a same-process actor changes a legacy sentinel file or writes `claude-config.toml` without going through `_config_set`/`_config_scaffold` — `anchors: row11`. No such caller exists today: `migrate-legacy-config.sh` deletes legacy files but runs as its own process and never calls `_config_value`. Recorded as a one-line note in `_config_lookup`'s docstring, not defended with further machinery. A third vector — reassigning `CLAUDE_CONFIG_DIR` (or `HOME`, when `CLAUDE_CONFIG_DIR` is unset) mid-process, which changes which location a key resolves from — is not accepted risk: `_config_memo_lookup` fingerprints the environment the memo was populated under (`_CONFIG_MEMO_ENV_DIR`/`_CONFIG_MEMO_ENV_HOME`, compared independently rather than via one concatenated string, so a literal `|` in either value can't alias two distinct pairs into the same fingerprint) and resets the cache on a mismatch, so a reassignment is a memo miss rather than a stale hit, pinned by `TestMemoization`'s `test_claude_config_dir_reassignment_mid_process_invalidates_the_memo`, `test_home_reassignment_mid_process_invalidates_the_memo`, and `test_claude_config_dir_unset_mid_process_falls_back_to_home_and_invalidates_the_memo`. `[verified: install.sh:681-696; claude/.claude/scripts/migrate-legacy-config.sh]`
16. `docs/design-decisions/sentinel-config-consolidation.md`:17 drops its inline `~22ms` figure and keeps a cross-reference to `docs/config-file.md` § "Performance" — `anchors: root`. It is the only other copy of a number this change makes stale, and the paragraph's own pointer already names the canonical home, so the figure has no second job there. `[engineer-verified]`, corroborated at `docs/design-decisions/sentinel-config-consolidation.md:17` and `docs/config-file.md:266-270`
17. No supersession annotation is added to that file — `anchors: row16`. `.claude/rules/design-decisions.md`'s Supersession bullet covers a decision "that a later one overturns"; this change implements the remediation the paragraph itself prescribes and leaves per-call resolution intact, so there is nothing to overturn and a `**Superseded by …**` line would misstate the record. `[verified: .claude/rules/design-decisions.md:28-32; docs/design-decisions/sentinel-config-consolidation.md:13-17]`
18. `_config_location_value` gains its own `local LC_ALL=C` — `anchors: row14`. Its `content-matches`-branch guard has no locale scoping of its own, unlike `_config_line_key_value`'s identical guard; without it, the guard's correctness would rest on an unverified claim that no collation on any platform (including the macOS/Darwin bash 3.2 this file's header names as its binding constraint) lets an ASCII `A`-`Z` fail an `[A-Z]` case-glob match. Adding the one-line scoping this function already lacks removes the dependence on that claim rather than resting behavior on it; the only key using this branch today is `pr_cost_disclosure`, not an enforcement-critical key, so this is a correctness fix with a narrow, non-security-critical blast radius, made anyway because the fix is cheaper than the assumption. `[verified: claude/.claude/hooks/_config.sh:143-144 (existing guard) vs. 507-534 (this branch, unguarded); claude/.claude/hooks/config-keys.psv:97 (only content-matches row)]`
19. `_config_schema_row`'s, `_CONFIG_MEMO_CACHE`'s, and `_config_resolve`/`_config_lookup`'s `_CONFIG_RESOLVED_*`/`_CONFIG_LOOKUP_*` global-return convention requires that no function in this file ever declares a `local` sharing one of those names — `anchors: root`. Bash's dynamic (not lexical) `local` scoping means such a declaration anywhere up the call stack would silently intercept the plain assignment; enforced by comment plus a static grep-based test rather than by review discipline alone (see Critical files). `[verified: claude/.claude/hooks/_config.sh — bash `local` semantics, confirmed against this file's existing single-name global-return precedent at lines 85-94,721-726]`

## Critical files

One `code-writer` dispatch. The `_config.sh` edits, the tests that pin them, and the two doc files whose figures and cross-reference this change touches share one body of context that cannot be specified to a second agent without restating it, so splitting would buy nothing and risks two agents resolving the memo semantics differently.

**Merge/revert independence.** This branch's commits already separate the pure schema/`tr`-fork work (`4cc011bb`) from the fail-safe-gap fix (`e236ff0e`) from threading (`dfe060b6`) from this round's plan/doc reconciliation. The fail-safe-resolution semantics change is a distinct, separately-assessed change from the perf work, so a future revert triggered by a perf-side regression (a bash-3.2 break, a latency regression) is a decision made with the fail-safe fix's own correctness in view — not an accidental side effect that silently reintroduces an unchecked exit-status gap on the single-location internal-failure branches for the five enforcement-critical keys.

**Modify:**

- `claude/.claude/hooks/_config.sh` — the entire change. New: `_config_schema_row`, `_config_warn_truncated_row`, `_config_resolve_fail_safe`, `_config_resolve` (the one-schema-row-pass resolution body factored out of the old `_config_value`), `_config_lookup` (the single memo gate shared by `_config_value`/`_config_enabled`), `_config_memo_lookup`, `_config_memo_store`, `_config_memo_reset`, and the new globals: `_CONFIG_MEMO_CACHE`/`_CONFIG_MEMO_ENV_DIR`/`_CONFIG_MEMO_ENV_HOME`/`_CONFIG_MEMO_ENV_SET`/`_CONFIG_MEMO_LOOKUP_RESULT`, the ~10 `_CONFIG_ROW_*` globals `_config_schema_row` populates, and `_CONFIG_RESOLVED_VALUE`/`_CONFIG_RESOLVED_MEMOIZABLE`/`_CONFIG_LOOKUP_VALUE`. Rewritten internals: `_config_schema_field` (delegates to `_config_schema_row`), `_config_value` and `_config_enabled` (both now thin wrappers over `_config_lookup` rather than each running its own resolution or memo logic — `_config_lookup` alone checks the memo, calls `_config_resolve` on a miss, and stores the result), `_config_line_key_value`:164 (guard the `tr` call) and `_config_location_value`:523 (guard the `tr` call **and** add its own `local LC_ALL=C`, per ledger row 18), `_config_set` and `_config_scaffold` (call `_config_memo_reset`). Unchanged: `_config_schema_known_keys`, `_config_read_key_from_file`'s 2-arg branch, `_config_trim`, `_config_file_lines`, `_config_quote_value_for_write`, `_lib_config_dir`, and every arity guard and error string.

  **Reuse, do not reimplement:** `_config_trim`'s global-result convention for `_config_schema_row`'s return shape; `_config_scaffold`:937's existing single-pass `IFS='|' read -r` field list verbatim for `_config_schema_row`'s loop; `_config_schema_known_keys`:428-434's `" k1 k2 "` accumulation, reused both for `_CONFIG_ROW_KNOWN_KEYS` and — extended with parameter-expansion value extraction — for `_CONFIG_MEMO_CACHE`'s own lookup; the existing 6-arg and 4-arg precomputed branches, which already do exactly what the generalized path needs.

  Prescribed durable comment lines, one fact each: on `_config_schema_row`, *"Captures the first matching row and scans on to end-of-file, so duplicate key rows resolve first-match-wins while known_keys still lists every row."* On `_config_schema_row`'s `_CONFIG_ROW_*` globals and on `_CONFIG_MEMO_CACHE`, *"No function in this file may declare a `local` sharing this name — bash's dynamic scoping would silently redirect this global's assignment into that shadow."* On `_CONFIG_MEMO_CACHE`, *"Memoizes only a successfully resolved value for a zero-override lookup; a transient resolution failure is re-checked on every call."* On `_config_memo_reset`'s call in `_config_set`, *"A write invalidates every memoized value, so a later read in the same process sees what was just written."* On `_config_value`, *"Resolves a key at most once per process; a same-process change to a legacy sentinel file or a direct claude-config.toml write bypasses the memo and is not seen."* On each `tr` guard, *"The fork runs only for a hand-authored uppercase value; an all-lowercase file skips it entirely."* On `_config_location_value`'s new `local LC_ALL=C`, *"Scopes this function's `[A-Z]` guard to ASCII, matching `_config_line_key_value`'s identical guard, rather than resting on a cross-platform collation assumption."*

- `docs/config-file.md` Performance section (lines 259-277) — replace the three timing figures with re-measured ones, and describe the resolution chain's new pass count. Also correct the parenthetical at lines 269-270: `advance-past-commit-stall.sh` resolves three keys per fire on an autonomous-shipping-enabled machine (`commit_stall_block`, then `autonomous_shipping` at line 77 and again at line 181), not two. The "three or more keys in one fire" caveat at lines 275-277 should be re-read against the new numbers rather than carried over unexamined.

- `docs/design-decisions/sentinel-config-consolidation.md`:17 — one sentence, not the paragraph. Delete the inline `~22ms` figure, put the existing pointer in the repo's citation grammar, and re-tense the two verbs the figure carried. The sentence becomes:

  *The measured per-call overhead (`docs/config-file.md` § "Performance") was not the cost of reading a 15-line file — it was `_config.sh` re-parsing `config-keys.psv`'s schema up to seven times per single-key lookup, plus one `tr` subprocess fork per state-file line for case-folding.*

  Everything else in the file is copied through verbatim, including that paragraph's closing sentence ("That is an implementation cost in the resolution chain, fixable inside `_config.sh` alone …") and line 5's `[docs/config-file.md](../config-file.md)` markdown link. Do not add a supersession line (ledger row 17).

  **Coupling to the other doc edit, and what is mechanically checked.** The citation names `docs/config-file.md`'s `## Performance` heading by exact text, and this PR rewrites the body under that heading. `test_skills.py`'s citation-*resolution* test runs `_citation_report(_all_skill_md_files(), …)` — the skill tree, not `docs/design-decisions/**` — so renaming that heading in the same PR would leave a dead pointer with nothing to catch it. Keep the heading text exactly `## Performance` while replacing the figures beneath it. Citation *extraction* is enforced here: `test_every_citation_shaped_construct_is_extracted` covers `docs/**/*.md` via `_all_doc_paths()`, so the citation must stay on one line with a space before the opening quote. That file's paragraphs are already single unwrapped lines — preserve that rather than re-wrapping. `test_design_decision_files.py` is unaffected: its `_CONVERTED_CITATION_RE` is `\[§(\d+)\]\(([^)]+)\)`, the legacy `[§N](slug.md)` link form only.

- `claude/.claude/hooks/tests/test_config_lib.py` — twelve new tests, none of which exist today:
  1. `_config_set` followed by `_config_value` for the same key **in one bash process, both calls bare/uncaptured (printed directly, not wrapped in an inner `$(...)`)** returns the newly written value. This is the memo-invalidation contract; without `_config_memo_reset` it fails. The bare-call constraint is load-bearing and must be stated in the test's own docstring: a `first=$(_config_value KEY); ...; second=$(_config_value KEY)` shape would fork a fresh subshell per call, lose any memo store to that subshell exactly like `install.sh`'s own real calls do (ledger row 15's "survives by luck" case), and pass unconditionally regardless of whether `_config_memo_reset` exists — testing nothing.
  2. Two `_config_enabled` calls for the same key in one process agree, with `_config_set` between them flipping the second. Covers the `_config_enabled`-frame store and its reset together. Same bare-call constraint as test 1.
  3. A same-process legacy-file creation between two `_config_value` calls for one key. Assert the memoized value and state in the docstring that the staleness is the documented per-process contract from ledger row 15, not an accident.
  4. `_config_schema_field KEY <bogus-field>` returns 1 for a key whose row exists, and 4 for a key whose row does not. The delegating rewrite moves that `return 1` relative to the row scan, and nothing currently pins the ordering.
  5. Two *different* keys memoized in one process (e.g. `autonomous_shipping` then `handoff_nudge`), re-read the first, assert it still returns its own value. Pins the memo cache's own membership/extraction logic against cross-key corruption — the shape a single-key test structurally cannot catch.
  6. A first `_config_value`/`_config_enabled` call fails with a transient status (3 via an unreadable schema, or 4 via a truncated row), the condition is corrected in the same process (restore/fix the fixture file), and a second call for the same key now resolves successfully. Pins ledger row 12 ("only exit 0 is memoized") directly — nothing else in this list exercises it.
  7. A direct call to `_config_schema_row` for each of the 15 real `config-keys.psv` keys, asserting each populated `_CONFIG_ROW_*` global matches `_config.py`'s own `schema()` function's independent parse of the same row (not `_config_schema_field`, since that function now delegates to `_config_schema_row` and so would report the same wrong value on a column-swap bug). Pins the consolidation's core correctness claim directly, rather than trusting the incidental breadth of the existing `_config_value`/`_config_enabled` behavioral suite (which does exercise `_config_schema_row` for every real key once `_config_value` always reaches the 4-arg precomputed form, but wouldn't reliably catch a single shifted column on a key whose test coverage happens to route around that field).
  8. Two static tests, not behavioral ones, scanning two different scopes because `_CONFIG_ROW_*`/`_CONFIG_RESOLVED_*`/`_CONFIG_LOOKUP_*` and `_CONFIG_MEMO_*` don't carry the same risk shape. The first greps `_config.sh`'s own source alone for `local _CONFIG_ROW_`/`local _CONFIG_RESOLVED_`/`local _CONFIG_LOOKUP_`: those three groups are populated and consumed within one call chain (read-immediately-after-write), so a `local`/`declare` shadow declared by a caller elsewhere doesn't break the global-return contract — only one declared inside `_config.sh`'s own writer functions does, and this narrow scan still catches that. The second greps `_config.sh`, `_lib.sh`, `install.sh`, every `claude/.claude/hooks/*.sh`, and every `claude/.claude/scripts/*.sh` for `local _CONFIG_MEMO_`: `_CONFIG_MEMO_CACHE`/`_CONFIG_MEMO_ENV_DIR`/`_CONFIG_MEMO_ENV_HOME` persist across separate top-level calls in the same process by design, so a caller-declared shadow of one of these anywhere in that wider set can desync it from a legitimate `_config_set`-triggered `_config_memo_reset` — the reset clears the shadow, not the real global, leaving a stale pre-reset value for a later unshadowed lookup to replay. That is genuine cross-call staleness, not a mere cache miss, which is why this group needs the wider scan the first group does not. Enforces the global-return shadowing invariant by construction, as a fail-fast check with a clearer failure message than the existing behavioral suite would give — not a completeness guarantee.
  9. A same-process test forcing `_config_location_value` to fail on the primary-location-only branch while the home-location branch succeeds, asserting `_config_resolve`'s union-fold guard (`[ "$primary_status" -ne 0 ] || [ "$home_status" -ne 0 ]`) resolves to the fail-safe value rather than the home branch's real value. Pins the `||`/`&&` operator the fail-safe rewrite depends on — an accidental flip to `&&` passes every other test in this file.
  10. Two tests reading `config-keys.psv` directly, through neither `_config_schema_row` nor `_config.py`'s `schema()` (`TestConfigKeysPsvGrammarInvariants`): every non-comment, non-blank row has exactly 11 pipe-separated fields, and the file's own grammar-comment column order matches `_SCHEMA_ROW_GLOBALS`/`_SCHEMA_ROW_PY_FIELDS`'s hardcoded order. Closes the one gap item 7's independent-parse comparison can't reach — both parsers apply the same deliberate padding/truncation leniency to a malformed row, so a wrong-field-count row passes that comparison silently.
  11. A third `TestMemoization` reassignment case, alongside the two ledger row 15 already names: `CLAUDE_CONFIG_DIR` set for a first bare `_config_value` call, then unset before a second bare call for the same key, asserting the second call falls back to and reflects `$HOME`'s own state rather than replaying the first call's `CLAUDE_CONFIG_DIR`-anchored memoized value. Closes the one reassignment transition shape the other two tests leave untested.
  12. A same-process pair of `(CLAUDE_CONFIG_DIR, HOME)` values chosen so their naive concatenation collides: `dir_a`/`dir_b` are `isolated_home`-relative paths (`"X|Y"`/`"X"`), `home_a`/`home_b` are bare strings (`"Z"`/`"Y|Z"`, deliberately not nested under `isolated_home` — nesting them too would prepend an identical prefix to both sides of the pipe join and break the collision). `dir_a|home_a` and `dir_b|home_b` both concatenate to the same `"<isolated_home>/X|Y|Z"`. `home_a`/`home_b` stay bare because `handoff_nudge` is a `resolution=config-dir` key, so `$HOME` is read only for the memo fingerprint, never for value resolution — it doesn't need to be a real directory. Asserts a bare `_config_value` call under the second pair returns the second pair's own value rather than replaying the first's. Pins the reason `_CONFIG_MEMO_ENV_DIR`/`_CONFIG_MEMO_ENV_HOME` (documented on those globals' own declaration comment, `_config.sh:44-46`) are compared independently rather than as one joined string — the one property with no prior test that would fail if it were silently reverted to concatenation.

  A parametrized non-ASCII case is added to `test_content_matches_case_folded`'s family (`test_config_lib.py:437`), run under an explicit non-`C` `LC_ALL` override in the test's own subprocess call, asserting `_config_location_value`'s `content-matches` branch still resolves to today's value. `test_content_matches_case_folded`'s existing cases are pure ASCII and only exercise this guard's real-match direction; `test_config_parser_parity.py`'s U+212A fixture targets `_config_line_key_value` instead (a different, already-locale-scoped guard) and its own docstring states it cannot exercise this one. With `local LC_ALL=C` added per ledger row 18, this guard's correctness no longer depends on the ambient locale, but the test still pins that scoping against a regression.

## Verification

Run from the worktree root. `.venv` paths are worktree-relative per README.md's Tests section.

1. The issue's own scoped command, as the targeted first pass:

   `.venv/bin/pytest claude/.claude/hooks/tests/test_config_lib.py claude/.claude/scripts/tests/test_config_py.py claude/.claude/scripts/tests/test_config_parser_parity.py claude/.claude/scripts/tests/test_migrate_legacy_config.py -q`

2. Four files that list omits, each covering a path this change touches and that one does not:

   `.venv/bin/pytest claude/.claude/hooks/tests/test_install_sh_machine_level_opt_ins.py claude/.claude/hooks/tests/test_advance_past_commit_stall.py claude/.claude/scripts/tests/test_config_get.py claude/.claude/hooks/tests/test_lib.py -q`

   The first does **not** catch a missing `_config_memo_reset` — `install.sh:438,458` calls `_config_value` through `$(...)` capture on both reads, so (per ledger row 15) any memo store there is lost to its own subshell regardless of whether `_config_memo_reset` exists; this file is re-run here only as a general regression check on `install.sh`'s own opt-in flow, unrelated to memo correctness. The new `test_config_lib.py` tests 1–2 above (bare, uncaptured calls) are the only tests that can catch a broken reset — see their own docstring requirement. The second file exercises the double-`autonomous_shipping` lookup the memo targets. The third covers `config-get.sh`'s TOCTOU-defense call ordering. The fourth covers the five enforcement-critical `_lib.sh` wrappers and their exit-code `case` arms.

3. The repo's documented selector, which owns the domain mapping and widens on its own:

   `.venv/bin/python3 claude/.claude/scripts/select-tests.py`

   A path it cannot map is a bug in its rule table, not licence to widen by hand (repo `CLAUDE.md`, Commands). This is what covers the two doc edits: `select-tests.py`'s `DOCS_DIR = "docs"` is a deliberate blanket rule (its own comment records why it is not a per-file constant list), so both `docs/config-file.md` and `docs/design-decisions/sentinel-config-consolidation.md` select the doc-dependent test directories without a hand-added path.

4. Shell lint, since the whole diff is bash:

   `scripts/list-shell-files.sh | xargs -0 .venv/bin/shellcheck`

5. Re-measure per-fire latency and update `docs/config-file.md`'s three figures. Measure the **baseline on the same machine in the same sitting**, not against the numbers already in the doc — those were taken on an unknown machine, and comparing a new "after" to an old "before" would be an ungrounded causal claim under CLAUDE.md's quantitative-claims rule. Match the existing paragraph's stated methodology: real `bash script.sh` subprocess invocations, 20 iterations each, otherwise-idle machine. Three shapes, each measured at the merge-base and at `HEAD`:
   - a bare `. _lib.sh` with no config call (the ~16ms floor),
   - one `_config_enabled` call for a `config-dir` key (the ~38ms figure),
   - `advance-past-commit-stall.sh`'s real shape — `commit_stall_block`, then `autonomous_shipping` twice (the ~61ms figure, whose two-call description is itself being corrected).

   Measure the third shape with a state file present and with one absent: the absent case is where `_config_location_value`'s remaining `default` pass fires, and it is the honest worst case to publish.

## Out of scope

- **`claude-config.toml`'s file format, `config-keys.psv`'s grammar, and the per-call resolution model.** All three are settled in `docs/design-decisions/sentinel-config-consolidation.md`, which names this work as fixable inside `_config.sh` alone.
- **`_config.py`.** Its `schema()` already does one pass per call and folds in-process; no Python-visible change is required by any part of this design. `test_config_parser_parity.py` would catch a grammar or resolution divergence independently if one crept in.
- **The decision, argument, and conclusion in `docs/design-decisions/sentinel-config-consolidation.md`:17.** The inline `~22ms` figure there *is* edited (see Critical files; ledger rows 16-17) — that is the whole edit. The paragraph's claim that the cost is implementation rather than file format, its enumeration of the three fixes, and the per-call-resolution decision at lines 13-15 are confirmed by this PR, not revised, and carry no supersession annotation. Every other paragraph in that file is untouched.
- **The `~22ms` figures inside this plan file.** `.claude/plans/` holds dated provenance for a change, not a live doc; the motivating measurement is the record of why this work was scoped. Context and the root ledger line above are left as written.
- **A hand-rolled ASCII casefold replacing `tr` outright.** Ledger row 14; a follow-up if the residual forks ever matter.
- **Deduplicating `advance-past-commit-stall.sh`'s two `autonomous_shipping` lookups.** The redundancy is documented as deliberate in that hook (lines 68-76), and the memo makes the second one free without re-ordering it.
- **Caching at the schema-read or row level.** Ledger row 3; it would retire `config-get.sh`'s second readability check.
- **Threading `default` as a seventh argument to `_config_location_value`.** It would remove the last remaining schema pass at the cost of the arity-guard text pinned by `TestPrecomputedArgsArityGuard`.
- **`_config_read_key_from_file`'s 2-arg self-deriving branch.** Left at two schema passes so `_config_schema_known_keys` stays dispatched by name for `_migrate_process_key`; it is off the hot path once `_config_value` always reaches the 4-arg form.
- **Any timeout or I/O backstop on the resolution chain.** `_config.sh`:25-30 records the unwrapped-I/O risk as an accepted tradeoff for local-disk deployment; nothing here changes that posture.
- **A bash-3.2 execution of the new memoization/global-threading code.** Neither this session (glibc/Linux bash 5.2 only) nor CI (`.github/workflows/tests.yml` runs `ubuntu-24.04` exclusively) can execute bash 3.2. The new parameter-expansion forms (`${_CONFIG_MEMO_CACHE#*" $key="}`, `${rest%% *}`) are ordinary POSIX expansions with no bash-4+ feature, matching the same "cannot confirm, mitigated by removing the dependency on an untestable claim" framing ledger row 18 already uses for the `LC_ALL=C` guard — accepted here rather than blocking on hardware this project has no access to.
