# Audit claude-config against Anthropic's "Optimizing for cost and intelligence" guide

## Context

The user asked to audit this repo against Anthropic's official "Optimizing for cost and intelligence" guide (https://platform.claude.com/docs/en/about-claude/models/optimizing-for-cost-and-intelligence), sourced from a Todoist task titled "claude-config run harness against official cost guide here." This repo already runs an extensive, actively-measured cost-optimization program spanning `docs/design-decisions.md` (dated entries §3 through §39+) and `docs/cost-levers-considered.md` (a consolidated lever register — "so a seventh plan doesn't re-measure ground already covered"). Now (2026-09-01) is a natural time because the guide reflects the current Claude 5 model family (Opus 5, Sonnet 5, Haiku 4.5, Fable 5.1) and the repo's own effort/model routing conventions already cite the same primary-source family of Anthropic docs. The intended outcome is a lever-by-lever gap check: for every lever the guide names, confirm whether the repo already has adopted/rejected/considered coverage (cite it), or record it as a genuine, previously-uninvestigated gap — landing as new rows in `docs/cost-levers-considered.md`, following that register's own established convention.

## Approach

Append one dated section to `docs/cost-levers-considered.md` holding a lever-by-lever cross-check of the guide against what this register already records — one row per lever the guide names, so a later reader of the same guide sees at a glance which recommendations are already closed here and which are not. Nothing else in the repo changes: no `docs/design-decisions.md` entry, no agent frontmatter, no test-constant edit, no CHANGELOG line.

Three sub-decisions, settled below:

1. One table with a row per guide lever (11 rows), not just rows for the newly-found gaps. The register's job is stopping re-investigation, and a partial table leaves a future reader to re-derive coverage for the rest of a 320-line file.
2. The `experimental.cacheTtl` finding gets its own row in the new section that supersedes the existing cache-TTL thread by name, rather than a fourth scattered follow-up paragraph under the original row. This matches the register's own precedent — the `cost-attribution-integrity.md` row corrects an earlier row by name with no back-pointer added to it.
3. The vendor's sixth effort level, `ultracode`, gets one clause inside the effort-level row, not a row of its own and not a `VALID_EFFORT_LEVELS` change. That test constant exists to catch a typo in a value the repo actually assigns, not to mirror the vendor's list, and `ultracode` plans a per-task Workflow this repo disables by default.

No `docs/design-decisions.md` entry: that file is for "non-obvious architectural choices," and its own §21 already points at the register as the cost-lever home. No CHANGELOG entry: nothing a stow consumer runs changes, and the most recent comparable register-only correction (`skill-fidelity-reviewer-yield-gap.md`, 2026-08-30) shipped with no CHANGELOG mention.

### Prescribed section content

Heading, matching the file's session-sourced convention:

`## From a 2026-09-01 session — cross-check against Anthropic's "Optimizing for cost and intelligence" guide`

Preamble (two sentences): name the source URL and the read date; state the table carries one row per guide lever, and that the guide's own "Measure on Your Own Workload" method — production-weighted tasks with outcome checks, priced across model tiers and effort levels before any multi-model arrangement is added — is what this register already is, so it gets no row of its own.

Table header: `| Guide lever | Verdict | Coverage, or why not reachable |` (not the file's default `| Lever | Verdict | Measured reason |` header — most reasons here are citations, not measurements; the file already varies its header per section, e.g. `handoff-threshold-impact-analysis.md` uses `| Question | Verdict | Headline figure (scope) |`).

Rows, in the guide's own order:

1. **Prompt caching (cache repeated context)** — *Already covered, root-caused* — The "Context cost root cause" section above traced idle-gap rebuilds to concurrent-session switching (92.9%), not operator breaks (7.1%). `transcript_analysis/pricing.py` independently encodes the same multipliers the guide prices against: 5-minute write 1.25x, 1-hour write 2x, cache read 0.1x.
2. **Pick the cache duration (5-minute vs. 1-hour)** — *Reopened on a new mechanism; verdict unchanged* — Supersedes the cache-TTL thread running through three earlier entries (the original "Cache-TTL selection" row, its 2026-08-15 follow-up, and the "Cache TTL as a uniform, non-account-scoped property" row under `cost-attribution-integrity.md`). A mechanism none of them covered exists: `experimental.cacheTtl` in subagent frontmatter selects `5m` or `1h` per agent (Anthropic, *Create custom subagents*), the first cache-duration control living in repo-tracked config rather than per-machine environment. Still not worth pulling: the 1-hour tier costs 2x base per write against the 5-minute tier's 1.25x, so it pays only when a prefix is re-read after a gap past five minutes, and the field sits in an `experimental.` namespace, not a stable surface for repo-shared config. Not measured for subagents: the 96–97% sub-5-minute idle-gap concentration behind the 2026-08-15 verdict was taken on main-thread calls; `cache-rebuild` includes subagent groups but reports no main/sidechain split, so a subagent-scoped figure needs a `transcript-analysis.py` change, not a rerun.
3. **Trim tokens** — *Already covered; all three sub-levers adopted by a different mechanism* — Deferring unused tool definitions: `disableArtifact`/`disableWorkflows` default to `true`, measured 16k-token drop (23.8k→7.8k), see the `disable-artifact-workflow-default.md` section above. Keeping bulk data out of the prompt: the guide's Files-API-plus-code-execution mechanism has no Claude Code surface, and the reachable equivalent is already mandatory — "Locate before a whole-file read" in the global `CLAUDE.md`, plus pass-a-path-not-the-content in `brief` and `handoff`, with one documented exception in `ready-for-review` for a subagent holding no `Bash` to resolve a path itself. Managing the context lifecycle: the handoff-nudge program, retuned against `$/PR` (`handoff-nudge-cap-recalibration.md` above).
4. **Audit prompts against the current model** — *Swept 2026-09-01, nothing found* — The guide targets stale over-specification: "verify twice," "maximally thorough," mandatory step-by-step, hand-rolled reasoning scratchpads, retired thinking-budget settings, contradictory rules. A sweep across `claude/.claude/`, both `CLAUDE.md` files, and `docs/` returned zero instructional hits for any of those shapes; the only "step-by-step" and "scratchpad" matches are descriptive prose or unrelated senses (a filesystem scratch path, the ephemeral-isolation worktree primitive). Recorded so the next reader of this guide doesn't re-run the sweep.
5. **Batch processing (50% discount, 24-hour turnaround)** — *Rejected, no exposed surface* — Nothing in `settings.json`, hooks, or agent frontmatter exposes a batch-submission path, so a session cannot route its own turns through it — the same no-surface shape as the original `cache_control` rejection above. The guide forecloses it directly: "Not available for Claude Managed Agents sessions (interactive by design)." Zero prior mention anywhere in this repo, which is why it gets a row rather than a pointer.
6. **Workspace spend limits** — *Outside this repo's surface — observability yes, enforcement no* — A workspace spend limit is an Anthropic Console/account setting, not a `settings.json`, hook, or frontmatter one. What this repo owns is observation: the opt-in local ledgers (`docs/cost-ledger.md`, `docs/pr-cost.md`) and the statusline dollar display. No hook here enforces a spend cap, and building one is a separate decision.
7. **Model choice priced per completed task, not per token** — *Already practiced, under different names* — The guide's phrasing appears nowhere here, but the method is in use twice. `opus-plan-boundary-handoff.md` above priced three model arms against 95 sessions' real post-plan-boundary turn sequences in dollars with a work-inflation breakeven, not per-token rates. `handoff-nudge-cap-recalibration.md` above tuned a threshold off a 145-row `$/PR` ledger. The per-token framing that does appear (`docs/auto-mode.md`'s "roughly 5x the per-token cost of Sonnet") is a routing heuristic for an accidental Opus dispatch, not a model-selection methodology.
8. **Effort level tuned per workload shape** — *Already covered; one vendor level deliberately out of reach* — Per-agent `effort:` pins are enforced by `EXPECTED_EFFORT` in `claude/.claude/hooks/tests/test_agent_roster.py`, with the task-shape-over-inheritance rationale in the global `CLAUDE.md` and `design-decisions.md` §24, which already cites the same Anthropic Effort and model-configuration docs this guide sits beside. One delta worth recording: Anthropic's *Model configuration* doc lists a sixth level, `ultracode`, absent from that test's `VALID_EFFORT_LEVELS`. The omission is deliberate — `ultracode` plans a dynamic workflow per task and this repo disables the `Workflow` tool by default (`design-decisions.md` §31) — and `VALID_EFFORT_LEVELS` exists to catch a typo in a value the repo actually assigns, not to mirror the vendor's list.
9. **Re-run failures at higher effort** — *Named, not investigated* — The one guide lever with no prior coverage and no verdict here. Effort pins are static per agent, and nothing re-dispatches the same agent at a higher effort after a failed outcome check, though the guide's precondition (checkable outcomes) holds via tests and the review gates. The nearest existing arrangement differs in kind: `code-writer` runs at `high` with a downstream review pass as backstop (`design-decisions.md` §24), escalating to a different agent rather than re-running the same one. Closing this needs a measurement nobody has run — what share of `code-writer` dispatches fail their own downstream check, and what a lower starting tier would cost against that share.
10. **Set budgets and output caps** — *Rejected, no exposed surface* — Subagent frontmatter (Anthropic, *Create custom subagents*) carries no `task_budget`, `max_tokens`, or output-token-cap field; the only budget-adjacent fields are `maxTurns` (a turn count, not a token limit) and `experimental.cacheTtl` (row 2). `max_tokens` is set by the API caller, which is Claude Code, not this repo. `maxTurns` is reachable and truncates a dispatch rather than pricing it — a safety cap, not a cost lever.
11. **Multi-model architectures (Advisor, Orchestrator)** — *Already implemented, under different names* — Advisor: `plan-architect`'s `MODE=consult` dispatch is a cheap executor escalating one hard design decision to an Opus-pinned read-only agent on demand (`design-decisions.md` §37), and `MODE=plan-sections` is the same escalation on a fixed trigger (§30). Orchestrator: a Sonnet-default parent decomposes and delegates bulk work to `code-writer` and the reviewer roster, with the global `CLAUDE.md`'s Model & Effort Routing rule fixing which tier each dispatch gets. Recorded honestly: the guide's precondition — a multi-model configuration must beat a single model's entire score-vs-spend curve — was never measured here. Both arrangements were adopted on other grounds, not as a validated curve win.

### Assumption ledger

**Root.** No single place records whether this repo's cost program covers what Anthropic's current cost guide recommends, so a future session reading that guide re-investigates levers already closed here — and two levers it names (batch processing; budgets and output caps) have in fact never been checked in this repo at all.

**Givens** (conditions the design treats as fixed and beyond its reach):

- **G1.** The guide's lever list is vendor-owned and fixed; this plan conforms its row set to that list rather than choosing its own taxonomy. *Vendor owns the source.*
- **G2.** Batch submission, task budgets, output-token caps, and workspace spend limits are API-platform or Console surfaces that Claude Code does not expose to `settings.json`, hooks, or agent frontmatter. This repo cannot make them reachable. *Vendor owns the surface.*
Two conditions the plan could change but deliberately doesn't — restructuring the register's established format, and re-deriving figures it cites from earlier sections — are recorded in **Out of scope** below with their reasons, not listed here as givens.

**Rows:**

1. `docs/cost-levers-considered.md` is the correct and only home for these findings; `docs/design-decisions.md` needs no entry. `[verified: docs/cost-levers-considered.md:1-17 declares itself the cost-scoped sibling; docs/design-decisions.md:3 declares itself for non-obvious architectural choices; §21 at design-decisions.md:262 points at the register as the cost-lever home]` `anchors: root`
2. Appending one dated `##` section is the lightest mechanism that fits. Two lighter primitives fail: (a) chat-only findings leave nothing durable — the exact failure §21 records the register fixing; (b) rows for only the new levers force the next reader to re-derive coverage for the other eight from a 320-line file. Two heavier ones also fail: a new `docs/case-studies/` file (that directory holds measurement-backed records; this audit ran no measurement), and a `design-decisions.md` entry (duplicates row 1's home). `[verified: docs/cost-levers-considered.md structure; docs/case-studies/ entries are all measurement-backed]` `anchors: root`
3. The register accepts non-measured reasons for a lever a mechanism forecloses. `[verified: docs/cost-levers-considered.md:66 "A hook cannot see how much work remains"; :25 "nothing in settings.json, hooks, or env vars exposes this field"]` `anchors: row2`
4. The register accepts an open, uninvestigated lever as a row — row 9's "Named, not investigated" verdict has precedent. `[verified: docs/cost-levers-considered.md:50 and :274 both use "Named, not solved"]` `anchors: row2`
5. A new-section row that cites and corrects a prior row is the register's established pattern for a cross-section correction, and adds no back-pointer to the corrected row. `[verified: docs/cost-levers-considered.md:267, the cost-attribution-integrity.md row, corrects the :25 row by name with no reciprocal edit]` `anchors: root`
6. `experimental.cacheTtl` exists as a per-subagent frontmatter field selecting `5m` or `1h`, and there is no `task_budget`/`max_tokens`/output-cap field in that table. `[engineer-verified — the dispatching session fetched https://code.claude.com/docs/en/sub-agents directly this session]` `anchors: root`
7. `ultracode` is a sixth vendor effort level absent from `VALID_EFFORT_LEVELS`. `[engineer-verified — the dispatching session fetched https://code.claude.com/docs/en/model-config directly, twice independently]`, cross-checked against `[verified: claude/.claude/hooks/tests/test_agent_roster.py:113]` `anchors: root`
8. Widening `VALID_EFFORT_LEVELS` to admit `ultracode` would make it strictly worse at its stated job. `[verified: claude/.claude/hooks/tests/test_agent_roster.py:452-462 — the docstring names typo-catching as the purpose]` `anchors: row7`
9. The 1-hour cache tier costs 2x base per write against the 5-minute tier's 1.25x, so buying it is only rational when a prefix is re-read past the 5-minute boundary. `[verified: claude/.claude/scripts/transcript_analysis/pricing.py — _CACHE_WRITE_1H_MULTIPLIER = 2, _CACHE_WRITE_5M_MULTIPLIER = 1.25]` `anchors: row6`
10. A subagent-scoped idle-gap rebuild figure is not obtainable from the existing toolkit without a code change, so row 2's verdict rests on price ratio plus the main-thread measurement's direction, not on a subagent measurement. `[verified: transcript-analysis.py:5627 hardcodes include_subagents=True and :5683 partitions per group, but the report at :5647-5658 aggregates with no main/sidechain split]` `anchors: row9`
11. Exploration finding ("no cost-per-completed-task framing anywhere") is a phrase-grep result, not a methodology gap, and row 7 closes it rather than recording it as one. `[verified: docs/cost-levers-considered.md:180-184 prices three model arms in dollars against 95 sessions' real turn sequences; :280 tunes a threshold off a 145-row $/PR ledger]` `anchors: root`
12. A docs-only change under `docs/` is domain-selected by `select-tests.py`, not unmapped and not a full-suite fallback. `[verified: claude/.claude/scripts/select-tests.py:328 — (lambda p: _is_under(p, DOCS_DIR), (HOOKS_TESTS_DIR, SKILLS_TESTS_DIR))]` `anchors: root`
13. New prose in this file is covered by the per-account-state-path contract, so it may not contain a literal `~/.claude`, `$HOME/.claude`, or `${HOME}/.claude`. `[verified: claude/.claude/skills/tests/test_skills.py:2885-2947 — _all_doc_paths() globs docs/**/*.md excluding only reports/ and case-studies/; test_doc_has_no_state_path parametrizes over it]` `anchors: row12`
14. No index enumerates this register's sections, so appending one needs no index update. `[verified: grep for cost-levers-considered across the repo returns only prose cross-references from design-decisions.md, docs/transcript-analysis.md, CHANGELOG.md, and three case studies — none listing its sections]` `anchors: root`
15. A docs-only register append needs no CHANGELOG entry. `[verified: the 2026-08-30 skill-fidelity-reviewer-yield-gap.md register section shipped with zero CHANGELOG mentions of its subject]` `anchors: root`
16. No agent in this repo sets `maxTurns`, making row 10's "reachable and unset" clause true. `[unverified]` — Verification step 1 settles it before the row is written; if any agent does set it, drop that clause. `anchors: row6`
17. Whether `effort: ultracode` would be *accepted* in agent frontmatter is unknown. Row 8's text deliberately does not depend on it — it rests on the `Workflow` tool being disabled by default, which is true either way. `[unverified]` `anchors: row7`
18. No link-checking test was found in the domain-selected suites, so section cross-references use prose section names rather than markdown anchors, matching the file's dominant style. `[unverified — no exhaustive search for a link checker was run]` `anchors: row12`

## Critical files

**Modify — one file, one dispatch:**

- `docs/cost-levers-considered.md` — append the new `##` section at end of file, after the `skill-fidelity-reviewer-yield-gap.md` section (currently the last). Chronological order holds: the two preceding sections are dated 2026-08-25 and 2026-08-30.

**Create:** none. **Delete:** none.

**Explicitly not modified**, so a reviewer doesn't read their absence as an oversight: `docs/design-decisions.md` (ledger row 1), `claude/.claude/hooks/tests/test_agent_roster.py` (rows 7-8), `CHANGELOG.md` (row 15), any file under `claude/.claude/agents/` (Out of scope), and the earlier sections of the register itself (Approach, second sub-decision).

**Reuse:** the file's own section-per-source heading form, three-column table grammar, and verdict vocabulary (`Adopted`, `Rejected`, `No change needed`, `Named, not solved`, `Corrected`, and the register's habit of qualifying a verdict with a short clause). No new file, no new helper, no new convention.

**Dispatch split:** one `code-writer` dispatch. Single file, single coherent edit — the split test ("non-overlapping file sets each specifiable without restating the other's context") is not met by any partition of this work.

## Verification

Run from the worktree root:

1. **Before writing row 10**, settle ledger row 16 with one grep: `maxTurns` across `claude/.claude/agents/*.md` and `plugins/*/agents/*.md`. Zero hits keeps the "reachable and set on no agent here" clause; any hit drops it.
2. **`.venv/bin/python3 claude/.claude/scripts/select-tests.py`** — the project's documented required local command. A `docs/`-only diff is domain-selected, not unmapped: `select-tests.py:328` maps it to `claude/.claude/hooks/tests` and `claude/.claude/skills/tests`. Expect a domain-selected run of those two directories, not the full suite and not a no-op.
3. Two tests in that selection actually read the new bytes, and both must pass:
   - `test_skills.py::TestPerAccountStatePathContract::test_doc_has_no_state_path` — parametrized over `docs/**/*.md` minus `reports/` and `case-studies/`, which includes this file. **Constraint on the prose:** no literal `~/.claude`, `$HOME/.claude`, or `${HOME}/.claude`. Write `<config-dir>/…` or the repo-relative `claude/.claude/…` stow-source path instead.
   - `test_nudge_transcript_toolkit.py::TestNeverFiresOnMarkdown` — builds its corpus from a repo-wide `rglob("*.md")` content scan, so new markdown enters it.
4. `test_doc_counts.py` pins counts appearing in `docs/design-decisions.md` and `README.md`. Nothing here pins `cost-levers-considered.md`, and this plan touches neither pinned file, so it stays green. It becomes relevant only if a reviewer pushes for a `design-decisions.md` entry — which ledger row 1 rejects.
5. `ruff` and `shellcheck` are not implicated: no Python and no shell change.
6. **Manual format check against the file's own conventions**, since no test asserts register prose:
   - Heading matches the session-sourced form already in the file (`## From a 2026-08-15 session measurement — "…"`).
   - Table is three columns with the section-appropriate header, and every row's reason traces to either a named repo file/section or a named vendor doc URL.
   - Cross-references name a section by its source-plan filename, never by line number.
   - The only verbatim guide quote is "Not available for Claude Managed Agents sessions (interactive by design)" in row 5. Do not introduce further verbatim quotes without re-fetching the guide to confirm them.
7. `deny-private-project-refs.sh` fires on the `Write`/`Edit` itself. Content is public Anthropic doc URLs plus this repo's own history, so no denial is expected.
8. `/code-review` before commit, per the repo's hook-enforced pipeline. No `/skill-review` (no `SKILL.md` staged), no `/agent-review` (no agent file), no `plugin-semver` (nothing under a plugin directory).

CI runs the full suite on push, per README.md's Tests section.

## Out of scope

- **Adopting any lever this section records.** No `experimental.cacheTtl` on any agent, no `maxTurns` added anywhere, no effort re-pinning, no model re-routing. The audit records verdicts; adopting one is its own change with its own review.
- **Adding `ultracode` to `VALID_EFFORT_LEVELS`.** Ledger row 8 — it would make a typo guard strictly worse.
- **Any `docs/design-decisions.md` entry.** Ledger row 1.
- **A CHANGELOG entry.** Ledger row 15.
- **Building a main/sidechain split in `cache-rebuild`** so row 2's subagent idle-gap question could be measured. That is a `transcript-analysis.py` change and a measurement project; row 2 states the gap instead of closing it.
- **Running the row 9 measurement** (`code-writer` downstream-check failure rate against starting effort tier). Row 9's verdict is "Named, not investigated" precisely because closing it is a separate piece of work.
- **Designing a spend-limit hook.** Row 6 notes no such hook exists; proposing one is a new feature, raised separately rather than bundled here.
- **Editing prior sections' rows, or adding back-pointers to them.** Approach, second sub-decision — the register appends, and the `cost-attribution-integrity.md` precedent adds no reciprocal edit.
- **Re-measuring any figure this section cites from an earlier section.** Those figures are one-off scans recorded in their own sections, several explicitly flagged non-rerunnable — this section cites them, it does not re-derive them; re-deriving any of them is a separate measurement project.
- **Restructuring `docs/cost-levers-considered.md`'s section-per-source format, table grammar, or verdict vocabulary.** Established across 20 prior sections; changing it would mean re-litigating all of them, a separate decision from this audit.
