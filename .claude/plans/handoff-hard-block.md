# Tighten the handoff-nudge hard block and reference agent output on handoff

## Context

**Goal: make the existing handoff-nudge hard block actually engage soon after it's ignored, fix a bug that currently makes it dangerous to tighten, and point a written handoff at subagent output the session hasn't yet synthesized.**

The engineer reported sessions repeatedly blowing past the handoff-nudge hook's context threshold before ever handing off, showing a transcript where a session hit 485k tokens after dismissing two advisory nudges ("I'm almost done" — reasonable each time in isolation, but the pattern recurs). He asked for the nudge to become a hard block soon after being ignored, for background subagents to never be stopped by that block, and for unprocessed subagent output to be captured into a secondary file as part of handoff.

Discovery found the hard block already exists (PR #636, `HANDOFF_NUDGE_BLOCK_AFTER`, default 3) but almost never engages in practice, and — independent of anything this plan changes — its exit path has a live inversion bug on one of its two hook registrations that lowering the block-after count would turn from latent to routinely triggered. Discovery also found the harness already durably persists every subagent dispatch's full transcript to disk, independent of the parent session, which changes "capture output" from a new-mechanism problem into a reference problem.

## Approach

**Concluded design, in two parts:** (1) tighten `HANDOFF_NUDGE_BLOCK_AFTER`'s default from 3 to 1 and fix a pre-existing exit-code inversion bug in the same hard-block path that the tightening would otherwise turn from dormant to live; (2) have the `handoff` skill name any not-yet-synthesized subagent dispatch by its harness-assigned `agent-<agentId>`, inside its existing "mid-flight" clause, rather than building a new file-capture mechanism — the content is already persisted by the harness.

Both parts stay inside files this plan already owns. Neither touches `HANDOFF_NUDGE_ABS_CAP`, `PCT_THRESHOLD`, or `HANDOFF_NUDGE_REARM_SPACING` — a peer session (coordinated live, this session, via cross-session message) is independently regrounding the absolute-cap value; the engineer separately confirmed this plan must not hardcode or relitigate that constant.

**Alternatives considered**, inline per mechanism, are in the ledger below (each mechanism row carries its own rejected-lighter-primitive pair, per the over-powered-primitive gate).

### Assumption ledger

**Root: the shipped hard-block escalation ladder (PR #636) essentially never engages before a session's context balloons far past its intended ceiling, and the handoff skill has no reference to subagent output the harness already persists — so sessions both run too long and lose synthesis of work already done when they finally do hand off.**

**Givens:**

| # | Given | Reason |
|---|---|---|
| 1 | `HANDOFF_NUDGE_ABS_CAP` / `PCT_THRESHOLD` / `HANDOFF_NUDGE_REARM_SPACING` are fixed inputs this plan does not touch. | Another party owns it: a peer session is actively regrounding `ABS_CAP` this same session (cross-session-coordinated, scope confirmed both directions — no file/line overlap); the engineer independently confirmed this plan must not hardcode or relitigate it. `[engineer-verified]` |
| 2 | Claude Code's per-hook-event exit-2 semantics (`PostToolBatch` stops the loop; `Stop` forces continuation) are a vendor-fixed harness contract, not something this hook can redefine. | Vendor/protocol imposes it. `[verified: code.claude.com/docs/en/hooks, fetched this session — "PostToolBatch: Stops the agentic loop before the next model call" vs "Stop: Prevents Claude from stopping, continues the conversation"]` |
| 3 | A background Task/Agent dispatch is torn down only by an explicit `TaskStop` call; the hook process itself has no tool-calling capability and never invokes one, so it cannot terminate an in-flight subagent regardless of this plan's changes. | Harness-imposed capability boundary. `[verified: TaskStop tool schema, fetched this session — "Stops a running background task by its ID"; the hook's block path is pure shell (exit 2), it calls no harness tool]` |

**Mechanisms:**

| Mechanism | Justification | Anchor |
|---|---|---|
| Lower `HANDOFF_NUDGE_BLOCK_AFTER` default 3 → 1 | Band arithmetic (block point = `THRESHOLD + BLOCK_AFTER × REARM_SPACING`, all held fixed per given #1) gives 440k at 1, 520k at 2, 680k at today's 3. Only 1 would have caught the engineer's own reported 485k incident — 2 already blocks later than where he interrupted the session himself. Corroborated (not led by) this session's own corpus scan: of 426 fired sessions across 6 accounts, 60 reach a second fire (the point BLOCK_AFTER=1 trips) vs. only 3 ever reaching a fourth (today's trip point), and none of those 3 show a recorded `/handoff` conversion afterward. The corpus figure is right-censored — it shows where sessions happened to stop, not where they should have been stopped — so it corroborates but does not by itself justify the value; the incident arithmetic is the primary grounding. Engineer confirmed 1 over 2 directly, citing exactly this cost-ballooning risk. `[engineer-verified]` | root |
| Fix the `Stop`-registration exit-2 inversion in the same hard-block path | The block's `exit 2` (`nudge-handoff-near-context-cap.sh:605-613`) has no `HOOK_EVENT` guard, but the hook is registered on both `PostToolBatch` and `Stop` (`settings.json:183,429`). Per given #2, on `Stop` this *forces the conversation to continue* — the opposite of the intended block, and the stderr's own "finish it in this reply" escape hatch becomes unreachable on that path. Today this is latent (only 3 sessions in the whole corpus ever reached the current trip point); at `BLOCK_AFTER=1` it becomes live in every escalating session. Must land with or before the default change, not after. | given #2 |
| Add `action=block` to the hard-block fire's existing `nudged` log line (not a new line-kind); exclude `action=block` entries from `_operator_response_lag_from_log`'s population | Today's `nudged` line is byte-identical whether emitted by the advisory path or the hard-block path (`nudge-handoff-near-context-cap.sh:608-609` vs. `:627-628`) — confirmed directly against the source this session, and confirmed as the exact reason this session's own corpus grounding couldn't tell whether any historical streak-4 fire was ever actually blocked. An additive field parses under `_parse_nudge_log_entries`'s existing superset-field check (`transcript-analysis.py:9324`) with zero risk of being silently dropped by an already-shipped consumer. A forced (non-voluntary) block-triggered overshoot is not the same signal as voluntary operator-response lag, so leaving it in `_operator_response_lag_from_log`'s population would contaminate the exact metric `HANDOFF_NUDGE_REARM_SPACING` (given #1, not touched by this plan) was itself grounded on. | root; given #1 |
| Handoff skill: name a not-yet-synthesized subagent dispatch by its `agent-<agentId>` basename inside the existing §2.5 "mid-flight" clause; no new section, no new file | Subagent transcripts already persist independently to `<config-dir>/projects/<slug>/<session-id>/subagents/agent-<agentId>.jsonl` with a small `.meta.json` sidecar (`agentType`, `description`, `model`) — confirmed directly on this session's own disk (4 dispatches, `.meta.json` present at spawn time, `.jsonl` still growing on the still-running one) and against `code.claude.com/docs/en/sub-agents`'s own description of the same mechanism. The gap was never lost content — it was that nothing pointed to it. Engineer confirmed this reinterpretation (reference over duplicate) directly. `[engineer-verified]` | root |
| Cite only the `agent-<agentId>` basename, never the full transcript path | The full path embeds this session's own session_id as a directory segment (confirmed by resolving this session's own dispatched-agent paths) — quoting it would violate the handoff skill's own existing rule at `SKILL.md:18` ("Do not quote the raw `session_id` into prose that may reach a commit, PR body, or handoff file"). The basename plus the project slug (already derivable from §4's own worktree line) is sufficient for a resuming session to resolve the file via a session-id-wildcarded glob. | (amends the mechanism above — same anchor) |

**Other assumptions:**

| # | Assumption | Tag |
|---|---|---|
| 4 | After the first hard block in a session, every subsequent re-arm in that same session also hard-blocks — the ignored-re-arm counter is monotonic within a session and is reset only by a genuine `/handoff` conversion (the skill's own conversion step removes the `-ignored` marker) or the global kill-switch; there is no in-session escape. | `[verified: nudge-handoff-near-context-cap.sh:594-603]` — accepted as an intentional cost of `BLOCK_AFTER=1`, not something this plan adds a workaround for. Adding one would let the blocked agent `rm` its way past its own block, which stops being a block. |
| 5 | A resumed session (fresh, via `/handoff`'s `resume-context`) can reach a peer session that is still open, to ask about an in-flight background dispatch, via `SendMessage`-style peer addressing. | `[verified: SendMessage tool schema, fetched this session — a background subagent's result routes to "main" = the spawning session; peer-session addressing by name is separately documented and demonstrated live in this session]`. Whether a *fresh* session's own agent-listing call would directly surface a *different* session's in-process subagents (as opposed to only peer full-sessions) was **not** independently confirmed either direction. `[unverified — flagged for plan-review]`. This gap doesn't block the design: the handoff-skill wording routes through peer-session messaging, which is the verified path, not through direct cross-session subagent addressing. |

## Critical files

**Modify:**

- `claude/.claude/hooks/nudge-handoff-near-context-cap.sh`
  - `resolve_block_after()` (~line 171-174): default `3` → `1`.
  - Hard-block condition (~line 605): guard with `&& [ "$HOOK_EVENT" = "PostToolBatch" ]` so on `Stop` the same ignored-count condition falls through to the existing advisory-fire path below instead of exiting 2.
  - Hard-block's log-line `printf` (~line 608-609): append `action=block` after the existing `event=%s` field.
  - Hard-block's stderr message `printf` (~line 611-612): append a short mention that the block-after count is `HANDOFF_NUDGE_BLOCK_AFTER`, overridable by the engineer for a workflow that finds the new default too aggressive — today's message names only the global kill-switch's implicit route (`/handoff` or finish now), with no discoverable graduated alternative. Safe to surface here: this is a session-scoped env var read at hook-fire time from the calling process's environment, and per this harness's own contract shell state (env vars) does not persist across separate Bash tool calls — so a blocked agent cannot durably raise its own threshold mid-turn by exporting it; only the engineer's own shell/profile configuration makes the override take effect.
  - Header comment (lines 9-13, 25-27): note the event-guard fix and the new log field.
- `claude/.claude/scripts/transcript-analysis.py`
  - `_parse_nudge_log_entries` (~9292-9334): add the `"action"` key to a `nudged` entry's dict **only when the field is present in the line** (absent-key, not an always-present `None`) — the existing `test_all_three_line_shapes_are_parsed` and `test_malformed_lines_are_skipped_without_raising` tests (`transcript-analysis.py` tests, ~14571-14589) assert exact dict equality on lines with no `action` field; an always-present `None` value breaks both, an absent-key addition leaves them untouched.
  - `_operator_response_lag_from_log` (~9346-9386): skip entries with `action == "block"` from the lag population, **and increment `excluded`** for each one skipped this way — mirroring this function's own existing "every skip is counted, never silently dropped" convention (`test_no_match_is_excluded_and_counted_not_silently_dropped`). Comment why (forced overshoot isn't voluntary operator-response lag).
- `docs/handoff-nudge.md`
  - "Why this block-after count" (~line 24): replace with the band-arithmetic grounding tied to the reported incident; note the corpus figure was consulted and is right-censored (corroborating, not primary); note the `Stop`-registration bug this change required fixing; note stickiness (assumption #4) as an intentional, documented cost; note that the block-after count is discoverable and overridable by the engineer via `HANDOFF_NUDGE_BLOCK_AFTER` (see the stderr-message addition below) for a workflow that finds the new default too aggressive.
  - "Log location" table (~line 83-88): document the optional `action=block` field on hard-block fires.
  - "Recovering from a hard block" (~line 26): add the stickiness note (assumption #4) — every re-arm blocks post-first-block, not just the triggering one.
  - "Known limitations" (~line 115-127): add a new bullet — the `Stop`-registration guard fix means a session whose context growth happens entirely on toolless turns (the exact case the dual `PostToolBatch`+`Stop` registration exists to cover, per "What the hook does") now gets advisory-only nudges with no escalation ever, regardless of `BLOCK_AFTER`, since the hard-block path only ever fires on `PostToolBatch`. This is the correct fix for the exit-code inversion, but the coverage consequence needs to be named, not left implicit.
- `claude/.claude/skills/handoff/SKILL.md`
  - §2.5 (~line 61): amend the mid-flight sentence to name a still-running background subagent dispatch by its `agent-<agentId>` (never the full path), instructing the authoring session to record — as part of the note, not as free-floating advice — whether this session is still open (so a resuming session checks there before re-dispatching). Exact proposed replacement text:

    > If the handoff reason is context-limit, note what was mid-flight: open tool calls — including any still-running background subagent dispatch, named by its `agent-<agentId>` (never its full transcript path, which embeds this session's own id) — noting whether this session is still open, so a resuming session can address it by this session's name rather than re-dispatching (never by session_id) — and pending verifications (see §2.6 for task-list state).
  - Pre-write checklist (~line 142): tighten the existing "§2.5 names what was mid-flight" line to reference the same instance, rather than adding a new checklist item.
- `claude/.claude/hooks/tests/test_nudge_handoff_near_context_cap.py`
  - `DEFAULT_BLOCK_AFTER` (line 55): `3` → `1`; update its comment.
  - `test_escalation_counter_concurrent_rearms_no_lost_update` (~1090-1131), `test_second_fire_allowed_after_rearm_spacing` (~704-719), `test_rearm_boundary_at_last_fired_plus_spacing`'s `expect_fire=True` case (~721-742), `test_three_fire_sequence_rearms_twice`'s third call (~793-812): each currently drives a real re-arm (the first ignored re-arm, `IGNORED_COUNT=1`) and asserts it stays advisory (`returncode == 0`, non-empty stdout) — true only at `BLOCK_AFTER >= 2`; at the new default 1, `IGNORED_COUNT=1 >= BLOCK_AFTER=1` now hard-blocks there instead. These four tests are about re-arm/marker mechanics, not the escalation ladder itself, so pin `HANDOFF_NUDGE_BLOCK_AFTER` explicitly in each one's env to a value **strictly greater than 3** — not the sibling escalation-ladder tests' literal `"2"` (~1133-1197): `test_escalation_counter_concurrent_rearms_no_lost_update`'s own docstring documents `ignored_count` legitimately reaching 2 under genuine concurrent overlap, so pinning to `"2"` there would make that specific test flaky (red only under genuine-overlap ordering) rather than consistently either red or green.
  - `test_block_after_malformed_override_falls_back_to_default_not_zero` (~1202-1223): its docstring ("must fall back to the shipped default rather than degrade BLOCK_AFTER toward 0... which would hard-block on the very first re-arm") is no longer accurate at the new default and must be rewritten — the invariant it protects has moved. Its *first* call (line ~1213, `assert first.stdout.strip() != ""`) already correctly discriminates a real fallback from a degraded-to-0 value — trace the hook's own logic: the `IGNORED_COUNT >= BLOCK_AFTER` check runs even on a session's very first-ever crossing (`IGNORED_COUNT=0` there, since the marker is untouched pre-first-fire), so a degraded `BLOCK_AFTER=0` would hard-block with *zero* advisory nudges ever (`0 >= 0` true), while a correct fallback to 1 stays advisory (`0 >= 1` false) — leave this assertion as-is, and add `assert first.returncode == 0` alongside it for symmetry with the sibling test's fix below. Its *second* call (lines ~1215-1222, asserting the first re-arm stays advisory) is no longer correct at the new default — a real, correctly-configured default=1 session *should* hard-block at that exact point, identically to a degraded `BLOCK_AFTER=0` (both `0 >= 0`-style checks on the first re-arm's `IGNORED_COUNT=1` are true) — so this specific call can no longer distinguish correct-fallback from a bug. Drop the assertion on this second call rather than flipping it to expect a block: the sibling positive-control test below already asserts that exact "does the real default correctly block at the first re-arm" fact, and duplicating it here would re-litigate the same signal under a test whose name and remaining docstring are about the *first*-fire distinction, not block timing.
  - `test_block_after_malformed_override_positive_control_blocks_at_default` (~1224-1251): `range(DEFAULT_BLOCK_AFTER - 1)` becomes `range(0)` at default 1, collapsing the loop — the test still mechanically passes (the lone pre-loop call becomes the first fire, the post-loop call becomes the first re-arm and correctly hard-blocks), but per the same trace above it can no longer alone distinguish "fell back to 1" from "degraded to 0" (both block at that second call identically) — that discrimination now lives entirely in the sibling test's first-call assertion above. Add `assert result.returncode == 0` to this test's own pre-loop call (currently only checks stdout, line ~1236) so this test doesn't silently pass under a degraded value either, rather than relying solely on the sibling test to catch it.
  - New test: the hard-block path exits 2 only when `HOOK_EVENT=PostToolBatch`; under the identical ignored-count/block-after condition with `HOOK_EVENT=Stop`, it falls through to the advisory JSON-envelope path instead (regression test for the inversion bug fixed above). Mirror the file's existing `HOOK_EVENT_NAMES`-parametrized pattern used for the advisory-fire tests.
  - New test (or extend an existing hard-block test): the hard-block fire's log line carries `action=block`; an ordinary advisory fire's does not.
- `claude/.claude/scripts/tests/test_transcript_analysis.py`
  - New/updated cases for `_parse_nudge_log_entries` capturing `action=block`, and for `_operator_response_lag_from_log` excluding those entries — mirror this file's existing test style for those two functions.
- `claude/.claude/hooks/tests/test_doc_counts.py`
  - New `_count_handoff_nudge_block_after_default()`, mirroring `_count_handoff_nudge_abs_cap_default`'s *behavioral* pattern (fire the hook against a synthetic transcript and read the value back out), not `_count_handoff_nudge_threshold_percentage`'s source-scanning regex — a regex over the fallback arm's literal would only prove the literal exists, not that it's the value the runtime path actually uses. The hard-block stderr message already embeds `HANDOFF_NUDGE_BLOCK_AFTER=%s` at runtime (per the stderr-message addition above), giving this a value to read back the same way `_count_handoff_nudge_abs_cap_default` reads its own computed threshold back from `additionalContext`.
  - New `DocCountFact` entry wiring it to the default-value occurrence(s) this plan adds to `docs/handoff-nudge.md`'s "Why this block-after count" section.

**No change needed (confirmed during discovery):**

- `docs/hooks.md:33` — already defers to `docs/handoff-nudge.md` for the numeric default and doesn't restate it; its prose ("hard-blocks... instead of staying advisory") stays accurate regardless of the value or the event-guard fix.
- `claude/.claude/hooks/tests/test_agent_roster.py`, `test_hook_alignment.py` — confirmed during discovery to carry no assertions this change interacts with (see discovery notes; re-verify only if code-writer's own diff review suggests otherwise).

**Reuse (do not reimplement):**

- `_ignored_marker_path` test helper (`test_nudge_handoff_near_context_cap.py:313-317`) for the new `Stop`-guard test.
- The file's existing `HOOK_EVENT_NAMES`-parametrization pattern for the new regression test.
- The file's existing malformed-value guard test pattern (~1199-1222) as the template shape for the redesigned positive-control test.
- `docs/handoff-nudge.md`'s existing "Recovering from a hard block" mechanics (kill-switch, per-session `-ignored` marker reset) — extend with the stickiness sentence only, do not restate the mechanism.

## Verification

1. `../../../.venv/bin/pytest claude/.claude/` — full suite, including the new/updated tests listed above.
2. `scripts/list-shell-files.sh | xargs -0 ../../../.venv/bin/shellcheck` — the hook file is touched.
3. Manual: `claude/.claude/hooks/nudge-handoff-near-context-cap.sh --check` in this worktree still returns a well-formed `status:"ok"` object — this path is untouched by the plan, confirming no regression.
4. The `Stop`-vs-`PostToolBatch` exit-code semantics this plan's fix depends on were independently verified this session against `code.claude.com/docs/en/hooks` (fetch date recorded in the ledger above) — no further verification needed, but re-confirm if that page's exit-2 table changes before this ships (mirroring this hook's own existing "re-verify by" convention for its other dated source comments).
5. `test_doc_counts.py` passes with the new `BLOCK_AFTER` fact wired in, proving the doc and the hook can't silently drift apart on this value going forward.

## Out of scope

- **`HANDOFF_NUDGE_ABS_CAP`, `PCT_THRESHOLD`, `HANDOFF_NUDGE_REARM_SPACING`.** Actively owned by a peer session this session coordinated with directly; the engineer separately confirmed this plan must not touch or hardcode against that value.
- **A corpus-based backtest of `BLOCK_AFTER`** (extending `rearm-backtest`/`_simulate_rearm_spacing` to gate on `fired_bands > block_after`, which would price the false-positive/dismissal-risk cost the same way `REARM_SPACING` itself was grounded). Mechanically cheap per this session's own read of the existing function, but deferred: the incident-arithmetic grounding above is sufficient justification for this specific change, and the same file already carries a peer session's active, unrelated edit — a follow-up, not bundled here.
- **An in-session escape from a hard block** (e.g., letting the blocked agent clear its own `-ignored` marker). Deliberately rejected — a block the blocked agent can undo from inside its own blocked turn isn't a block. The only exits stay a genuine `/handoff` conversion or the existing global kill-switch.
- **A new field on `--check`'s JSON contract** (e.g. surfacing `ignored_rearms`). `--check` has a documented, stable reason-vocabulary/JSON contract; not touched here.
- **The Reddit thread cited as inspiration for this work.** Unreachable from this environment's tools (reddit.com and web.archive.org both hard-blocked); not used to justify any part of this design.
- **Any specific "context rot" magnitude figure.** The Chroma primary source (trychroma.com/research/context-rot) supports the qualitative degrades-with-length claim; no specific number from it is cited anywhere in this plan.
