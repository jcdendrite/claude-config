# Skill-review gate: diff against the merge base

## Context

Goal: stop `plugins/skill-management/hooks/require-skill-review.sh` from denying the `git commit` that completes a merge whose only `SKILL.md` / `claude-skills/skills/plan-review/ROUTING.md` changes came in untouched from the default branch (GH-1076).

The hook derives its trigger, its structural-validator input, its empty-diff exit and its marker hash from a HEAD-relative `git diff --cached`. During a merge, every gated file the other parent brought in therefore reads as unreviewed staged content, and the only route past the gate is a `/skill-review` pass over content the committer did not author. The code-review gate (`_lib_gate_diff_base`, `claude/.claude/hooks/_lib.sh`) and the plan-review gate (#1075) already diff against a novel-content base. `git-feature-branch-sync` prescribes merge over rebase when review is in flight, so the trigger is routine. Intended outcome: allow that commit, and still deny when the merge resolution itself edits a gated file.

The engineer asked for the port in these words: "I'm hoping we can port the fix for plan review over to it."

`.claude/plans/merge-aware-review-gates.md` (#994) already specifies this port as its Phase 4; only Phases 1-2 shipped. This plan implements Phase 4 narrowed to what GH-1076 needs, and departs from that plan on the empty-diff sentinel (see Approach).

## Approach

Give the skill-review gate the same novel-content base the code-review gate already uses, and let it **disarm** — rather than consult a marker — when no gated file is novel to the commit, *except* mid-revert, where the synthesized base is a subtraction rather than a union of reviewed trees. Concretely: duplicate `_lib_gate_diff_base`'s six-function closure into the plugin's trimmed lib, add a seventh shared function that applies the revert exclusion, thread one resolved base through the plugin hook's trigger, structural-validator input and marker hash, and move `marker.sh`'s two `skill-review` arms onto the same `_lib_staged_diff_hash` the code-review arms already use, so write side and read side compute a byte-identical preimage.

Three departures from the obvious symmetric port are deliberate and stated here so they are not "fixed" later:

1. **No empty-base sentinel on the skill-review path.** `.claude/plans/merge-aware-review-gates.md` (#994) Phase 4's Verification bullet ("the same assertion, mirrored for `require-skill-review.sh:116`") assumed this hook's empty-diff exit is structurally identical to `require-code-review.sh`'s. It is not. This hook disarms one exit earlier, at the trigger, on a name-only diff scoped to exactly the pathspec set the marker hash uses (rows 3, 9, 10). The correct precedent is `docs/design-decisions/plan-review-gate-disarms-on-empty-active-plan-set.md`, not its "Why `_lib_code_review_marker_value` keeps its sentinel" section.
2. **Mid-revert the gate keeps today's HEAD-relative behavior**, on both the read and the write side (rows 19-21, M-8). `[settled at round 1: the ciso finding "the disarm's reach across in-progress states is not justified per state" — revert is excluded, not accepted-and-pinned.]`
3. **The whole-diff empty exit (`require-skill-review.sh:123`) is deleted, not threaded.** It is unreachable on an honest result after the trigger and its only reachable effect is a one-config-write fail-open (rows 3, 18). `[settled at round 1: ciso's defense-in-depth finding — deletion, not a base-conditioned second exit.]`

**Root problem:** `require-skill-review.sh` derives its trigger, its structural-validator input set and its marker preimage from a HEAD-relative `git diff --cached`, so during a merge every `SKILL.md` / `plan-review/ROUTING.md` the other parent brought in unchanged reads as unreviewed staged content — and the `git commit` that completes the merge cannot be released without a `/skill-review` pass over files already reviewed on their own PRs (GH-1076).

**Givens**

- **G-1** — A plugin hook cannot source the stowed `claude/.claude/hooks/_lib.sh`; shared logic is duplicated into `plugins/skill-management/hooks/_lib.sh`. The packaging boundary is Claude Code's, not this repo's (`plugins/skill-management/hooks/_lib.sh:9-16`). `[verified: that header read directly]`
- **G-2** — A conflict-free `git merge` creates its commit inside the initiating command, with no separate `git commit` tool call for any PreToolUse gate to see. Beyond this plan's reach: closing it needs a post-tool-call mechanism the hook system does not have (`.claude/plans/merge-aware-review-gates.md` § "Out of scope" residual 1, G-3). `[verified: that plan read directly; reproduction of the gated/ungated split relayed from this session's claim-verification dispatch]`
- **G-3** — `_lib_gate_diff_base`'s two trust anchors (`origin/<default>`, `HEAD`) are locally forgeable and are admitted on a non-dominance argument against G-2's zero-gate path, not on unforgeability. Settled in a merged decision record; re-opening it is a different change (`docs/design-decisions/merge-tree-base-recipe-for-gate-diff-base.md`, and rows 23/24 of the #994 plan). `[verified: _lib.sh:846-950 read directly]`
- **G-4** — The marker's write side (`marker.sh`, stowed by `./install.sh`, live on `git pull`) and its read side (the plugin hook, refreshed by `claude plugin install skill-management@claude-config --scope project`) update through two independent paths this repo cannot synchronize. A skew window is therefore unavoidable, not a defect to design away; `CHANGELOG.md:56` is the precedent for disclosing it. `[verified: CHANGELOG.md:56 and CLAUDE.md's marketplace-plugin section read directly]`
- **G-5** — On a machine with neither `timeout(1)` nor `gtimeout(1)`, every capped call in the copied closure runs **uncapped** (`_lib_capped_for`'s third arm), so the base resolves normally and a stalled `git` holds the gate open only until the harness's own hook timeout, which does not block the tool call. Making `install.sh` block rather than warn on a missing `timeout` is an install-policy decision for every stow consumer, declined in #994 (residual 20). `[verified: plugins/skill-management/hooks/_lib.sh:89-99 read directly; the stowed twin and `test_lib.py:6478-6499`'s pin relayed from the round-1 SDET and platform findings]` `[settled at round 1: both reviewers flagged the old G-5 as factually wrong — status 2 needs a cap firing or a killed/unexecutable capped call, not an absent binary.]`

**Rows**

1. Only `git commit` reaches this gate: `hooks.json:10` carries `"if": "Bash(git commit *)"` and the hook's own matcher at `:83` is a literal `git[[:space:]]+commit` regex. `git commit --no-edit` on a completed merge is a plain `git commit` and already matches. `[verified: hooks.json:10 and require-skill-review.sh:83 read directly; the scratch-repo reproduction — clean merge never gated, `--no-commit` + `git commit` denied — relayed from this session's claim-verification dispatch]`
2. **Three** sites derive from the HEAD-relative staged diff and must take the base: the trigger (`:115-117`), the structural validator's path list (`:139-142`, consumed at `:165`), and the marker hash (`:251`). A fourth, the whole-diff empty exit (`:123`), is deleted rather than threaded (row 18). Fixing only `:251` would leave the merge denied at the trigger and re-validating upstream's blobs. `[verified: require-skill-review.sh read in full]`
3. The trigger's pathspec union is exactly the marker hash's pathspec set — `SKILL_CONTENT_PATHSPECS` + `ROUTING_PATHSPEC` = `MARKER_PATHSPECS` (`:105-107`). So "no gated path in the base-relative diff" and "the base-relative marker diff is empty" are the same condition, and the empty case is unreachable past the trigger. This is what makes both the sentinel and the `:123` exit unnecessary. `[verified: :105-107, :115-116, :251]`
4. Write and read sides agree today by using identical argv: `marker.sh:21`'s `SKILL_REVIEW_PATHSPECS` is the same five pathspecs in the same order as `MARKER_PATHSPECS`, and both sides are HEAD-relative. Any base change must land on both in one release or every skill-review marker written mid-operation fails to match. `[verified: marker.sh:463-488, :797; require-skill-review.sh:105-107, :251]`
5. `git diff --cached <tree-oid>` — a *tree*, not a commit — is already a shipped production shape on the code-review path, so the plugin side needs no new git idiom. `[verified: _lib.sh:989-991 consuming an already-resolved base; marker.sh:434, :444]`
6. `_lib_gate_diff_base`'s transitive closure inside `_lib.sh` is exactly six functions: `_lib_gate_diff_base`, `_lib_git_inprogress_state`, `_lib_default_branch_or_guess`, `_lib_default_branch_from_origin_head`, `_lib_staged_diff_hash`, and `_lib_capped`. `_lib_capped_for` already exists in the plugin lib; nothing else outside bash builtins, `git`, `cat`, `sha256sum`, `awk` is referenced. `[verified: _lib.sh:742-1003 and 46-48 read directly; the no-name-collision sweep of the plugin lib relayed from the round-1 platform findings]`
7. `_hash_staged_diff`'s only production callers are the two `skill-review` arms (`marker.sh:473`, `:797`). `check` supports `code-review` only and routes through `_lib_code_review_marker_value`. `[verified: marker.sh:194-218, :463-488, :795-800; repo-wide grep relayed from the round-1 platform findings]`
8. Swapping `_hash_staged_diff` for `_lib_staged_diff_hash` at `marker.sh:797` silently changes `status`'s output unless a blanking line is added: `_hash_staged_diff` returns rc 3 with empty stdout on an empty diff (so `status` reports *absent*), while `_lib_staged_diff_hash` returns `sha256("")` — a real value a stale marker could match, reporting *live* on a clean tree. The code-review arm already carries that blanking at `:790`. `[verified: marker.sh:194-218, :784-800; _lib.sh:964-1003]`
9. `_lib_code_review_marker_value`'s sentinel is deliberately retained, and its stated reason is that `require-code-review.sh` *falls through to a marker comparison* on an empty base-relative diff. The skill-review path does not: it disarms. `[verified: docs/design-decisions/plan-review-gate-disarms-on-empty-active-plan-set.md § "Why `_lib_code_review_marker_value` keeps its sentinel"]`
10. The declined "pre-analyzed successor" in that same doc — re-arming a disarmed gate by recomputing the HEAD-relative set — is exactly the alternative to disarming here, and it is declined on the ground that it "re-arms the gate on exactly upstream's plan content, converting a guaranteed over-gate into a probabilistic one." That reasoning transfers verbatim to upstream's SKILL.md content. `[verified: same doc, "Pre-analyzed successor"]`
11. `plugin-semver`'s own table classifies "a trigger narrowed so it no longer fires where it used to" as **major**, and `CHANGELOG.md:56` applied the mirror-image row ("broadened to fire in strictly more cases") as minor at `skill-management` 3.1.0. This change narrows the trigger. `[verified: plugins/plugin-semver/skills/plugin-semver/SKILL.md:36-43; CHANGELOG.md:56; plugins/skill-management/.claude-plugin/plugin.json reads 3.6.1]`
12. CI validates every `SKILL.md`'s frontmatter and the corpus budget repo-wide, independent of the commit hook (`test_frontmatter_parses_strictly`, `corpus_budget_violations`) — **but that suite lives in claude-config only.** `plugins/skill-management` installs into arbitrary consumer repos through the marketplace, and those repos run none of it, so M-4's give-up is bounded in this repo and unbounded for plugin consumers. `[verified: claude-skills/skills/tests/test_skills.py:60-62, :2031, :2063-2078; the consumer-repo half relayed from the round-1 ciso findings]`
13. `claude/.claude/tests/helpers.py`'s existing merge fixtures reach **no trusted anchor**: `build_conflicted_merge:880` merges a local `theirs` branch and `build_conflicted_cherry_pick:908` picks an unpushed local commit, so `_lib_gate_diff_base` returns 1 before reaching `merge-tree`. Only `build_conflicted_revert` is HEAD-anchored. `bare_remote_with_default_branch:840` seeds one top-level file and `push_conflicting_edit_to_origin:872` pushes one file per call, neither creating parent directories, so a nested `claude-skills/skills/<x>/SKILL.md` needs a new builder (closest precedent: the test-local `_build_conflicted_merge_via_origin_with_upstream_plan_edit`, `test_marker_script.py:~1510`). `[verified: relayed from the round-1 SDET findings, which read each builder and ran `test_lib.py::TestGateDiffBaseTrustedAnchor` in a /tmp copy]`
14. `test_require_skill_review.py`'s parity suite is fixed-input and single-call (`:1200-1640`), and its `git_repo` fixture has no origin — so it exercises only the empty-base branch of every function this change copies. `[verified: the parity test names and file structure read directly; the no-origin property relayed from this session's research dispatch]`
15. Three sites restate this gate's trigger: `docs/hooks.md:33` ("blocks `git commit` only when staged changes include a `SKILL.md`"), `docs/hooks.md:197` ("when no audit covers the currently staged `SKILL.md` diff"), and `README.md:164`. `docs/hooks.md:116`'s "the staged diff for `/code-review` and `/skill-review`" is already stale for code-review (#994 Phase 5 never landed). `[verified: docs/hooks.md:33, :112-124, :197 read directly; README.md:164 relayed from the round-1 platform findings]`
16. `git diff --cached <tree> --name-only` lists an unmerged path, so a conflicted gated file keeps the gate armed until it is resolved and staged. `[verified: scratch-repo reproduction by this session's hook-review dispatch]` `[settled at round 1: both reviewers flagged this row as still-unverified; it is now executed, and Verification still pins it at unit layer so a git-version change surfaces as a test failure.]`
17. `_lib_capped_for` has three arms — `timeout`, then `gtimeout`, then **run the command uncapped**. Status 2 in `_lib_gate_diff_base` therefore arises only from a cap firing (124/137/143) or a capped call being killed or unexecutable (125/126/127), never from an absent cap binary. `[verified: plugins/skill-management/hooks/_lib.sh:89-99 read directly]`
18. The whole-diff exit at `:123` is unreachable on an honest result after the trigger (row 3), and its one reachable path is a no-op external diff driver: under `diff.external=/usr/bin/true`, patch output is empty while `--name-only` still lists the path, so the trigger arms and `:123` releases the commit with no marker consulted. `[verified: the HEAD-relative primitive was executed by the round-1 ciso reviewer on this tree; the `--cached` half of the driver dispatch is unexecuted and is pinned by the migrated write-arm test in Verification]`
19. Revert is the one in-progress state whose synthesized base is a **subtraction**: `merge-tree --write-tree --merge-base=X HEAD X^` is HEAD minus a reviewed patch, a tree whose removals were reviewed nowhere. Merge, cherry-pick and rebase all synthesize a union of two trees each of which reached a trusted anchor. `[verified: _lib.sh:913-929 read directly]`
20. Mid-revert the disarm would buy nothing anyway: a revert introduces no third-party content, so the HEAD-relative staged gated diff is exactly the committer's own removals — GH-1076's over-gating complaint does not arise there, while the cost (skipping the removal audit `/skill-review`'s behavioral-equivalence table exists for) is real. `[unverified — derived from row 19's command table, not executed]`
21. The engineer settled the two questions this design left open. `[engineer-verified: "ok yeah revise plan with Option F and exclude revert and round-1 fixes folded in."]` Read against the questions it answered, that quote settles: the full port (marker.sh write/status base-relative via `_lib_staged_diff_hash`, `_hash_staged_diff` deleted, hook + marker.sh in one commit), the revert exclusion, and folding the round-1 findings in. It settles nothing else in this plan.
22. Rebase is effectively inert for this gate — `REBASE_HEAD` is the pre-rebase commit being replayed, which in the ordinary case is an ancestor of neither `origin/<default>` nor mid-rebase HEAD, so the anchor check fails and today's behavior stands. Cherry-pick's premise is sound in the other direction: an anchored `CHERRY_PICK_HEAD` is by definition already merged upstream or already in this branch's history. `[verified: test_lib.py:5819-5828 and :5882-5890 relayed from the round-1 ciso and SDET findings]`
23. A HEAD-relative prefilter ahead of the base resolution is unsound, so the base must resolve on **every** `git commit` this hook sees: a keep-ours resolution of a conflicted `SKILL.md` leaves the index equal to HEAD, so the HEAD-relative gated set is empty while the base-relative one is not — the prefilter would disarm on exactly the commit that discards upstream's reviewed content. `[verified: reasoning from _lib.sh:913-917's merge case against the index semantics; the resolution shape was named by this session's hook-review dispatch]`
24. An empty `CURRENT_HASH` can never release the gate: `_lib_marker_value_present` returns 1 when the expected value is empty (`plugins/skill-management/hooks/_lib.sh:209`), so `_lib_staged_diff_hash`'s exit-1-empty-stdout failure posture reaches `emit_deny` through the existing terminal path with no new branch. `[verified: relayed from the round-1 ciso findings, which read that line]`
25. `require-plugin-version-bump.sh` compares against the branch's merge-base with the default branch and requires only a strict raise, so one bump covers both dispatches and the magnitude is `plugin-semver`'s call. The same check denies a plain revert of the plugin half, so rollback is a forward release (4.0.1 or later), never a revert commit. `[verified: `version_strictly_greater` behavior relayed from the round-1 platform findings]`
26. `declare -f` prints bash's own normalized parse of a function body with comments stripped, so definition-equality across the two libs is compatible with deliberately adapted comments and is blind to comment drift *inside* the copied bodies. `[unverified — bash's documented `declare -f` behavior, not executed this session; the equality test itself is what pins it]`
27. `marker.sh status`'s single `GATE_DIFF_BASE` (`:773`) can no longer serve skill-review once revert is excluded, and its comment at `:768-772` ("skill-review ... does not consume this base") is false either way after this change. `[verified: marker.sh:760-800 read directly]`
28. The in-chain bypass (`_lib_chains_marker_write_before_commit`, today `:246-248`) is a pure command-text match, unconditioned on `SKILL_DIFF`/`ROUTING_DIFF`/`BASE`, and this plan does not move or touch it. For the sanctioned `marker.sh write skill-review && git commit` chain, it grants `exit 0` before `gated_paths_at_base` or the hash call are ever reached — so a listing-only failure and a genuine empty diff are indistinguishable to it, and the structural validator (which runs only when `STAGED_SKILL_PATHS` is non-empty) also silently skips on a failed listing. The actual backstop for this one branch is `marker.sh write skill-review`'s own independent fail-closed hash, not the hook's terminal deny — row 24 covers the non-chained path only. `[verified: require-skill-review.sh:139-248 read in full; the identical shape on `require-code-review.sh:96-130,125-126` — pre-existing, out of this plan's scope — relayed from the round-2 ciso findings]`

**Mechanisms**

- **M-1 Duplicate the six-function closure (row 6) into `plugins/skill-management/hooks/_lib.sh`, byte-identical bodies, pinned by `declare -f` definition-equality (M-9).** The heavy choice: it makes a state machine, not a one-liner, into two maintained implementations across a boundary. Two lighter primitives were checked and both fail. *Source the stowed `_lib.sh` when it exists, fall back otherwise* fails because the preimage recipe would then depend on whether `./install.sh` ever ran on that machine, so the same tree yields two different marker values and a gate that can never be released on one of them (G-1). *Compute a merge-only base inline in the hook* (`git diff --cached MERGE_HEAD`, or path intersection) fails twice: it drops the trust-anchor check that keeps a stale, forged, octopus or exotic-topology ref from narrowing the authorization hash (G-3), and the write side already uses `_lib_gate_diff_base`, so any inline variant is a guaranteed write/read mismatch (row 4). `anchors: root, G-1`
- **M-2 Route both `marker.sh` skill-review arms onto `_lib_staged_diff_hash` and delete `_hash_staged_diff`, accepting a two-artifact release with a skew window.** `[settled at round 1: the platform reviewer's Foundation concern — a read-side-only release was weighed and declined.]` The engineer chose the full port (row 21); the design reason it is worth the skew is that the lighter read-side-only variant (base-aware trigger and validator, marker hash left HEAD-relative) leaves GH-1076's complaint standing in miniature for the subcase where the resolution itself edits a gated file — the review is still over-scoped to upstream's content there — and leaves the preimage recipe with three homes (`_hash_staged_diff`, `_lib_staged_diff_hash`, the plugin's copy) of which the plugin can parity-test only one, so write/read agreement would rest on a human comparing two different function bodies. Using the same function on both sides is what makes the parity test meaningful for the value that actually authorizes the commit. `anchors: row4, row7, row8, row21`
- **M-3 No empty-base sentinel anywhere on the skill-review path; the trigger exit disarms instead, and it fires only on a *successful* empty name-only listing.** Row 3 makes the degenerate case unreachable at the hash, and row 9 shows the code-review sentinel's stated justification does not transfer. The disarm's residual exposure — a forged anchor that makes upstream's gated content look identical to the base — is dominated by G-2's zero-gate clean merge, which needs no forgery, no engineered conflict and leaves no marker. The one alternative that would close it (row 10's declined successor: fall back to the HEAD-relative trigger set when the base-relative one is empty) reinstates GH-1076 exactly. `anchors: row3, row9, row10, G-2`
- **M-4 Scope the structural validator's input to the base-relative path list.** The lighter-looking alternative — keep the validator HEAD-relative, on the "never narrow a scanner" rule from #994 bucket 2 — was checked and set aside: the trigger exit sits *above* the validator, so a HEAD-relative validator would fire only in the already-armed case, and it would deny a merge for a defect in a blob the merger did not author while the conflict-free merge of the same two trees validates nothing at all (G-2). What is given up is the auto-merge-of-two-valid-files-into-an-invalid-one case mid-conflicted-merge, bounded by row 12's CI check **in this repo only**. `anchors: root, row12, G-2`
- **M-5 Copy `_lib_capped` into the plugin lib rather than rewriting the copied bodies' calls to `_lib_capped_for 5`.** The rewrite would scatter a bare `5` across roughly ten call sites inside functions whose whole value is being byte-comparable to the stowed originals, turning every future stowed-side edit into a manual re-translation and breaking M-9's equality check by construction. `anchors: row6`
- **M-6 The terminal deny builds its reason in a variable and appends up to two conditional notes.** When `BASE` is non-empty: name the two-artifact requirement (the stowed `marker.sh` and the installed plugin must both be current, with the `claude plugin install skill-management@claude-config --scope project` command) and point at `docs/hooks.md` § "Gate deadlock recovery" — without it, a skew-window deny is indistinguishable from the GH-1076 symptom the release claims to fix, and no in-session action satisfies it. When the base was undetermined or a gated-path listing failed: say the commit was gated against HEAD, so a mismatch mid-merge is expected rather than a review gap. The deny text is the only surface a blocked contributor is already reading. No log, counter or telemetry is added here; see M-10 for the disarm path. `anchors: G-5, G-4, row17`
- **M-7 Major version bump, `3.6.1` → `4.0.0`, landing in Dispatch A.** Row 11's table decides the magnitude; row 25 forces the dispatch. `anchors: row11, row25`
- **M-8 The revert exclusion lives in a seventh shared function, `_lib_skill_review_diff_base`, authored identically into both libs and wrapping — never modifying — the copied `_lib_gate_diff_base`.** It calls `_lib_gate_diff_base` first and re-probes the state only when a base was actually computed, so the hot path (no in-progress state) pays nothing extra and only an in-state commit pays one more `git rev-parse --absolute-git-dir`; a state re-probe returning 2 returns 2 rather than passing the base through, so an undetermined state cannot silently admit a revert. Two lighter primitives were checked and both fail. *Apply the exclusion at each call site* (the hook plus both `marker.sh` arms) puts one policy in three places, and M-9's equality mechanism compares functions, not top-level script fragments — so a write/read divergence in exactly the policy that decides the preimage would be undetectable, which is the failure class row 4 exists to prevent. *Put the exclusion inside `_lib_gate_diff_base`* breaks M-1's byte-identity and silently changes the code-review and plan-review gates, neither of which this change is scoped to touch. `anchors: row19, row20, row21, row4`
- **M-9 `declare -f` definition-equality over the seven shared functions is the primary drift guard; a thin real-fixture residual covers only what equality cannot reach.** `[settled at round 1: the SDET's Foundation concern — the heavy fixture-driven differential suite is dropped as the primary mechanism.]` The heavy primitive was the first draft's choice and is the over-powered one: a suite of 3 functions × ~4 states × 2 libs costs several bare-remote builds and subprocess spawns per case and still proves nothing about any input it did not construct, while identical bodies prove identity for *every* input in milliseconds. Equality cannot reach two seams — each lib's own `_lib_capped_for` dependency, and the plugin lib actually sourcing cleanly with the functions reachable in a real hook run — so the residual keeps exactly four differential cases: one via-origin anchored merge, one HEAD-anchored revert driven through *both* `_lib_gate_diff_base` (returns a tree) and `_lib_skill_review_diff_base` (blanks it), one unanchored local-branch merge (both copies exit 1 with empty stdout), and one status-2 case via an exit-status git shim. Each asserts the shared verdict against an independently computed expectation, never agreement alone. The differential half of the round-1 anchor-rejection request is deliberately not kept: identical bodies plus a parity-tested `_lib_capped_for` make a differential rejection case tautological, so anchor rejection is pinned at hook-verdict level instead, where it is not. `anchors: row6, row14, row26`
- **M-10 One non-blocking stderr line at the disarm exit, only when `BASE` is non-empty.** `[settled at round 1: both reviewers left the observability call open; a trace is kept.]` Without it there is no way to answer, after any later bypass question, whether a `SKILL.md` landed because the gate disarmed against a synthesized base or because no gate fired at all (G-2). Two alternatives were weighed. A compliance-log file mirroring `require-code-review.sh` is heavier: it needs a config-dir resolution at the disarm exit whose failure must not change the allow, introducing a second, fail-open config-dir posture into a file whose existing one is deliberately fail-closed. Keeping nothing is lighter but leaves the audit question unanswerable. stderr-on-allow is already this file's own shape (the corpus-budget warning at `:229-235`), costs no resolution and no write, and the condition (`BASE` non-empty) keeps it off the ordinary "no SKILL.md staged" exit. `anchors: G-2, root`
- **M-11 The base-conditional argv assembly lives in a hook-local function taking repo root, base and pathspecs, returning git's own exit status.** `[settled at round 1: the platform reviewer's `local`-at-top-level finding.]` The first draft's `local -a args=(...)` sat at the hook's top level, which bash rejects with "local: can only be used in a function" (the assignment still lands) and ShellCheck flags SC2168 at error severity, failing this plan's own third Verification command. A non-zero status from either listing never disarms; control falls through to the armed path. For a **non-chained** `git commit`, the hash call is a different git invocation over a different pathspec union, so it is not guaranteed to fail from the same cause — but its own exit-1 path reaches the existing terminal deny regardless (row 24), which is what actually backstops this branch, not "the hash call fails the same way" as the first draft of this row claimed. `[settled at round 2: three specialists (ciso, sdet, platform) independently confirmed this backstop-naming gap; platform additionally traced a concrete consequence for the chained form — see row 28.]` No HEAD-relative retry is added: a retry would be a second defensive layer closing a gap the first did not open, and it would reintroduce a second, HEAD-relative recipe that can disagree with the hash. `anchors: row2, row24, row28`

## Critical files

Two sequenced `code-writer` dispatches, both in this worktree, neither with `isolation: "worktree"`. They are sequenced, not parallel: dispatch B's hook and `marker.sh` edits call functions dispatch A creates, and both dispatches touch `test_require_skill_review.py` and `test_marker_script.py` — an overlap that is safe only because they run one after the other. Dispatch A is independently committable and green on its own (it adds unused functions, fixtures and their tests, and changes no runtime behavior beyond the version number). Dispatch B's plugin-read and stow-write halves must land in **one** commit, or a marker written under one recipe can never match the gate reading it under the other (row 4).

**Dispatch A — the shared closure, the revert wrapper, and the fixture seams.** Verification: the three commands below.

- `plugins/skill-management/hooks/_lib.sh` — copy `_lib_capped`, `_lib_default_branch_from_origin_head`, `_lib_default_branch_or_guess`, `_lib_git_inprogress_state`, `_lib_gate_diff_base`, `_lib_staged_diff_hash` from `claude/.claude/hooks/_lib.sh:46-48, 742-1003` (row 6), keeping each **body** byte-identical so M-9's equality check has a meaningful subject. **Reuse:** `_lib_capped_for` is already there (`:89-99`) — do not re-add it. Rewrite the header: its "No git helpers" claim and its function inventory are both about to be false, and the must-stay-identical contract note has to name the new set; if a test pins that inventory, update it here. Four copied comment blocks are **adapted**, not copied, because they cite functions that do not exist in the plugin lib: `_lib_git_inprogress_state`'s tri-state note (cites `_lib_command_invokes_git_subcmd`), `_lib_default_branch_or_guess`'s failure-shape note (cites `_lib_repo_root` and `_lib_command_invokes_git_subcmd`), `_lib_staged_diff_hash`'s fail-closed note (cites `_lib_active_plan_hash`, `_lib_reviewer_round_state_value`, and `marker.sh`'s `_hash_staged_diff`), and `_lib_gate_diff_base`'s doc pointer (a repo-relative `docs/design-decisions/...` path that resolves in no consumer repo — name the claude-config repository). Each adaptation states the same fact standalone; M-9 is blind to comments (row 26), so this is legal by construction.
- `claude/.claude/hooks/_lib.sh` — add `_lib_skill_review_diff_base REPO_ROOT` (M-8), and repoint `_lib_staged_diff_hash`'s docstring at `:980-983` so the external-diff residual is stated standalone rather than cross-referencing `marker.sh`'s `_hash_staged_diff`, which Dispatch B deletes. The repoint lands here, with the copy, so neither tree ever carries a dangling reference. The new function's docstring is two one-line facts, not a paragraph: what it returns, and that a revert's synthesized tree is HEAD minus a reviewed patch so its removals were reviewed nowhere. No other stowed behavior changes in this dispatch.
- `plugins/skill-management/hooks/_lib.sh` — the same `_lib_skill_review_diff_base`, written identically (M-8).
- `plugins/skill-management/.claude-plugin/plugin.json` — `3.6.1` → `4.0.0` (M-7, row 25). One bump for the branch; dispatch B does not re-bump. Leave `.claude-plugin/marketplace.json` version-less.
- `claude/.claude/tests/helpers.py` — four changes. (1) `staged_diff_hash_at_base(repo, base, *pathspecs)` takes optional pathspecs and omits the base argument entirely when `base` is empty, so one oracle expresses both the mid-merge and the HEAD-relative recipes. **This replaces the first draft's claim that the invalidation tests need `staged_diff_hash():758`** — that function is unscoped, so building an "old" skill-review marker from it produces a hash that differs from the real old recipe whenever any non-gated file is staged, which a conflicted merge always has; the old preimage is `staged_diff_hash_at_base(repo, "", *pathspecs)`. **Reuse:** leave `staged_diff_hash():758` itself untouched — other tests depend on it. (2) A new via-origin conflicted-merge builder that seeds a nested `claude-skills/skills/<name>/SKILL.md` into the bare remote and pushes a multi-file upstream commit, with the conflict engineered in an unrelated file (row 13; precedent `test_marker_script.py:~1510`), including the `mkdir -p` of parent directories that `bare_remote_with_default_branch` and `push_conflicting_edit_to_origin` do not do today. (3) Generalize the `.git/<STATE>_HEAD` assumptions at `:904`, `:932`, `:957`, `:1001` to resolve the gitdir through `git rev-parse --absolute-git-dir`, so the linked-worktree fixtures work (where `.git` is a file). (4) Promote `_make_git_exiting_with_status` out of `test_lib.py:6376`, since two test files now need it.
- `claude/.claude/hooks/tests/test_lib.py` — consume the promoted shim rather than keeping a private copy.
- `claude/.claude/hooks/tests/test_marker_script.py` (`:1386`, `:1399`) and `claude/.claude/hooks/tests/test_require_plan_review.py` (`:2962`) — the same gitdir generalization, mechanical, nothing else.
- `claude/.claude/hooks/tests/test_require_skill_review.py` — the parity suite: one fixed-input test per newly copied pure function in the established `test_plugin_lib_sh_*_matches_stowed_lib_sh` shape (`:1200-1640`), plus M-9's `declare -f` definition-equality over all seven shared names, plus M-9's four-case residual. Source each lib in its own `bash -c` and dump both with the same interpreter — sourcing both into one shell makes the second definition clobber the first and turns the comparison into a function against itself.

**Dispatch B — the gate, the write side, the deletion sweep, and the docs.** Verification: the three commands below, plus `plugin-semver` (hook-enforced; files under a plugin directory change).

- `plugins/skill-management/hooks/require-skill-review.sh` — resolve the base **once** after `REPO_ROOT` (`:96`) via `_lib_skill_review_diff_base`, keeping its status, and thread it through the three consumers of row 2. Add a hook-local `gated_paths_at_base REPO_ROOT BASE PATHSPEC...` (M-11) that builds `diff --cached --name-only`, appends the base only when non-empty, appends `-- "$@"`, and returns git's status; call it twice with the real `SKILL_CONTENT_PATHSPECS` and `ROUTING_PATHSPEC` arrays. The disarm exit fires only when both calls exited 0 and both outputs are empty. Delete the `:123` whole-diff exit outright (rows 3, 18) — do not thread a base through it. Replace the inline hash pipeline at `:251` with `_lib_staged_diff_hash "$REPO_ROOT" "$BASE" "${MARKER_PATHSPECS[@]}"`: identical argv to the write side by construction, and its exit-1-empty-stdout posture reaches the existing terminal deny with no new branch (row 24). Make `emit_deny` build its reason in a variable and append M-6's two conditional notes. Add M-10's one stderr line at the disarm exit, guarded on a non-empty base. The header gains four durable one-line facts: the novel-content base and its design-decision file; why the base resolves *before* the trigger with no HEAD-relative prefilter (row 23); and the three known gaps this gate does not close (G-2's ungated clean merge, G-5's uncapped run with no cap binary, M-4's auto-merge validator give-up). Re-derive the `~24s` worst-case latency comment at `:172-175` for the added base resolution: the capped worst case gains ~7s, not scaled by spawn count, since a stalled `git` returns status 2 at the *first* capped call regardless of how many more the base resolution would otherwise make. If the comment's framing also covers steady-state process-fork cost, mid-merge adds ~7 new git spawns (`_lib_gate_diff_base`'s 6 plus M-8's one state re-probe) against ~4 pre-existing, for ~11 total. Note that `merge-tree --write-tree` writes unreachable objects into the consumer's repo — this hook was read-only before.
- `claude/.claude/scripts/marker.sh` — `write skill-review` (`:463-488`): resolve `SKILL_REVIEW_BASE` via `_lib_skill_review_diff_base` and call `_lib_staged_diff_hash "$REPO_ROOT" "$SKILL_REVIEW_BASE" "${SKILL_REVIEW_PATHSPECS[@]}"`, keeping `[ -z "$MARKER_VALUE" ] → exit 2` **ahead of** the empty-digest `→ exit 0` branch, matching the code-review arm's order at `:445-458` — reversed, a failed hash would read as "nothing to review". `status` (`:795-800`): resolve its own `_lib_skill_review_diff_base` rather than reusing `GATE_DIFF_BASE` (row 27 — the two are no longer the same answer; the extra resolution is accepted on a human-invoked report path), rewrite the now-false comment at `:768-772`, and add the empty-digest blanking line mirroring `:790` — **unconditionally**, with no `[ -n "$GATE_DIFF_BASE" ]` branch, because skill-review has no sentinel to reconstruct (row 8, M-3). Delete `_hash_staged_diff`, `_HASH_STAGED_DIFF_RC_EMPTY` and the `MARKER_TEST_FIXTURE: hash-staged-diff` delimiters (`:159-219`); keep the empty-digest constant (still used at `:455`, `:788`, `:888`), rename it away from its deleted namesake, and preserve the comment explaining why it is computed rather than written as a hex literal. Sweep the two stale comments at `:299` and `:449`. Leave `_guard_staged_vs_unstaged:147` HEAD-relative and uncalled-with-a-base — the code-review arm already invokes it before resolving the base (`:428` vs `:434`).
- `claude/.claude/hooks/tests/test_require_skill_review.py` — the gate's verdict tests (see Verification). This file references none of `merge`, `MERGE_HEAD` or `origin` today. **Reuse:** dispatch A's helpers rather than hand-rolled `git init` fixtures, and `test_require_plan_review.py:3040-3044`'s anchor-precondition guard shape.
- `claude/.claude/hooks/tests/test_marker_script.py` — delete **by symbol, not by line range**: `_extract_hash_staged_diff_block`, `_run_hash_staged_diff`, and `TestHashStagedDiff` (`:868-972`). The removal must end at `:973` — `TestMarkerScriptStagedDiffStateClassification` starts at `:974` and stays, including the arity-stub tests at `~:1028` and `~:1103`, which are skill-review's only call-site coverage for "write aborts exit 2 with no marker when hashing fails" and "status degrades to absent". Add a mid-merge arm to each (the base-bearing 11-argument diff call). Migrate the `GIT_EXTERNAL_DIFF` residual pin (`:950-971`) to a write-arm test (see Verification). Sweep the stale docstrings at `:994`, `:1064`, `:3783`, `:3892`, `:3899`. Add the skill-review counterpart to `TestMarkerScriptMergeAwareBase`, which today has code-review write/status oracle tests and no skill-review arm.
- `claude/.claude/scripts/tests/test_pr_diff_against_base.py` — `:1019-1145`'s oracle extracts `_hash_staged_diff` by its fixture delimiters; repoint it at `_lib_staged_diff_hash` sourced directly from `_lib.sh` (no extraction needed — that file is sourceable), and sweep its stale references at `:1024-1064` and `:1126-1129`. Its artifact-vs-hash byte-equality claim holds outside an in-progress state only; calling it as `_lib_staged_diff_hash "$repo" ""` keeps that claim true and does not widen it.
- `docs/design-decisions/<slug>.md` — new file, in `docs/design-decisions/plan-review-gate-disarms-on-empty-active-plan-set.md`'s shape. It records: why the skill-review gate disarms on an empty base-relative gated set and why no sentinel exists here (rows 3/9/10); **why revert is excluded while merge, cherry-pick and rebase are not** — stated as the general rule (the disarm fires only where the synthesized tree is a *union* of trees that reached a trusted anchor; revert's is a *subtraction*, rows 19/20) plus the state-addressed-versus-delta-addressed reason the plan-review gate's opposite choice does not transfer, so a sibling-symmetry sweep meets the reasoning instead of the conclusion; that rebase is inert in the ordinary case (row 22); M-4's residual and that its CI bound exists in claude-config only (row 12); M-10's trace and why it is stderr rather than a compliance-log file; the `merge-tree --write-tree` object-DB writes; the pre-existing plugin matcher gap (`git -C … commit`, `git -c … commit`, `env git commit` and absolute-path `git commit` reach neither `hooks.json`'s `if` nor the hook's own regex, so this gate's disarm sits on a gate a plain `git -C` commit skips entirely); and the reopening criterion (G-2 being closed). Use `<config-dir>/skill-review-markers/`, never a literal home-rooted path — `test_doc_has_no_state_path` is parametrized over `docs/**/*.md`.
- `docs/hooks.md` — `:33`'s `require-skill-review.sh` bullet becomes the **one authoritative statement** of this gate's trigger and gains the novel-content-base sentence; `:197` drops its restatement and cites that bullet instead (row 15). **Incidental edit, flag it in the PR description:** `:116`'s "the staged diff for `/code-review` and `/skill-review`" is already stale for code-review; one clause fixes both.
- `README.md` — `:164` drops its own restatement of the trigger condition and points at `docs/hooks.md`'s bullet (row 15). In scope because leaving it is a statement this change makes false, not a cosmetic improvement.
- `CHANGELOG.md` `[Unreleased]` — the `4.0.0` entry, modeled on `:56`. It must carry: the trigger narrowing and the major bump; the skew in **both** directions (stow pulled first → the stowed `marker.sh` writes a base-relative preimage while the installed hook still reads HEAD-relative; plugin refreshed first → the reverse, which wedges the resolution-edits-a-gated-file case with no in-session action that satisfies it until `git pull`), with the note that outside any in-progress state both recipes are byte-identical so nothing changes there; that rollback is forward-only, 4.0.1 or later, because `require-plugin-version-bump.sh` denies a plain revert (row 25); the write-side cap change (`write skill-review`'s hash moves from uncapped to the shared 5s cap plus 2s grace, so a slow diff now aborts the write with exit 2 and no marker instead of hanging); and the `claude plugin install skill-management@claude-config --scope project` refresh command, which `plugin-semver`'s checklist also requires in the PR description.

## Verification

From this linked worktree (the contributor `.venv` lives at the main checkout root):

```bash
../../../.venv/bin/python3 claude/.claude/scripts/select-tests.py
../../../.venv/bin/ruff check claude/.claude/ claude-skills/ plugins/
scripts/list-shell-files.sh | xargs -0 ../../../.venv/bin/shellcheck
```

Run all three per dispatch. `plugins/` is added to the `ruff` target because CI includes it; it is a no-op here (no plugin `.py` file changes) and keeps the local and CI invocations identical.

**On `select-tests.py`'s scope**, correcting the first draft: it *does* map `plugins/skill-management/hooks/**` to `claude/.claude/hooks/tests`. Dispatch A will nonetheless select the **full suite**, because `claude/.claude/tests/helpers.py` is a `GLOBAL_TRIGGER_PATHS` member and `plugins/skill-management/.claude-plugin/plugin.json` maps to nothing. That is `CLAUDE.md`'s first carve-out (`select-tests.py` widened on its own), not a bug in its rule table — **do not edit `select-tests.py`**, which is itself a global trigger. Dispatch B, without those two files, selects the skills, hooks and scripts tests plus `claude/.claude/tests`. If a full-suite run stalls, rely on CI rather than re-running it.

**No new test depends on a cap firing.** Every status-2 case uses the promoted exit-status git shim, which returns immediately and behaves identically with or without a cap binary — so no new test needs `@pytest.mark.timing` or a `_timeout_binary_present` skipif, and no fifth copy of the sleep-based blocking `merge-tree` stub is created. If an implementer nonetheless reaches for a sleep-based shim, that test carries both markers.

**Ordering inside Dispatch B (fail-first evidence):** write the verdict tests and run them against the still-unmodified hook *before* editing it, and record in the commit message which allow assertions failed red. `run_hook` maps any silent non-2 exit — including exit 1 from a `set -u` unbound variable, which this hook runs under — to "allow", so a crash is otherwise indistinguishable from a correct disarm.

**The armed fixture**, named once and reused: upstream brings gated file Y untouched, *and* the resolution edits a different gated file X, so the HEAD-relative and base-relative preimages provably differ. Every test below that asserts a marker behavior runs on it, and asserts `oracle(base="") != oracle(base)` as a precondition. The oracle's base comes from an independent literal-OID `git merge-tree --write-tree` call in the test (the `_merge_tree_base` shape at `test_marker_script.py:1392-1404`), never from `_lib_gate_diff_base` — otherwise the "never seed a marker from the production function" rule is broken through the base input.

**Assertion strength, applied to every case below.** Each allow asserts returncode 0, empty stdout, and stderr free of shell-error text (`line NN:`, `command not found`, `unbound variable`); on a disarm with a non-empty base, stderr is exactly M-10's one trace line, and with an empty base stderr is empty. Each deny asserts the intended gate's reason substring — "Commit blocked by skill-review gate" versus the structural validator's — since a bare `== "deny"` passes for a deny from any phase.

Exit criteria, each a passing assertion rather than a design claim.

**Parity and drift (Dispatch A)**

- **Definition-equality** for all seven shared names (`_lib_capped`, `_lib_default_branch_from_origin_head`, `_lib_default_branch_or_guess`, `_lib_git_inprogress_state`, `_lib_gate_diff_base`, `_lib_staged_diff_hash`, `_lib_skill_review_diff_base`): each lib sourced in its own `bash -c`, both dumped by the same interpreter, `declare -f <name>` output compared. Standing, not introduction-time.
- **Fixed-input parity** per newly copied pure function, in the existing `:1200-1640` shape.
- **The four-case residual** (M-9), each asserting both copies' exit status *and* stdout against an independently computed expectation, never agreement alone: a via-origin anchored merge (non-empty tree OID equal to an independent literal-OID `merge-tree` result); a HEAD-anchored revert driven through both `_lib_gate_diff_base` (returns a tree) and `_lib_skill_review_diff_base` (returns 1, empty stdout) — this pair is what pins the exclusion as living in the wrapper rather than in the copied closure; an unanchored local-branch merge (both exit 1, empty stdout); and one exit-status-shim case (both exit 2, empty stdout).

**The gate's verdict (Dispatch B)**

- **The reported bug, end to end.** On the via-origin fixture where upstream edits a gated `SKILL.md` and the local branch does not, with the conflict engineered in an *unrelated* file so `MERGE_HEAD` persists: resolve, `git add`, and `require-skill-review.sh` allows with **no marker on disk**. Three preconditions asserted through primitives so it cannot pass vacuously: the fixture reaches a trusted anchor through the production `_lib_gate_diff_base`; the **HEAD-relative** gated name-only set is non-empty (the old gate would have fired); the **base-relative** one is empty.
- **Still armed where it must be**, same fixture: a resolution that edits any gated path denies. One arm per pathspec family — `claude-skills/skills/**/SKILL.md` and `claude-skills/skills/plan-review/ROUTING.md` — since `ROUTING_DIFF` and `SKILL_DIFF` are separate variables feeding different downstream consumers.
- **Anchor rejection at verdict level**, the deny counter to the allow above and the only place M-1's central claim is tested: an unpushed local-branch `MERGE_HEAD` denies; a fabricated `origin/<name>` ref pointing at an unreachable commit denies; a non-hex state-ref value denies; an octopus `MERGE_HEAD` denies. Each with upstream-gated content in the index — proving the allow depends on the anchor, not merely on "the gate is off mid-merge".
- **Forged-anchor residual, pinned both ways**, in `test_require_plan_review.py::test_hand_forged_anchor_no_real_merge_allows_unrelated_write`'s style: a hand-forged anchor with no gated content allows (the accepted residual), and the same forged anchor with a gated edit still denies. A later tightening or loosening then shows up as a visible test change.
- **Revert stays armed** (M-8): mid-conflicted-revert with a gated removal in the index, the gate denies with the marker-gate reason, and the same fixture's `_lib_gate_diff_base` is asserted to return a tree — so the deny is the exclusion firing, not an anchor failure.
- **Revert round trip on `marker.sh`, not only the hook** (M-8): on `build_conflicted_revert`'s fixture, `marker.sh write skill-review` stores a value equal to the independent **HEAD-relative** oracle digest, not the merge-tree-subtraction one, and `marker.sh status` reports that marker **live**. This is the direct write/status-side counterpart to the hook-verdict test above — without it, a call site that mistakenly invoked `_lib_gate_diff_base` directly instead of the `_lib_skill_review_diff_base` wrapper would go undetected, since M-9's differential residual tests the two functions directly and not through either `marker.sh` arm.
- **Row 16, pinned twice:** at unit layer, `git diff --cached --name-only <tree> -- <gated pathspec>` lists an unmerged path (modelled on `test_lib.py:5705`); and at verdict layer, mid-conflicted-merge with a gated file itself conflicted and unstaged, the gate denies — with the trusted-anchor precondition asserted, or the deny holds vacuously through the HEAD-relative fallback.
- **Validator scoping (M-4), both directions, non-vacuously.** Armed fixture: gated file X is novel to the resolution and well-formed, gated file Y arrives untouched from upstream and is malformed. Direction 1 — with an oracle-seeded matching marker the commit allows, and without one the deny names the skill-review gate and *not* the structural validator. Direction 2 — the resolution also malforms X; the deny names X's repo-relative path and **does not name Y**. Y's absence is the only thing that distinguishes base-relative from HEAD-relative validator input; the first draft's "malformed file from upstream does not deny" fixture exited at the trigger and could not fail. Use the well-formed and broken YAML shapes at `test_require_skill_review.py:823-890`.
- **A cherry-pick allow/deny pair**, since the hook's code past the base is state-agnostic and one non-merge state suffices (`test_require_plan_review.py:3064-3123`'s rationale).
- **Status 2 is visible, and it changes no verdict:** under the exit-status shim, mid-merge with no matching marker the hook denies *and* its text names the undetermined base; with a HEAD-relative marker under the same shim it allows.
- **No cap binary on PATH, mid-merge, allows.** Using the existing curated-PATH farm (`test_require_skill_review.py:1025-1037`, `:1888-1899`) minus `timeout`/`gtimeout`: the base resolves uncapped and the disarm fires (G-5, row 17). The farm needs no widening — the new path adds only `cat` to binaries it already carries.
- **Hash failure denies:** a broken `sha256sum` on PATH mid-merge, the gate denies with the marker-gate reason (row 24).
- **A listing-only failure, both command forms, distinct from status 2 and from a hash failure** (row 28): a git shim that fails only `gated_paths_at_base`'s call (not `_lib_skill_review_diff_base`'s own resolution, and not the later hash call). For a **non-chained** `git commit`, the gate denies with the marker-gate reason — pinning M-11's backstop claim for the case it actually covers. For the **chained** form (`marker.sh write skill-review && git commit`), the state this design intentionally accepts: the in-chain bypass grants `exit 0` unconditioned on the listing's success, so the actual gate on that commit is `marker.sh write skill-review`'s own independent hash computation, asserted here to still fail closed under the same shim (mirroring the write-side call-site contract below) rather than asserting a specific hook exit code for that branch.

**Write / read agreement and the removal sweep (Dispatch B)**

- **Marker invalidation, three assertions, all on the armed fixture:** a marker holding the old HEAD-relative preimage — built as `staged_diff_hash_at_base(repo, "", *pathspecs)`, *not* the unscoped `staged_diff_hash` — denies; a marker holding the new base-relative preimage allows; and **outside any in-progress state a marker holding the old preimage still allows**, which is what proves no marker on disk invalidates when this ships.
- **Write / status / read three-way agreement** on the same tree, mid-merge and outside any state: `marker.sh write skill-review` and `marker.sh status` produce the value the independent oracle computes, and the hook allows with an oracle-seeded marker. The hook emits no hash, so its half is asserted as a verdict, not a value.
- **The two `_hash_staged_diff`-removal guards:** mid-merge with an empty base-relative gated diff, `write skill-review` exits 0 without writing a marker; and `status` reports skill-review **absent**, not live, with a stale marker present (row 8).
- **Write-side call-site contracts, with mid-merge arms** on the surviving unknown-state tests: with a git shim that fails the base-bearing diff call, `write skill-review` exits 2, writes no marker, and **does not truncate an existing one**; `status` reads absent.
- **The migrated external-diff pin:** with `diff.external` set to a no-op, a genuinely-staged gated change makes `write skill-review` exit 0 and write no marker, carrying the same "revisit if this flips" message the deleted `test_git_external_diff_noop_misclassifies_staged_content_as_empty_rc` carried. If the test shows `git diff --cached` does not honor the driver, delete the pin and record that the residual does not reach this call site — either outcome resolves row 18's unexecuted half.
- **Plain clone and linked worktree, both:** the reported-bug fixture and the marker round trip run parametrized over both, since this repo enforces worktrees and contributors hit GH-1076 inside a linked one.

`/skill-review` is not required (no `SKILL.md` is edited). `plugin-semver` is, and is hook-enforced. `/code-review` will route the hook-file change to `claude-hook-review`; the argv function, the deleted `:123` exit and the trace line are its surface.

## Out of scope

Each is accepted with a reason, not overlooked.

1. **`git merge --continue`, `git rebase --continue`, `git cherry-pick --continue` and `git revert --continue` stay ungated by this hook.** `_lib_command_concludes_marker_gated_commit` exists in the stow-tree `_lib.sh` with zero production callers — #994 Phase 3 never landed, so `require-code-review.sh:66` still matches `git commit` only. Wiring the plugin arm alone would make the skill-review gate strictly stricter than the code-review gate on the same command, and would require duplicating the whole fragment-matcher dependency chain into the plugin lib. This is an under-gating defect (#994 defect 2), the inverse of GH-1076, and belongs to that plan's coordinated sweep.
2. **The plugin's `_lib_chains_marker_write_before_commit:271-286` keeps its `git commit`-only regex.** It fails closed, so it is friction rather than a bypass, and it only starts mattering once item 1 lands.
3. **`"if": "Bash(git commit *)"` stays in `hooks.json:10`, and the hook's own matcher keeps its adjacency regex.** Dropping the `if` is #994 Phase 4's item, coupled to the matcher change in item 1. The matcher gap it leaves — `git -C … commit`, `git -c … commit`, `env git commit` and absolute-path `git commit` match neither the `if` nor the hook's regex, where the stowed code-review gate uses the fragment-aware `_lib_command_invokes_git_subcmd` — is pre-existing and unchanged in either direction here, and gets a sentence in the design-decision doc rather than a fix in this change.
4. **`_guard_staged_vs_unstaged` stays HEAD-relative.** Consequence, stated rather than hidden: mid-merge it cannot notice a forgotten `git add` on a gated file, because upstream's incoming content keeps the HEAD-relative staged diff non-empty. It matches the shipped code-review arm (`marker.sh:428` runs before the base resolves at `:434`), and the failure mode is a later marker mismatch and a re-review, never a false release.
5. **A conflict-free merge, cherry-pick or revert still reaches a commit with no gate firing at all** (G-2). Unchanged in either direction by this plan, and load-bearing: it is the dominating bypass that makes M-3's disarm and G-3's anchors admissible.
6. **With neither `timeout` nor `gtimeout` on PATH the copied closure runs uncapped, so a stalled `git` inside this gate exits only by the harness's own hook timeout, which does not block the tool call** (G-5, row 17). That is the whole hook family's existing posture on stock macOS without coreutils, not something this change introduces; `install.sh`'s warn-rather-than-block choice is #994 residual 20. Where a cap *does* fire, or a capped call is killed or unexecutable, the base is undetermined and the merge is over-gated exactly as today, with M-6's deny note as the only mitigation.
7. **An auto-merge that combines two gated files into a tree no reviewed state ever contained is no longer caught at commit time** during a conflicted merge. Two shapes: the structural one M-4 gives up (two valid files merging into an invalid one), and behavioral drift between two independently-reviewed edits to the same gated file. Both were never caught at all during a conflict-free merge of the same two trees (G-2), which dominates them; row 12's CI check catches the structural shape in this repo before a branch can land, and **plugin consumers get no compensating check for either** (row 12).
8. **Conflict markers in the computed reference blob over-scope the novel diff** for a hand-resolved gated file — the review then spans marker-text→resolution rather than the resolution alone. Over-scoped in the safe direction, and only inside files the reviewer must read anyway (#994 residual 5).
9. **Octopus merges, `cherry-pick -m 2`, `rebase --rebase-merges`, and git older than 2.38 fall back to the empty base** and get today's behavior (#994 residual 4, residual 6). Inherited from `_lib_gate_diff_base`, not introduced here. The version floors come with it: `merge-tree --write-tree` needs git 2.38, and the cherry-pick/rebase/revert arms' `--merge-base=` needs 2.40; the routine trigger, a merge, needs only 2.38, and CI runs `ubuntu-24.04`.
10. **The content-addressed marker scheme itself is not revisited** (`docs/hooks.md` § "Marker keying and gate-release authority"). Decoupling authorization from the preimage would make any future base change a non-event and is still declined: it is a rewrite of the mechanism all five marker kinds depend on.
11. **The `_lib_jq` fold onto `_lib_capped_for 5` is dropped from this change.** `[settled at round 1: both reviewers flagged it; it is not merely deferred but declined for this release.]` The plugin's `_lib_jq` today probes `timeout` only and passes no `-k`; folding it would add `-k 2`, which a BusyBox `timeout` older than 1.35 rejects outright — turning a failure confined to commits that stage a `SKILL.md` into one that fails closed on *every* Bash call reaching this hook, and falsifying `CHANGELOG.md:37`'s standing claim that the plugin's exposure is narrower. That is a widened blast radius bundled into an ergonomics cleanup, and it would need its own plugin-side `-k`-rejection test to ship safely. Dropping it also makes Dispatch A purely additive. The two-timeout-idioms inconsistency it would have fixed gets one line in the plugin lib header instead.
12. **A hash-computation failure denies with the ordinary marker-gate text**, indistinguishable from a real mismatch. Pre-existing — today's inline pipeline has the same property and `test_marker_gate_blocks_without_sha256sum[skill_review]` pins it — and a third conditional deny note would add a branch for a case a contributor cannot act on differently anyway.
13. **`test_default_branch_resolution_is_shared.py` is not widened to the plugin copy.** It scans `claude/.claude/` only, so the plugin's second `refs/remotes/origin/HEAD` read escapes the "exactly one home" invariant it states. Widening it would require teaching it a copy-with-parity-test exemption; M-9's definition-equality *is* the plugin-side enforcement of that invariant, and that is the better home for it.
14. **`require-code-review.sh:112-117`'s structurally identical whole-diff exit is left alone**, even though this change deletes the skill-review twin (row 18). Its own comment records that the branch is load-bearing for `deny-invisible-commit-content.sh`, so the sibling audit ends in "different reason, leave it", not in "same bug, fix both" — flagged here so the asymmetry reads as deliberate.
15. **Write-side and read-side base resolutions are independent and each hits its own cap**, so a marker can fail to match on read despite unchanged staged content mid-operation. `marker.sh:435-440` already documents this for the code-review arm; the skill-review arm inherits the identical gap. It is an availability gap producing a false re-review, never a false accept.

---

**One open item for the engineer, if they want it decided rather than defaulted.** M-10's stderr trace fires on every mid-merge commit whose gated content is entirely non-novel — which, under `git-feature-branch-sync`'s merge-over-rebase rule, is the routine case this change exists to fix, not a rare one. I recommend keeping it (it is the only record distinguishing "disarmed against a synthesized base" from "no gate fired"), and the design works either way; if the engineer would rather that common path stay silent, drop M-10 and record in the design-decision doc that no trace is kept and why.
