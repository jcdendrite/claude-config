# Cache ready-for-review's Step 2 (Verification) pass

## Context

Add a completion-marker cache to `ready-for-review` Step 2 (Verification)
so a resumed session doesn't re-run the project's full check/test suite
from scratch when nothing has changed since the last clean pass. Step 3
(code review) already gained exactly this treatment in the just-merged
`cumulative-review` marker (PR #837): a content-addressed marker lets a
session skip re-invoking `/code-review` when the cumulative diff is
byte-identical to one that already passed. Step 2 has no equivalent —
every resumed session re-executes the project's check/test commands
inline, even when HEAD (and the tree it points to) is unchanged from a
prior clean pass, including across a mid-gate handoff where the resuming
session has no memory that a prior session already ran it clean. The plan
for PR #837 named this residual explicitly (row 8): "step 2's verification
runs against the rebased tree on every pass and is not cached." A session
transcript surfaced the same gap independently, hypothesizing it as a
contributor to unusually high local check/test invocation volume.

The intended outcome: Step 2 skips re-running the project's check/test
commands when the current state has already been verified clean, recording
that fact durably (not by session judgment call) the same way Step 3 now
does for review.

## Approach

Add a sixth content-addressed marker kind, `verification`, whose value is `git rev-parse HEAD^{tree}` — the content-address of the tree `ready-for-review` step 2 actually executes. Step 2 consults it through a new `marker.sh check verification` subcommand (not through `marker.sh status`) and skips the project's check commands on a match; it writes the marker only after every selected command has run and passed. The read is bounded by a marker-file age so a tree hash — which cannot see the installed environment — can't authorize a skip indefinitely.

**Root problem:** `ready-for-review` step 2 re-executes the project's full check/lint/test suite on every pass, including passes where HEAD's tree is byte-identical to one that already passed cleanly, because no durable record of a clean verification exists for a resumed session or a mid-gate handoff to read.

**Givens** (fixed beyond this design's reach):

- **G1. Marker writes are self-attested.** No hook correlates a write to work actually performed; this holds for all five existing kinds and is named as an open residual in `docs/design-decisions/ready-for-review-cumulative-diff-cache.md`. Closing it needs a control that does not exist anywhere in the marker architecture, so it is outside this plan.
- **G2. `ready-for-review/SKILL.md` is capped at 200 lines.** `check-skill-length.sh`'s `limit_for()` default (`:74`) enforces it. `docs/skills.md:125-126` reserves cap relief for a narrow content class — a skill whose body is genuinely one-paragraph-per-line with no compliant reflow path, as `pr-description`'s 250-line override is (`check-skill-length.sh:22`; corrected from this row's original "210," a stale figure caught in plan-review) — and this skill isn't in it: it still has a compliant reflow path (Critical files, Phase 2's Budget bullet), so raising its cap is routine relief the documented exception excludes — see Alternatives set aside below. **Revisited in Phase 3's plan-review round (plan-architect consult), reaffirmed:** the "compliant reflow path" framing this row and the rejected-alternative below both rely on is itself a substantive finding, not just a mechanical check — reflowing a multi-line paragraph to one line removes zero words and zero complexity, so "a reflow path exists" doesn't establish that the content "resists trimming" in the sense `docs/skills.md:129`'s exception actually requires. No override is sought regardless (see Phase 3's Critical files decision-record bullet for why), but the reasoning this row and the rejected alternative give for that conclusion is corrected, not merely restated.
- **G3. A git tree addresses tracked, committed content only.** Untracked files, gitignored files, and a submodule's own working-tree state are outside any tree hash. This is git's object model, not a choice this design makes.
- **G4. CI runs on every push.** Owned by the repo's workflow config, and already the backstop §44's residuals lean on.
- **G5. GitHub branch-protection contents aren't API-verifiable by this tooling.** `require-ready-for-review.sh`'s own header already discloses that its gate-integrity argument assumes branch protection requires CI to pass before merge, a GitHub setting no hook here can verify. `gh api repos/<owner>/<repo>/branches/<default>/protection` returns 403 for a token lacking that scope, and `/rulesets` only covers the newer rulesets feature, not classic branch-protection rules — confirmed empirically against this repo's own `main` (`protected: true` via the plain branches endpoint, but `/protection` 403 and `/rulesets` empty). `[verified: gh api calls against this repo, this session]` A repo's opt-in into the mechanism below is therefore a human declaring branch protection exists, not tooling confirming it.

**Addendum (2026-09-25): per-repo opt-in gate for the `verification` cache.**

A cumulative `/code-review` round against this PR (see `agent-reviews/ciso-reviewer-*.md`) found that `verification` compounds with the pre-existing `cumulative-review` marker in a way that weakens `ready-for-review`'s stated compensating control: before this PR, `cumulative-review`'s self-attestation gap was accepted because "step 2's verification and CI still run unconditionally on every pass, cache hit or not" (`ready-for-review-cumulative-diff-cache.md`, pre-PR text) — a second, structurally non-skippable local gate backstopped a silently-skippable review pass. This PR removes exactly that backstop: step 2 becomes self-attested and cache-skippable by the identical mechanism, leaving CI as the sole non-skippable check, with CI's own dependency on a GitHub setting this tooling cannot verify (G5). The engineer directed expanding this PR to gate `verification`'s skip behind an explicit per-repo opt-in — default off — rather than accepting the compounding or redesigning the cache mechanism itself.

**Assumption rows:**

1. Step 2 executes the whole checked-out tree rather than the diff, so the tree's content-address is the correct key; a diff-hash key's only incremental benefit comes from cases where the executed tree changed underneath the cache, which is the population least entitled to a skip. `[engineer-verified]`
2. Step 3's `cumulative-review` marker stays diff-keyed, and the general rule is that each cache keys on the content-address of what its own step consumes — step 2 executes the tree, step 3 reads the diff. One rule, two consumers. `[engineer-verified]`
3. §44's compensating-control prose is stale in **two** sentences, not the one PR #837 named — line 11 ("step 2's verification runs against the rebased tree on every pass and is never cached") and line 13 ("step 2's verification and CI still run unconditionally on every pass, cache hit or not"). `[engineer-verified]` for line 11; `[verified: docs/design-decisions/ready-for-review-cumulative-diff-cache.md:13]` for line 13. This extends the engineer's stated scope by one sentence — same defect, same file, same fix shape, per CLAUDE.md's audit-structural-siblings rule.
4. `marker.sh status` is the only marker surface that makes a network round trip: its `cumulative-review` line calls `_lib_cumulative_diff_hash`, which shells to `pr-diff-against-base.sh` (→ `gh pr view`) under a 15s cap. Step 2 reading the cache via `status` would pay that round trip for a line it does not consume, one step before step 3 pays it again. `[verified: marker.sh:728-747; _lib.sh:612-632]`
5. `status`'s `live`/`historical` vocabulary means "hash matches / does not match" for all five kinds; folding an age bound into it for one kind alone would make `historical` ambiguous between a mismatched hash and an aged-out marker. `check` already owns the age-bounded skip semantics. `[verified: marker.sh:683-755, 788-797]`
6. The capped/uncapped convention is: write arms call git uncapped and abort loudly, `check`/`status` call it capped and degrade to no-match/absent. `[verified: marker.sh:466 (write, uncapped), :725 (status, capped), :770 (check, capped)]`
7. Four of the five existing write arms recompute their subject at write time; only `cumulative-review` captures first, and §50's rationale for that is a subject that drifts with **no local action at all** (the base moves on the remote). `HEAD^{tree}` has no external drift channel — it changes only when this worktree commits, rebases, or resets. `[verified: marker.sh:399-522; docs/design-decisions/cumulative-review-marker-not-recomputed.md]`
8. `_code_review_marker_fresh_age` is already fully parameterized on (markers_dir, expected_value, glob_prefix, max_age) and is applied only on `check code-review`'s skip path, never on `write` or a commit gate. Its header documents a precondition: callers must run `_lib_marker_value_present` first, so the O(n) candidate loop only runs once a hash match exists. `[verified: marker.sh:295-342, 788-797]`
9. `_resolve_code_review_check_max_age_seconds`'s malformed-input guard is a five-branch `case` pattern (`''|0|*[!0-9]*|0[0-9]*|?????????*`). A second threshold constant duplicating it is a drift defect under CLAUDE.md's single-source-of-truth rule. `[verified: marker.sh:288-293]`
10. `ready-for-review/SKILL.md` is exactly 199 lines against a 200 cap — one line of headroom. `[verified: file; check-skill-length.sh:67-74]`
11. Three count claims are already stale by one from PR #837, independent of this change:
    - `enforce-marker-script-shape.sh:75` says 19 single-command shapes; the deny list at `:658-677` has 20.
    - `enforce-marker-script-shape.sh:639` says 17 shapes in permissions.allow; settings.json has 18.
    - `docs/scripts.md:54` says 17 (same, 18).

    `test_enforce_marker_script_shape.py:27` and `:57` also say 19 against a 20-entry `TILDE_MARKER_SHAPES`. No test pins any of them. `[verified: all five sites read]`
12. `_extract_scope_anchor_region` and `_normalized_anchor_text` are already generic over the marker name and need no change; only `_EXPECTED_CACHE_ANCHORS` (exact set) and `_PINNED_CACHE_CLAUSES` (exact text, whitespace-collapsed) need entries. `[verified: test_skills.py:3211, 3395-3402, 3461-3528]`
13. `_resolve_repo_root` calls `_refuse_main_tree_under_enforcement`, so both new arms inherit the main-tree refusal automatically — including `check verification`, matching `check code-review`'s existing behavior. `[verified: marker.sh:132-144]`
14. No `.gitmodules` exists in this repo, and no `verification-markers` / `check verification` / `write verification` string collides with existing code. `[verified: Glob and Grep over the worktree]`
15. The new kind's tests need no `origin/<default-branch>` arming, unlike `cumulative-review`'s, because a tree hash needs no merge-base. The shared `git_repo` fixture suffices unmodified: it seeds one commit (`init`) before staging further edits, so `git rev-parse HEAD^{tree}` resolves against it with no `origin` arming. `[verified: claude/.claude/hooks/tests/conftest.py:272-285]` **Correction to this row's original text (Phase 3 supersedes its conclusion, per staff-sdet's plan-review finding):** this row's *reasoning* — a tree hash needs no merge-base — remains true for the tree-hash computation itself. Its *conclusion* — no origin arming needed, `git_repo` suffices unmodified — no longer holds once Phase 3 stacks `_lib_verification_cache_sentinel_present`'s sentinel-presence check in front of the tree-hash check on `check verification`'s own path (M10). Every test in `TestMarkerScriptVerification`, not only the tests that assert `match`, now runs against an armed-or-unarmed origin as a precondition of reaching the code each test was written to exercise. See Phase 3's `test_marker_script.py` bullet below for the corrected, class-wide retrofit.
16. `VERIFICATION_CHECK_MAX_AGE_SECONDS` defaults to 14400 (4h) — an explicit design choice, ungrounded in the same sense §62 discloses for its own 86400. Rationale is design intent, not measurement: 4h covers the motivating window the Context names (a resumed session, a mid-gate handoff) while expiring between working sessions, where an interpreter bump or a rebuilt venv would land. Overridable by env var like the existing one. `[unverified]`. **Correction to this row's original text:** it also named "a step-3/4 fix loop back into step 2" as a motivating window; `ready-for-review/SKILL.md`'s own fix-loop rule ("after a fix produced by step 2, 3, or 4, return to step 2") always follows a fix *commit*, and a fix commit always changes `HEAD^{tree}`, so that path can never actually produce a `verification` cache hit. Dropped from the motivating-window list; the residual reasoning around the 4h default is otherwise unaffected. `[verified: claude-skills/skills/ready-for-review/SKILL.md's own fix-loop-order line]`
17. **The opt-in sentinel is read from `origin/<default-branch>`, never the working tree, via `_lib_default_branch_from_origin_head` — not `_lib_default_branch_or_guess`.** A working-tree read would let the branch under review self-authorize its own relaxation: the same self-attestation shape G1 already names, recurring one level up at the opt-in *decision* itself rather than at a marker write. The strict resolver is required specifically because its failure mode (empty stdout, no candidate fallback) is the correct fail-closed default here: an unset `origin/HEAD` must disable the cache, not guess a branch name via `_lib_default_branch_or_guess`'s candidate-probing fallback and risk activating it against the wrong ref. `[verified: _lib.sh:769-826 — the strict resolver's two-outcome contract vs. the guessing resolver's candidate fallback]` **Scope correction (plan-review, ciso-reviewer):** this row's self-authorization claim holds only against a *content-based* channel — committing the sentinel file on the branch under review itself. It does not hold against a *ref-based* channel: `refs/remotes/origin/<default-branch>` is an ordinary local ref, writable by any git command (`git update-ref refs/remotes/origin/main <fabricated-commit>`) with no live network or `gh api` verification, and no hook in this repo gates that fabrication for the engineering session that would run `ready-for-review` (as opposed to a review-only agent, which `deny-reviewer-tree-mutation.sh` does restrict). This relocates the self-authorization gap by one hop (file-on-branch → ref-on-local-clone) rather than closing it; forging the sentinel is the sole precondition standing between "cache permanently inert" and "cache active" for a repo that never actually opted in. Named as a residual at parity with G1 in Phase 3's decision-doc addition below, not as a closed gap. `[verified: relayed from ciso-reviewer's plan-review finding, this session — _lib_default_branch_from_origin_head's own "local refs only, never the network" docstring, contrasted with pr-diff-against-base.sh:70's live gh pr view anchor]`
18. **This deviates from the `.claude/worktree-required` precedent, which reads the working tree.** `_refuse_main_tree_under_enforcement` (`_lib.sh:1865`) checks `$repo_root/.claude/worktree-required` in the tree being operated on, not `origin`. That precedent is safe to read locally because its consequence is confined to the same session's own git hygiene — a branch declaring "worktree not required" only relaxes a check on itself. This sentinel's consequence is different: a `match` silently skips a step whose false-clean marker isn't confined to the session that wrote it (verification-cache.md's residual 6), so reading the sentinel off the branch under review would let that branch grant itself the exact relaxation a reviewer round is meant to catch — against the content-based channel row17's correction scopes this claim to; see that correction for the ref-based channel this design does not close.
19. **plan-architect (consult) rejected both readings of "rescope the self-attestation toward running the full-suite of tests" as an alternative to opt-in gating.** Binding `check verification`'s match to evidence of an actual passing run (G1's closure) needs each repo to declare its verification commands machine-readably first — the opt-in sentinel is a prerequisite for that future work, not an alternative to it, since nothing in this design can verify "the full suite ran" without such a declaration existing somewhere. Gating only `write verification` on a full-suite run (rather than gating `check`) doesn't bound the risk either, since the `write` call itself stays unverified by any hook, and it would collide with this repo's own `select-tests.py`-not-full-suite convention for agents (`CLAUDE.md`'s Commands section). `[engineer-verified: relayed from plan-architect's MODE=consult report this session]`
20. **The Python 3.12→3.14 CI bump (commit `6c2d4b17`) is out of this plan's scope entirely, not a mechanism this design touches.** It rode into the branch as two standalone follow-up commits reacting to a misdiagnosed CI failure (the actual cause was an unrelated `assert_cap_engaged()` call-signature bug, fixed by `04816924`), was never part of any phase below, and is reverted as an independent commit alongside this addendum's changes — not because this design requires Python 3.12, but because no rationale for 3.14 survived a `plan-architect` consult and a repo-wide check for any construct actually requiring it. `pyproject.toml`'s `target-version` and the three `.claude/plans/*.md` "CI pins 3.12" citations are untouched by the revert: the former was never keyed to CI's interpreter in the first place (it is ruff's minimum-target setting, and raising it would let `--fix` apply syntax that breaks stow consumers at `install.sh`'s enforced 3.11 floor), and the latter are dated records of what was checked when each plan ran, not live claims about current CI (`CLAUDE.md`'s Axis-3 preserved-content test). `[engineer-confirmed: revert, this session]`

**Mechanisms:**

- **M1 — a new `verification` marker kind, tree-keyed.** *anchors: root, row1.* Over-powered-primitive check, two lighter alternatives from the existing system: (a) *reuse the existing `ready-for-review` HEAD-SHA marker as step 2's signal* — fails because it is written at step 8 after the whole gate completes, gates the push, and asserts "the gate finished," not "the checks passed," so reading it at step 2 would skip on a record written for a different claim; (b) *let the session remember it already ran step 2* — fails on the Context's own motivating case, a resumed session or handoff with no such memory, which is precisely why the record must be durable rather than in-context.
- **M2 — `check verification` as the read surface, plus a `status` report line.** *anchors: row4, row5.* `check` is the existing home for an age-bounded advisory skip and keeps step 2 fully offline; the `status` line is added separately, unbounded, because `status`'s documented contract is to report every completion marker and an omitted kind is a debugging blind spot. This is the same split `code-review` already has across the two surfaces.
- **M3 — age bound via `_marker_fresh_age` + `VERIFICATION_CHECK_MAX_AGE_SECONDS`.** *anchors: row8, row16, G3.* The tree hash cannot observe interpreter version, venv contents, gitignored files, or a submodule's dirty tree, so hash-match alone must not authorize a skip forever. Rename `_code_review_marker_fresh_age` → `_marker_fresh_age` once it has two callers (CLAUDE.md: a narrowly-scoped label pushes up to the canonical name); it is already parameterized, so only the name and five test docstrings move.
- **M4 — recompute at write time; do not adopt §50's capture-then-consume.** *anchors: row7.* §50's two-phase shape exists for a subject with an external drift channel; `HEAD^{tree}` has none, so importing it would be a heavier coordination pattern closing a gap that does not exist, and would cost a new subcommand plus a third command in a SKILL.md with no headroom. This puts `verification` at parity with the four existing local-subject arms. The residual — a false hit needs a commit landing strictly between checks passing and the write, inside one step-2 pass, which the gate's own flow never produces — is named in the decision doc rather than engineered away.
- **M5 — shared `_lib_head_tree_hash <capped|uncapped> REPO_ROOT` in `_lib.sh`.** *anchors: row6.* One helper called by write, check, and status, mirroring `_hash_staged_diff`'s existing mode argument; a hash recipe duplicated across write and read sides drifts and then never matches, which is `_lib_active_plan_hash`'s documented reason for existing.
- **M6 — tighten step 1's clean check to name untracked files.** *anchors: G3, row1.* The tree key is only honest if the working tree matches HEAD's tree; an untracked test file is executed by the checks but sits outside the tree hash, in both directions (present at the clean pass then deleted, or added after it). `git status --porcelain` already reports untracked entries by default, so this is a prose-ambiguity fix, not a new check — and it fits on the existing line. Closes the precondition rather than leaving it a residual.
- **M7 — correct §44's two compensating-control sentences and link the new decision file.** *anchors: root, row3.* The change falsifies both in the literal sense while keeping them true in substance (a rebase changes the tree, so step 2 still re-runs); §44 gets corrected clauses plus a pointer, and the new file owns the why-two-keys-differ explanation.
- **M8 — correct the three stale counts while bumping them.** *anchors: row11.* Bumping an already-wrong count to wrong-plus-one is worse than correcting it, and all five sites live in files this change already opens.

**Addendum mechanisms (per-repo opt-in gate):**

- **M9 — a committed sentinel, `.claude/ready-for-review-verification-cache-optin`, and a new `_lib_verification_cache_sentinel_present REPO_ROOT` helper.** *anchors: row17, row18, G5.* The file's presence at the tip of `origin/<default-branch>` is the sole signal; content is documentation only, following `.claude/worktree-required`'s own convention (a comment explaining what the file's presence activates) rather than a parseable value — there is nothing to parse, since this is a boolean fact, not a configuration surface. The helper runs `_lib_default_branch_from_origin_head`, then, only on its success, `git cat-file -e "origin/$default_branch:.claude/ready-for-review-verification-cache-optin"` (precedent: `cleanup-merged-branches.sh:536`'s identical `cat-file -e` existence-check shape) — capped, since this call sits on `check verification`'s read path alongside the tree-hash and status calls it already makes. Any failure at either step (unresolved `origin/HEAD`, the path missing at that ref) returns non-zero, meaning "not opted in."
- **M10 — the gate lives only in `check verification`; `write verification` and `status` stay unconditional.** *anchors: root, row19.* Call `_lib_verification_cache_sentinel_present` first, before the existing uncommitted-status and tree-hash calls, so a not-opted-in repo's `check` short-circuits to `no-match` without the extra git calls the existing arm already makes. `write` writes exactly as before regardless of the sentinel: a marker nobody's `check` will ever read grants nothing on its own, so gating the write would add a second enforcement point for a property `check` already enforces alone. `status`'s `verification` line stays unconditional too, for the same reporting-contract reason M2 gives for not folding the age bound into it — an operator debugging a not-opted-in repo still wants to see the raw hash state.
- **M11 — add "step 2's reported cache hit" to `SKILL.md:132`'s completion-outcomes list.** *anchors: root.* That line already names "step 2's scope-exception skip" and "step 2's skip of undefined commands" as outcomes that count as complete without full execution, and separately names "step 3's reported cache hit" — but omits step 2's own cache hit, the exact outcome this whole plan adds. As literally written, a `verification` cache hit (in an opted-in repo) blocks the completion marker from ever being written. One clause added to an existing line; no line-count growth beyond it.
- **M12 — document, not code, for the CI-diagnose false-clean-marker interaction.** *anchors: root.* `SKILL.md:197`'s diagnose step treats a local-pass/CI-fail split as `root-cause-analysis`'s environment-drift signal. In an opted-in repo, a `verification` cache hit means step 2 didn't actually run locally this pass, so "local pass" may mean "cached," not "just verified" — the diagnose step could misread a stale-cache false-clean as environment drift. This applies only in opted-in repos and needs a residual line in the new decision doc, not a code change: the diagnose step already asks step 2 whether it passed, and a cache-hit report already exists (M11) for it to read.

**Alternatives set aside.**

- Keying on the diff hash or the commit SHA — settled: step 2 executes the tree; commit-SHA is strictly dominated.
- Reading the cache from `marker.sh status` — rejected on M2's two grounds.
- Chaining the write to the check command as `<checks> && marker.sh write verification` — rejected: `enforce-marker-script-shape.sh` fast-exits at `:591` unless the command *starts* with the marker.sh path, so the chained form would be shape-unvalidated, and `permissions.allow`'s exact-match entry would not cover it, causing a permission prompt on every gate run.
- Adding `ready-for-review/SKILL.md` to `limit_for()`'s per-skill override table instead of reflowing — rejected on two grounds: `pr-description`'s 250-line override (corrected from "210," a stale figure caught in plan-review; `check-skill-length.sh:22`) is justified by having *no* compliant reflow path (a file that is one-paragraph-per-line with no hard-wrap, so word-trimming can't reduce its line count), and that rationale is false here, since `:54-63`'s two paragraphs are a compliant reflow path worth 7 lines against the 5 needed; and the override is the larger diff besides, since `test_check_skill_length.py:385-465` pairs every override entry with its own under-cap/over-cap test pair and pins the absence of others, while reflow touches no test.
- **Reading the sentinel from the working tree instead of `origin/<default-branch>`** — rejected per row18: the branch under review would then control its own opt-in state, defeating the point of gating a cross-session skip behind a declared, reviewed, default-branch fact.
- **Binding `check verification`'s match to an actual passing run instead of opt-in gating** — rejected per row19 (plan-architect consult): needs a machine-readable per-repo declaration of verification commands that doesn't exist yet; the opt-in sentinel is a prerequisite for that later work, not a substitute for it.
- **Gating `write verification` on a full-suite run instead of gating `check`** — rejected per row19: the write call itself would remain unverified by any hook, and it collides with this repo's own `select-tests.py`-not-full-suite convention for agents.

## Critical files

Two sequenced `code-writer` dispatches. Phase 2 is not parallelizable with phase 1: the SKILL.md text prescribes the exact commands phase 1 makes valid, and `_PINNED_CACHE_CLAUSES` pins text that must already exist.

**Phase 1 — marker plumbing and gates** (verification: `.venv/bin/python3 claude/.claude/scripts/select-tests.py`, `.venv/bin/ruff check claude/.claude/`, `scripts/list-shell-files.sh | xargs -0 .venv/bin/shellcheck`)

- `claude/.claude/hooks/_lib.sh` — add `_lib_head_tree_hash <capped|uncapped> REPO_ROOT` beside `_lib_cumulative_diff_hash` (`:612`), running `git -C "$repo_root" rev-parse "HEAD^{tree}"` (quote the literal). **Reuse:** `_lib_capped_for`/`_lib_capped` (`:27-48`) for the capped mode; follow `_hash_staged_diff`'s existing mode-argument shape rather than inventing one.
- `claude/.claude/scripts/marker.sh` —
  - Extract `_marker_max_age_or_default RAW DEFAULT` from `_resolve_code_review_check_max_age_seconds` (`:288-293`) so the five-branch malformed-input `case` has one copy; add `_resolve_verification_check_max_age_seconds` with default `14400`, disclosing it as ungrounded in its header the way the existing one cites §62.
  - Rename `_code_review_marker_fresh_age` → `_marker_fresh_age` (`:295-342`, call site `:793`).
  - New `write verification` arm after `cumulative-review` (`:522`): resolve session id and repo root, compute via `_lib_head_tree_hash uncapped`, abort without writing on empty, `mkdir -p "$CONFIG_DIR/verification-markers"`, write `<repo-hash>.<session-id>`. Compute before redirecting, per the convention `:404-406` states. No `_guard_staged_vs_unstaged` (the subject is the committed tree, not the staged diff).
  - New `check verification` arm beside `check code-review` (`:756-800`): `_lib_head_tree_hash capped`, no-match on empty, then `_lib_marker_value_present` first and `_marker_fresh_age` second — that ordering is the helper's documented precondition (`:299-302`). Print `match age_seconds=%s` / `no-match` with the same exit codes.
  - New `status` line for `verification` after the `ready-for-review` line (`:726`), capped and stderr-suppressed, empty-not-fatal — no age bound here, matching how `status`'s `code-review` line reports hash state while `check code-review` applies the bound.
  - Update `usage()` (`:37-53`): the `status` description's kind list, the `write` combination row, and the `check` row (`check code-review | verification`).
  - Update the `write` rejection message (`:524`) — `test_write_roster_is_closed_and_self_reported` parses this string and compares it to `WRITE_SKILLS`, so it must list `verification` last — and the `check` rejection message (`:802`).
  - **Load-bearing comment**, one line, at the new write arm's value computation: ``Each marker kind hashes what its own step consumes. `verification` hashes the tree (what step 2 executes); `cumulative-review` hashes the diff (what step 3 reads).``
- `claude/.claude/hooks/enforce-marker-script-shape.sh` — `MARKER_SHAPE` (`:598`): `check[[:space:]]+code-review` → `check[[:space:]]+(code-review|verification)`, and add `verification` to the `write` alternation. Add both new shapes to the deny-message list (`:658-677`). Leave `VALID_CHAINED_COMMIT_PATTERN` (`:629`) untouched — `verification` is written after checks pass, never before a commit, same as `cumulative-review`. **Count corrections:** header `:75` 19 → 22 (correcting row 11's pre-existing off-by-one to 20, then adding 2 new shapes — not 19 + 2); `:639` 17 → 20 (same fix: pre-existing actual is 18, plus 2 new).
- `claude/.claude/settings.json` — two exact-match entries after `:21`: `"Bash(~/.claude/scripts/marker.sh write verification)"` and `"Bash(~/.claude/scripts/marker.sh check verification)"`. No globs.
- `claude/.claude/hooks/tests/test_marker_script.py` — append `"verification"` to `WRITE_SKILLS` (`:869`); add a `TestMarkerScriptVerification` class modelled on `TestMarkerScriptCumulativeReview` (`:1981-2514`) covering: write success and directory isolation, write abort in a commit-less repo, `check` match/no-match, `check` no-match on an aged-out marker and on a moved HEAD, `check` no-match when the tree hash can't be computed, `status` live/historical/absent, and write/check recipe agreement. Use plain `git_repo` with no `origin` arming, per row 15. Update the five `_code_review_marker_fresh_age` docstring references (`:2904, 2963, 3031, 3149, 3156`). Three further cases are load-bearing, not optional additions:
  - **Same-tree HEAD move still matches.** `check` must report `match` after `git commit --amend --no-edit` (or any commit reproducing an identical tree) against a marker written before the amend — the positive-direction proof that tree-keying (row 1's whole justification over commit-SHA-keying) actually behaves differently from commit-keying. Pair this with a write-side assertion that the marker's stored value equals an independently-computed `git rev-parse HEAD^{tree}` (not just "looks like a hex digest," which would pass even if the write arm accidentally hashed `HEAD` instead of `HEAD^{tree}`).
  - **`VERIFICATION_CHECK_MAX_AGE_SECONDS` gets its own resolver tests**, mirroring the six existing `CODE_REVIEW_CHECK_MAX_AGE_SECONDS` tests (`:2883-3016`) rather than relying on those tests alone to prove `_marker_max_age_or_default`'s extraction is safe: override, malformed-value fallback to 14400, and the strict-less-than boundary, keyed to `verification`'s own env var name and default.
  - **A timing test for the new capped git call**, mirroring the three existing `@pytest.mark.timing` tests (`:3075-3193`) that prove a stalled `git diff`/`stat`/`grep` degrades to `no-match` rather than hanging — `_lib_head_tree_hash capped` is a new call on the same `check` fast-path and needs the same proof.
- `claude/.claude/hooks/tests/test_enforce_marker_script_shape.py` — append both shapes to `TILDE_MARKER_SHAPES` (`:31-52`); correct the count comments at `:27` and `:57` from 19 to 22 — `TILDE_MARKER_SHAPES` already has 20 entries (row 11's pre-existing off-by-one), plus 2 new, not 19 + 2.

**Phase 2 — skill body, anchors, docs** (verification: `.venv/bin/python3 claude/.claude/scripts/select-tests.py`, `/skill-review` hook-enforced on the SKILL.md diff, `wc -l claude-skills/skills/ready-for-review/SKILL.md` ≤ 199)

- `claude-skills/skills/ready-for-review/SKILL.md` — must end at ≤199 lines.
  - Step 2 gains a `CACHE_RULE:ready-for-review-verification-cache` anchor pair placed before the command-selection paragraph and before the scope-exception skip, enclosing exactly this text:

    > Before selecting any commands, run `~/.claude/scripts/marker.sh check verification`. `match` means this exact tree already passed a clean verification pass inside the freshness window. On a match: skip the commands below, report the cache hit in the Completion summary, and continue to step 3. On `no-match`, run the step normally, then write `~/.claude/scripts/marker.sh write verification` only after every selected command has run and passed. Do not write it after the scope-exception skip below — that path runs no commands, so nothing has passed.

    Do not copy step 3's "Content type is never a skip reason on its own" sentence: step 2 has its own content-type skip immediately below, and the two would contradict.
  - Step 1 (`:38`): `no unstaged or uncommitted changes` → `no unstaged, uncommitted, or untracked changes`. Same line, no growth.
  - Completion (`:166`): `- Verification: commands run and their results.` → `- Verification: commands run and their results, or "skipped — cache hit."`
  - **Budget:** +5 lines from the anchor block. Recover ≥7 by reflowing two step-2 paragraphs to the file's own long-line style (`:70` and `:118` are already single long lines): "Scope exceptions" (`:54-59`, 6 lines → 1) and "Pre-existing failures" (`:61-63`, 3 lines → 1), landing at 197. Preserve every word and every `**bold**` marker — this is reflow (line-joining only), not compression; verify with a whitespace-insensitive diff of the two paragraphs against `git show HEAD:claude-skills/skills/ready-for-review/SKILL.md`, not by eye. Leave the three `HOOK_TEST_FIXTURE` fenced blocks (`:27-30, 140-143, 147-150`) untouched; the hook-alignment suite reads them verbatim.
- `claude-skills/skills/tests/test_skills.py` — add `("ready-for-review", "CACHE_RULE:ready-for-review-verification-cache")` to `_EXPECTED_CACHE_ANCHORS` (`:3466`) and the exact clause above to `_PINNED_CACHE_CLAUSES` (`:3501`). **Reuse:** `_extract_scope_anchor_region` and `_normalized_anchor_text` unchanged.
- `docs/design-decisions/ready-for-review-verification-cache.md` — new file. H1, blank line, `*2026-09-09.*` (no `Formerly §N` — recorded after the split). Content: the tree-hash key and why; the one-sentence general rule (each cache keys on the content-address of what its own step actually consumes) reconciling it with `cumulative-review`'s diff key; why the read is age-bounded and that 14400 is an ungrounded default disclosed the way §62 discloses 86400; why `check` and not `status` carries the bound; and four named residuals — recompute-at-write's narrow false-hit window (with §50's rationale for why the two-phase shape does not transfer), a submodule's dirty working tree being invisible to a tree hash, G1's self-attestation at parity with the other five kinds, and the manual-invalidation path: with no operator-facing invalidation lever beyond the 4h age bound or an actual tree-changing commit, the answer to "the cache said clean but it's wrong" is deleting the stale file directly under `verification-markers/`, which is worth stating explicitly rather than leaving implicit.
- `docs/design-decisions/ready-for-review-cumulative-diff-cache.md` — correct two sentences (`:11` and `:13`) to say step 2's verification is now cached on the tree it executed, so a rebase always re-runs it, and link the new file for the two-keys reconciliation. Do not restate the reconciliation here. Everything else in the file is a preserved record — leave it.
- `docs/design-decisions/content-addressed-review-markers.md` — `:5` currently opens "Five kinds exist"; make it six and add a `verification` clause naming the tree-hash key, mirroring how the `cumulative-review` clause reads.
- `docs/hooks.md` — Marker keying section (`:103`): add `verification` to the per-kind content enumeration, naming `HEAD^{tree}` and that it is read by `ready-for-review` step 2 via `marker.sh check verification`, not by a `require-*.sh` hook.
- `docs/scripts.md` — `:54`: 17 → 20 (pre-existing actual is 18, plus 2 new — not 17 + 2), and name the new kind.

**Review surface:** 13 files across three domains (shell scripts + hooks, skill body, docs), with risk concentrated in `marker.sh`'s two new arms and the SKILL.md reflow. The doc and count edits are mechanical.

**Phase 3 — per-repo opt-in gate, plus disposition of the cumulative-review round's findings** (verification: `.venv/bin/python3 claude/.claude/scripts/select-tests.py`, `.venv/bin/ruff check claude/.claude/`, `scripts/list-shell-files.sh | xargs -0 .venv/bin/shellcheck`; not parallelizable with the SKILL.md doc edits below, since the new decision-doc residual and the corrected compensating-control sentences cite each other)

- `.claude/ready-for-review-verification-cache-optin` — new committed file at the repo root. **Naming, per plan-review (plan-architect consult):** the file names the mechanism it toggles and which way — matching this repo's existing sentinel family (`.claude/worktree-optout`, `.claude/autonomous-shipping-optout`, `pr-description-tighten-prose-optout`, all `<mechanism>-optout`/`-optin`, no hyphen) — rather than naming the precondition (the original `branch-protection-enforced` draft named an incomplete, externally-unverifiable GitHub-settings fact the mechanism itself never checks). The skill-name prefix (`ready-for-review-verification-cache`, already used twice in this codebase — the `CACHE_RULE:` anchor and the design-doc filename) resolves the ambiguity a bare "verification" would carry against this repo's own `code-review`/`plan-review`/`cumulative-review`/`skill-review` vocabulary, without inventing a new term the way a "test-" prefix would (step 2 skips test, lint, *and* typecheck commands — "test" alone understates it). Content is the full compound attestation, since the name can't carry it:

  ```
  # Claude Code ready-for-review verification-cache opt-in.
  #
  # Presence of this file on the default branch (read from origin, never the
  # working tree) lets ready-for-review's step 2 skip re-running this repo's
  # test, lint, and typecheck commands when the same tree already passed them
  # inside the freshness window.
  #
  # Committing it attests two things no tooling here can verify:
  # 1. The default branch requires CI to pass before merge.
  # 2. That CI runs the checks step 2 would otherwise run locally.
  #
  # Forks and copies: this attestation belongs to the repo that committed it.
  # Delete it unless both facts hold for yours.
  # To disable, delete this file from the default branch; existing worktrees
  # keep the old state until their next git fetch.
  # See docs/design-decisions/ready-for-review-verification-cache.md.
  ```

  Land this file itself in this PR — `claude-config`'s own `main` already has branch protection with `tests` as a required check (engineer-confirmed screenshot; `protected: true` via `gh api repos/<owner>/<repo>/branches/main`), so this repo is a legitimate first opt-in, not a hypothetical example. **Fork-inheritance disclosure (plan-review, plan-architect consult; engineer-confirmed to proceed):** a GitHub fork's own default branch starts as a copy of upstream's default branch at fork time, including this committed file — but a fork's branch-protection rules are a separate, per-repo GitHub setting that forking never configures, so a fresh fork would have the file present (content copies) while having no branch protection configured (settings don't copy). `check verification` reads only the file's presence, so a fork would report itself opted in with nobody having made that decision for the fork specifically. The comment's "Forks and copies" line is the sole mitigation — a disclosure, not a control — named as the 13th residual below rather than engineered around, since withholding the maintainer's own opt-in to avoid it would cost every clone of upstream (which *are* protected) the cache entirely.
- `claude/.claude/hooks/_lib.sh` — add `_lib_verification_cache_sentinel_present REPO_ROOT` beside `_lib_head_tree_hash` (M9): resolve the default branch via `_lib_default_branch_from_origin_head`, return 1 immediately on its failure, then `_lib_capped git -C "$repo_root" cat-file -e "origin/$default_branch:.claude/ready-for-review-verification-cache-optin"`, propagating that call's exit status. **Pinned decision (staff-sdet's plan-review finding):** `cat-file -e` confirms object existence only, not type — a tree object (directory) at that path returns exit 0 identically to a blob. This is deliberately accepted, not a gap: the signal this helper reads is "something is committed at this path on the default branch," and a directory there is still a deliberate commit by whoever controls `main`, carrying the same opt-in authority as a file. Document this pinned reading in the function's own header comment, one line, so a future reader doesn't mistake the untyped check for an oversight.
- `claude/.claude/scripts/marker.sh` — in the `check verification` arm (M10), call `_lib_verification_cache_sentinel_present "$REPO_ROOT"` immediately after `REPO_ROOT=$(_resolve_repo_root)`, before the existing `UNCOMMITTED_STATUS` check; on failure, `printf 'no-match\n'; exit 1` with no further calls. No change to `write verification` or the `status` line.
- `claude-skills/skills/ready-for-review/SKILL.md` — M11: append ", or step 2's reported cache hit" to the completion-outcomes list at `:132`, inserted after "step 2's skip of undefined commands" for step-2-then-step-3 ordering. This edit inserts no new line break — it lengthens an existing single-line paragraph — so per `check-skill-length.sh:24-26`'s own documented convention for this file's one-paragraph-per-line style, it cannot change the file's line count at all; no budget consideration applies.
- `docs/design-decisions/ready-for-review-verification-cache.md` — add five residuals, continuing the existing seven:
  - **8th — the compounding self-attestation effect with `cumulative-review`** (M12's framing plus the ciso-reviewer finding from the cumulative code-review round): this is the first marker kind whose self-attestation gap covers actual local test/lint/build execution rather than an LLM judgment pass, and a repo that opts in still leaves CI as the sole non-skippable backstop once both `cumulative-review` and `verification` are cache-hit in the same pass. Restate (do not duplicate) `require-ready-for-review.sh`'s branch-protection-unverifiable caveat here, per G5, since the compensating-control argument now depends on it more heavily than before.
  - **9th — the CI-diagnose interaction** (M12), scoped to opted-in repos: a `verification` cache hit means step 2 didn't actually run locally this pass, so the diagnose step's local-pass/CI-fail environment-drift signal could misread a stale-cache false-clean as drift.
  - **10th — the opt-in gate's self-authorization resistance is scoped to the content-based channel only** (row17's plan-review correction, ciso-reviewer): reading the sentinel from `origin/<default-branch>` instead of the working tree stops a branch from opting itself in by committing the file, but does not stop the same session from fabricating the local `refs/remotes/origin/<default-branch>` ref directly via git plumbing (`git update-ref`) with no live network verification — no hook in this repo gates that fabrication for the engineering session that runs `ready-for-review`. At parity with G1: the actor capable of forging this ref is the same actor already capable of a dishonest `write verification`.
  - **11th — `status`'s `verification` line reports the same tree-hash-match fact `check`'s sentinel gate depends on, ungated** (ciso-reviewer): `status` was never gate-bearing before this addendum and still isn't — `SKILL.md` never instructs consulting it for the skip decision — but it now reports a fact a gate depends on with no sentinel test of its own, a second surface an imprecise read could mistake for authorization.
  - **12th — a local, stale `refs/remotes/origin/<default-branch>` delays rollback** (staff-sdet): turning the cache off is deleting the sentinel from the real `origin/<default-branch>`, but a worktree whose local remote-tracking ref predates that deletion keeps reporting the repo as opted-in until its next `git fetch`, with no bound analogous to `VERIFICATION_CHECK_MAX_AGE_SECONDS` on the opt-in decision itself.
  - **13th — a fork inherits the sentinel without inheriting the branch-protection it attests to** (plan-review, plan-architect consult): a GitHub fork's default branch copies upstream's committed content, including this file, but never copies upstream's branch-protection configuration — that is a separate per-repo GitHub setting a fork owner must set up independently. A freshly forked repo therefore reports itself opted in (the file is present) while carrying none of the CI-required-check guarantee the file exists to attest. The sentinel's own "Forks and copies" comment line is the sole mitigation — a disclosure a fork owner must read and act on, not a control this design enforces.
  
  Correct residual 3's "at parity with the other five kinds" framing is unaffected — these are new residuals, not edits to that one. Also correct the "step-3/4 fix loop back into step 2" bullet in the age-bound motivating-window list (row16's correction) by deleting it.
- `docs/design-decisions/ready-for-review-cumulative-diff-cache.md` — scope both edited compensating-control sentences (`:11`, `:13`) to "in repos that have opted into the `verification` cache" and link the new residual above, resolving the ciso-reviewer [BLOCKER] and [CONCERN] findings together. Also fix the stale count in the same paragraph (`:13`, "the other four marker kinds" → "the other five marker kinds" — this PR's sixth kind, `verification`, makes it five *other* kinds, not four; a pre-existing miss caught by staff-platform-engineer, not introduced by this addendum). Split the "accepted mitigation" sentence at the same location into two sentences per comment-discipline-reviewer's exact fix text (one fact — the rebase/tree-change cause-effect pair — per sentence, CI's unconditional-regardless-of-cache-hit fact in its own sentence).
- `claude/.claude/scripts/marker.sh` — split the `status` subcommand's `verification` line's multi-fact comment into one fact per sentence, per comment-discipline-reviewer's exact fix text (same-recipe-as-write fact; zero-commit-repo-reports-absent fact; no-age-bound-here fact each get their own sentence, with the third sentence's two clauses kept together as a tightly coupled contrast pair).
- `claude/.claude/hooks/tests/test_marker_script.py` — **fold the opt-in gate into the existing `TestMarkerScriptVerification` class; do not add a separate `TestMarkerScriptVerificationOptIn` class.** A separate class was considered and rejected (staff-sdet's plan-review finding): M10's gate ordering runs `_lib_verification_cache_sentinel_present` *before* the existing `UNCOMMITTED_STATUS` and `_lib_head_tree_hash` calls, so once Phase 3 lands, every existing test in this class — not only the ones asserting `match` — must reach an armed-and-opted-in origin before the code path it was written to exercise is reachable at all. A separate class would only add new tests without ever re-arming the old ones, silently defeating the class's entire existing no-match coverage (the untracked-file guard, hash-staleness detection, tree-changed invalidation, both age-bound no-match paths, the unresolvable-hash fail-closed path, and — critically — both `@pytest.mark.timing` tests Phase 1 designated load-bearing, `test_head_tree_hash_computation_times_out_to_no_match` and `test_check_status_call_times_out_to_no_match`, which would keep passing without ever reaching their stubbed slow call). **Required retrofit, class-wide:** add origin-arming (reusing `TestMarkerScriptCumulativeReview`'s helpers, `test_marker_script.py:198-216, 329-331`, which add `refs/remotes/origin/main` and the `origin/HEAD` symref) plus a committed `.claude/ready-for-review-verification-cache-optin` on that armed `origin/main`, to this class's own shared fixture or setup — not to individual tests — so every existing assertion, match and no-match alike, continues to exercise the code path it was written against rather than the new prerequisite gate. Then add the opt-in-specific cases: `check` reports `no-match` with a valid fresh marker but no sentinel anywhere; `check` reports `match` with the sentinel present on `origin/<default-branch>`; `check` reports `no-match` when the sentinel is committed to the branch under test's own local `HEAD` but absent from `origin/<default-branch>` (the self-authorization case row18 names — committed-to-local-HEAD, not merely uncommitted, since an uncommitted-only variant is already subsumed by the pre-existing untracked-file guard and proves nothing new about row18); `check` reports `no-match` when `origin/HEAD` is unset entirely. Add one `@pytest.mark.timing` test for the new capped `_lib_verification_cache_sentinel_present` call, mirroring the two existing timing tests' stub-and-assert-degradation shape — Phase 1's plan imposed this requirement on `_lib_head_tree_hash capped`; the new capped `git cat-file -e` call needs the identical proof.
- `claude/.claude/hooks/tests/test_lib.py` — direct unit test for `_lib_verification_cache_sentinel_present`, since it is a pure predicate over local refs (unlike the hash-computing family staff-sdet's FYI notes as conventionally untested at this layer): present-on-origin-default returns 0, absent returns 1, `origin/HEAD` unset returns 1, present only on a non-default `origin/<other>` branch returns 1.
- `CONTRIBUTING.md` — new `## Forking` section (no such section exists today; confirmed by reading the full file). States the 13th residual for a contributor audience: forking this repo copies `.claude/ready-for-review-verification-cache-optin` without copying the branch-protection configuration it attests to, so a fork owner who wants `ready-for-review`'s local check/lint/test skip should verify their own fork's branch protection actually requires CI before relying on it, and should delete the file otherwise. Cross-links `docs/design-decisions/ready-for-review-verification-cache.md` for the mechanism, per this repo's cite-don't-restate convention (`.claude/rules/design-decisions.md`).
- `docs/design-decisions/ready-for-review-verification-cache.md` — one further addition beyond the residuals above: a line recording that a per-skill cap override for `ready-for-review/SKILL.md` was considered and rejected during this plan's review (plan-architect consult) — this skill's growth is sequential procedure, not a routing/ownership table of the kind `docs/skills.md:129` reserves the exception for, and no trim audit had been done before Phase 1/2 reached for reflow. Record that further reflow-to-fit is closed as a pattern for this file going forward: the next change that would need it should trigger an actual trim audit first, with a byte-based length metric (`_lib_staged_length_gate`'s existing but currently-unused `BYTE_LIMIT` argument, `_lib.sh:1659`) as a possible follow-up if the audit finds nothing left to cut — out of scope for this PR, since it would be a repo-wide mechanism change, not a `ready-for-review`-specific one.

**Review surface (Phase 3):** 9 files. Risk concentrated in the new helper's origin-vs-working-tree distinction and the `check verification` gate-ordering change; the doc, comment, and contributor-doc edits are mechanical disposition of already-identified findings.

## Verification

Per this repo's CLAUDE.md, agents run the scoped selector, not the full suite:

```bash
.venv/bin/python3 claude/.claude/scripts/select-tests.py
.venv/bin/ruff check claude/.claude/ claude-skills/
scripts/list-shell-files.sh | xargs -0 .venv/bin/shellcheck
```

The changed paths (`claude/.claude/scripts/marker.sh`, `claude/.claude/hooks/_lib.sh`, `claude/.claude/hooks/enforce-marker-script-shape.sh`, `claude/.claude/settings.json`, `claude-skills/skills/ready-for-review/SKILL.md`, `docs/**`) should resolve to `SCRIPTS_TESTS_DIR` + `HOOKS_TESTS_DIR` + `SKILLS_TESTS_DIR` rather than a full-suite widen — `DOCS_DIR` is a declared blanket rule (`select-tests.py:139-147`) and `CLAUDE_SETTINGS_JSON` names all three test directories (`:119-126`). If the selector widens to the full suite anyway, that is the correct run, not a reason to narrow by hand.

Tests that must be green and are specifically load-bearing here:

- `test_marker_script.py` — the new `TestMarkerScriptVerification` class, plus `test_write_roster_is_closed_and_self_reported`, which reads the write roster back off the rejection message. Within that class, the same-tree-HEAD-move `check` case and its paired write-side hash assertion are the ones actually proving tree-keying behaves differently from commit-SHA-keying — treat those two as load-bearing above the rest of the class.
- `test_enforce_marker_script_shape.py` — `test_valid_shapes_allowed` and `TestPrescriptionAllowlistAlignment::test_every_hook_accepted_tilde_shape_has_an_allow_entry_or_is_excepted`, which cross-checks each new shape against `settings.json`.
- `test_marker_script.py`'s existing max-age tests (`:2883-3012`) — the regression net proving the `_marker_max_age_or_default` extraction is behavior-preserving for `code-review`.
- `test_skills.py` — `test_cache_rule_anchors_present` and `test_pinned_cache_clause_matches_live_text`.
- `test_design_decision_files.py` — filename grammar and provenance line for the new decision file.
- `test_hook_alignment.py` — the three `HOOK_TEST_FIXTURE` blocks must still match `require-ready-for-review.sh`'s layout after the reflow.
- (Phase 3) The class-wide origin-arming retrofit to `TestMarkerScriptVerification` is itself load-bearing, above the new opt-in cases: every pre-existing test in the class, not just the ones asserting `match`, must be confirmed to still exercise its original assertion post-retrofit — the two `@pytest.mark.timing` tests especially, since a silent defeat there means a stalled git call could hang instead of degrading with no test failure to catch it.
- (Phase 3) The new opt-in test cases — no-sentinel, sentinel-on-origin-default, sentinel-committed-to-local-HEAD-only, and origin-HEAD-unset — are the ones proving M9/M10's actual, scoped security property (row17's correction): resistant to a branch committing the sentinel to itself, not resistant to local ref fabrication (residual 10).
- (Phase 3) The new timing test for `_lib_verification_cache_sentinel_present`'s capped `git cat-file -e` call.
- (Phase 3) `test_lib.py`'s new `_lib_verification_cache_sentinel_present` cases.
- (Phase 3) `test_design_decision_files.py` and `test_skills.py`'s existing citation-grammar checks re-run clean against the two edited design docs' new cross-links.

Manual check the suite does not cover: run `~/.claude/scripts/marker.sh check verification` on a clean feature worktree, confirm `no-match`; run `write verification`; re-run `check` and confirm `match age_seconds=N`; modify a tracked file and commit it, then confirm `check` returns to `no-match` — an empty commit (`--allow-empty` or one with nothing staged) reproduces the parent's tree and must NOT trip this, so it is not a valid test of invalidation. Confirm the Phase 2 reflow lost no words: diff the reflowed "Scope exceptions" and "Pre-existing failures" paragraphs against `git show HEAD:claude-skills/skills/ready-for-review/SKILL.md`'s versions with whitespace collapsed on both sides, not by eye. (Phase 3) Confirm this repo's own opt-in works end to end: with `.claude/ready-for-review-verification-cache-optin` committed on `main` and a feature branch's `origin/HEAD` pointing at it, `check verification` should be able to `match`. **Corrected per staff-sdet's plan-review finding — the original draft of this step (renaming the local working-tree copy and expecting `no-match`) is confounded:** renaming a tracked file dirties the working tree, which the pre-existing, sentinel-independent `UNCOMMITTED_STATUS` guard already forces to `no-match` regardless of where the sentinel is actually read from — a buggy working-tree read would produce the identical observed result. The property row18 claims (`origin/<default-branch>` governs, not the working tree) instead requires inspecting `origin`'s copy only, never touching the tracked local file: with the tree otherwise clean, run `git cat-file -e origin/main:.claude/ready-for-review-verification-cache-optin` directly to confirm it resolves against `origin`, and treat the automated `test_marker_script.py` opt-in cases above (which vary the sentinel's *location* — origin vs. local-HEAD-only — without ever dirtying the tree) as the actual load-bearing proof of this property, not this manual step.

## Out of scope

- **Step 2's content-type skip carries the same base-move blind spot §44 names for step 3, and this change does not close it.** "Skip when the diff contains no executable code" decides from the diff while step 2 executes the tree, so a docs-only diff rebased onto a base that changed executable code still skips verification of code nobody ran. Closing it means re-deriving the skip's predicate from tree content rather than diff content, which would make the skip fire almost never once a base has moved — a real behavioral tradeoff deserving its own decision. Named here so the plan is not read as having fixed it.
- **`cumulative-review`'s diff-hash key is not touched.** Re-keying it on the tree would make it miss in exactly its motivating case, which is behaviorally equivalent to deleting the kind.
- **The self-attestation residual (G1) stays open**, at parity with all five existing kinds. This change adds a sixth self-attested kind; it does not widen or narrow that property. **The opt-in gate (Phase 3) narrows the residual's blast radius to repos that have declared branch protection; it does not close G1 itself** — an opted-in repo's `verification` marker is exactly as self-attested as before, just gated behind a declared precondition rather than universal. **That precondition itself carries a narrower instance of G1** (residual 10, row17's correction): the gate resists a branch committing the sentinel to itself, not the same session fabricating the local `origin/<default-branch>` ref directly, so the opt-in decision is self-attestable by the identical actor class G1 already accepts, one hop removed.
- **`check`'s main-tree refusal is inherited, not designed here.** `check verification` refuses on the main tree under worktree enforcement because `_resolve_repo_root` calls `_refuse_main_tree_under_enforcement` — identical to `check code-review` today. Whether a read-only subcommand should refuse at all is a pre-existing question for a separate change.
- **No age bound is added to any `write` arm, to `status`, or to a `require-*.sh` commit gate.** §62's reasoning for confining the bound to the advisory `check` path holds unchanged.
- **`docs/design-decisions/cumulative-review-marker-not-recomputed.md` (§50) and `reviewer-responsibility-bounded-to-diff.md` (§34) are read-only here** — both are cited by the new decision file, neither is edited.
- **Binding `check verification`'s match to evidence of an actual passing run (fully closing G1) is out of scope**, per row19 — it needs a machine-readable per-repo declaration of verification commands that does not exist anywhere in this repo yet. The opt-in sentinel is a prerequisite for that future work, not an implementation of it.
- **A `PostToolUse:Bash` hook that observes the check/lint/test commands' own exit status and writes the marker itself** (ciso-reviewer's named lower-privilege alternative to the whole self-attestation class) is out of scope. It would structurally close G1 rather than gating it, but is a materially larger change than this PR's opt-in addendum; named here as the foundation-level answer for a future PR, not implemented now.
- **The Python 3.12→3.14 CI bump (commit `6c2d4b17`) is reverted as a standalone commit, not a mechanism of this plan.** See row20. `pyproject.toml` and the three "CI pins 3.12" plan-doc citations are untouched.
