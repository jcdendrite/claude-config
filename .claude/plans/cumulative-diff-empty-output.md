# Empty-input classification in the marker hash sites

## Context

Stop classifying a legitimately empty diff as a tool failure, and repair the
sibling emptiness guards that cannot fire. A reviewer working in an unrelated
repo reported a suspected bug in the cumulative-diff review cache: once a
branch's PR merges, `pr-diff-against-base.sh` returns empty stdout, and they
claimed hashing that empty output yields the SHA-256-of-empty-string constant
`e3b0c442…b855` — a value any zero-diff branch would produce identically,
colliding as a marker value. A direct probe disconfirmed that claim for the
cumulative-review arm, which guards its input *before* hashing and is pinned by
`test_write_aborts_when_diff_is_empty`. The investigation confirmed two adjacent
real defects instead: a legitimately empty cumulative diff is reported to the
engineer as a tool failure (so `/ready-for-review` on an already-merged branch
runs a vacuous `/code-review` and then aborts with an error naming three causes
that did not occur), and four sibling sites write the same-shaped guard *after*
the hash, where `sha256sum`'s fixed-width output makes it unfireable. The
intended outcome is one change closing both.

## Approach

Fix the empty cumulative diff at the skill layer, and repair the four sibling emptiness guards by classifying the *input* before hashing instead of inspecting the digest afterward. `/ready-for-review` step 3 gains a halt for an empty `pr-diff-against-base.sh` result, so a merged branch never reaches `marker.sh write cumulative-review`; `marker.sh` and `_lib.sh` keep the cumulative arm's two-outcome contract and only get their two misleading message strings corrected. At the four sibling sites, a shared `_lib_staged_diff_state` helper distinguishes *git could not answer* from *nothing is staged*, and each site then does what its own gate already does with that state: abort on unanswerable, decline-without-a-marker on empty, hash unchanged on content. No hashed bytes change anywhere, so no existing marker on any stow consumer's machine is invalidated.

**Root problem.** `sha256sum` turns zero bytes into a full 64-hex digest, so every guard written *after* the hash (`marker.sh:286`, `marker.sh:300`, `_lib.sh:2287`) cannot fire on the emptiness it names, `marker.sh:525`/`:533` carry no guard at all, and the one site that does check the input first (`_lib_cumulative_diff_hash`) reports a legitimately empty branch diff as a tool failure.

**Givens** (fixed conditions this design accepts, each beyond its own reach):

- **G1 — `sha256sum` emits a 64-hex digest for empty input.** Coreutils behavior; the design must work around it rather than change it. `[verified: probe run this session — empty-string digest `e3b0c442…b855` vs. `_lib_cumulative_diff_hash` returning rc=1 for the same input]`

**Mechanisms and material assumptions.**

| # | Mechanism / assumption | Evidence | anchors |
|---|---|---|---|
| 1 | `/ready-for-review` step 3 halts when `pr-diff-against-base.sh` exits 0 with empty stdout, and treats a non-zero exit as a separate "resolve this first" case. Fixing this in prose rather than in code is correct because no hook gates this marker's content — the skill is the only reader. | `[verified: docs/hooks.md:89 — "not gated by a separate `require-*.sh` hook"; claude/.claude/skills/ready-for-review/SKILL.md:65-81]` | anchors: root |
| 2 | The halt paragraph goes **between** the fenced command block (`SKILL.md:71`) and the `CACHE_RULE` start anchor (`SKILL.md:73`), outside every anchor region. | `[verified: claude/.claude/skills/tests/test_skills.py:3241-3243,3276-3286 — exact-set and verbatim-text pins on the anchor's contents]` | anchors: row1 |
| 3 | Over-powered-primitive check on row 1. Two lighter primitives exist and both fail: (a) editing the `CACHE_RULE` region's own text — rejected because the halt must precede the `marker.sh status` read the cached rule performs, and folding two rules into one anchor muddies a pin whose whole purpose is one guarantee per region; (b) doing nothing at the skill layer and letting `marker.sh write cumulative-review` abort — rejected because the abort arrives after `/code-review` has already run a full pass on an empty diff. The heavier alternative, a new `require-*.sh` hook, is rejected outright: nothing gates this marker today, and a gate would be a new privileged execution context for a report-only cache. | `[verified: claude/.claude/scripts/marker.sh:355-372; claude/.claude/skills/ready-for-review/SKILL.md:74,81]` | anchors: row1 |
| 4 | `marker.sh:367` and `marker.sh:576` reword so the empty diff reads as a distinct, legitimate cause rather than a malfunction. The helper's two-outcome contract is unchanged, so both messages must keep enumerating causes without claiming which one fired. | `[engineer-verified: Step 4 answer 1]` | anchors: root |
| 5 | Row 4's `:576` rewrite requires updating a verbatim stderr assertion, and must keep the `cumulative-review: could not verify (` prefix — a sibling test asserts that substring's *absence* on the happy path. | `[verified: claude/.claude/hooks/tests/test_marker_script.py:2074-2078, 2174]` | anchors: row4 |
| 6 | New `_lib_staged_diff_state REPO_ROOT [PATHSPEC...]` in `_lib.sh` prints `content`, `empty`, or `unknown` and always exits 0. Callers branch on the word, so `unknown` can never be silently read as "no changes" the way an `if ! helper` call against a 0/1 return would. It probes with `git diff --cached --quiet -- "$@"` and compares the exit status exactly (`0` = no differences, `1` = differences, anything else = unanswerable, which is also where a `_lib_capped` kill lands). The probe uses `_lib_capped`'s shared 5s default (`_lib.sh:28`), not the 15s network-bound exception `_lib_cumulative_diff_hash` documents for itself (`_lib.sh:507-508`), because this probe is a local git operation like the other 5s-capped sites. This is a new cap on a write-arm path (`marker.sh:286`) that hashes uncapped today: a very large staged diff that would eventually succeed today could newly abort as `unknown` purely because the probe's cap fires before the still-uncapped hash step runs. Accepted because leaving the probe uncapped reintroduces the exact hang failure mode `_lib_capped` exists to prevent; the write-arm `unknown` tests (Verification) exercise this cap firing directly via the stub-`git`-sleep technique. | `[verified: claude/.claude/scripts/marker.sh:190-194 — the identical exact-exit-code idiom, with the same capped-timeout warning; marker.sh:139; claude/.claude/hooks/_lib.sh:27-48]` | anchors: root |
| 7 | The probe's status must be captured as `probe_status=0; _lib_capped git … || probe_status=$?`, not a bare `$?`. `marker.sh` runs under `set -u` only, but `_lib.sh` is also sourced by hooks under `set -euo pipefail`; a bare `probe_status=$?` after a plain nonzero-returning statement trips `errexit` before the assignment ever runs, and the `||` form is correct under both regimes. | `[verified: claude/.claude/scripts/marker.sh:10; claude/.claude/hooks/_lib.sh:1655-1661 — the same "a plain statement's failure trips errexit before the next line reads $?; the `||` form keeps the assignment inside a compound list, which -e does not abort on" reasoning, applied there to `lock_output=$(fn) || state=$?`]` | anchors: row6 |
| 8 | Over-powered-primitive check on row 6. The heavier alternative is a shared capture-then-hash helper (`_lib_staged_diff_hash`) replacing every inline pipeline. Rejected: bash command substitution strips trailing newlines, so it changes the hashed bytes, which forces lockstep edits in `require-code-review.sh` and the plugin-resident `require-skill-review.sh` (triggering `require-plugin-version-bump.sh`) and invalidates every completion marker already on every stow consumer's machine (see Out-of-scope: converting the two `require-*` hooks to a shared hashing helper). Three further alternatives were weighed and set aside: (a) inlining the four-line probe at each of the five sites — rejected under CLAUDE.md's "abstract into a shared helper once two or more share it," for a byte-critical classification; (b) re-running `git diff --cached` and testing the captured output for emptiness before hashing — rejected because it duplicates the recipe it is protecting and opens a TOCTOU window between two full diffs rather than between a cheap probe and one diff; (c) a 3-way exit code (0=`content`, 1=`empty`, 2=`unknown`) matching the house pattern every other multi-outcome helper in `_lib.sh` uses (`_lib_active_plan_hash`, `_lib_cumulative_diff_hash`, `_lib_reviewer_round_state_value`, `_lib_active_bypass_marker_live`) — rejected because it would spread the capped-exit-status-capture subtlety row 7 exists to get right (`probe_status=0; … || probe_status=$?`, safe under both `set -u` and `set -euo pipefail`) to each of the four call sites instead of containing it once inside the helper, buying back only stylistic consistency and not a correctness property the always-exit-0 word contract lacks. | `[verified: claude/.claude/hooks/require-code-review.sh:108; .claude/rules/review-pipeline-dispatch.md — plugin-directory version-bump enforcement]` | anchors: row6 |
| 9 | `git diff --cached --quiet` exiting 1 implies non-empty `git diff --cached` output for mode-only changes, renames, and binary files, but not universally: a `GIT_EXTERNAL_DIFF` or `diff.external` tool that exits 0 without writing to stdout makes the probe report a difference while the piped hash sees zero bytes. In that case `_lib_staged_diff_state` still classifies `content` (quiet rc=1) and takes the unchanged hash-as-today branch, so this is not a regression — the identical collision already exists today, unconditionally, on both `marker.sh`'s write side and the two `require-*` hooks' read side (see Out-of-scope: converting the two `require-*` hooks to a shared hashing helper). The new helper's header comment must scope this claim to what row 9 actually verifies and must not assert the divergence is categorically unreachable. | `[verified: ciso-reviewer empirical probe, GIT_EXTERNAL_DIFF counterexample]` | anchors: row6 |
| 10 | `write code-review` (`marker.sh:285`) and `write skill-review` (`marker.sh:299`): `unknown` → abort exit 2; `empty` → **exit 0, write no marker, print a one-line stderr note**; `content` → today's pipeline, unchanged. The existing post-hash `[ -n "$MARKER_VALUE" ]` guards stay — they still catch a `sha256sum`/`awk` failure, which is the one thing they can fire on. This doesn't reopen either read-side gate: `require-code-review.sh` and `require-skill-review.sh` already exit 0 on today's undifferentiated empty state (see Out-of-scope: not changing `require-code-review.sh` / `require-skill-review.sh`'s empty-diff posture) — though the `unknown` case now diverges between write and read side, named separately in Out-of-scope. | `[verified: claude/.claude/scripts/marker.sh:281-303]` | anchors: root |
| 11 | Row 10 chooses exit 0 over exit 2 for the empty case because exit 2 breaks a chain shape the shape-enforcement hook explicitly allows (see Out-of-scope: not changing `enforce-marker-script-shape.sh`'s chain allowlist). `marker.sh write code-review && git commit --amend -m …` on a message-only amend is the reachable case: today the marker write succeeds and the amend runs, and `require-code-review.sh` early-exits on that same empty staged diff either way (see Out-of-scope: not changing `require-code-review.sh` / `require-skill-review.sh`'s empty-diff posture). Exit 0 preserves that outcome exactly; exit 2 would short-circuit the `&&` and block a commit that is legitimate today. | `[verified: claude/.claude/hooks/enforce-marker-script-shape.sh:619-628; claude/.claude/hooks/require-code-review.sh:89-95]` | anchors: row10 |
| 12 | Row 10's `empty` branch is reached only on a genuinely clean (pathspec-scoped) tree: `_guard_staged_vs_unstaged` runs first and already exits 2 for "nothing staged but unstaged tracked changes exist." The new branch therefore cannot swallow a forgotten `git add`. | `[verified: claude/.claude/scripts/marker.sh:136-146,281,295]` | anchors: row10 |
| 13 | Row 10 does not loosen any gate. `require-code-review.sh` honors an in-chain marker write only after passing its own empty-diff early exit, so the chain path is reached only with staged content — where row 10 writes the marker normally. | `[verified: claude/.claude/hooks/require-code-review.sh:93,103-105]` | anchors: row10 |
| 14 | `status` (`marker.sh:525`, `:533`): compute the digest only when the state is `content`; leave the value empty for `empty` and `unknown`, letting `_status_report_completion_marker` fall through to its existing `absent`/`historical` report. `status` never aborts — it is a report, and that posture is already documented per-arm. The header comment immediately above these lines (`marker.sh:523`) currently claims a capped or killed process "yields an empty value" — false, since `sha256sum` of empty stdin still produces the non-empty `e3b0c442…` digest, which is the exact root-problem defect this plan documents; correct that comment alongside the code it describes. | `[verified: claude/.claude/scripts/marker.sh:168-180,538-542,551-554,562-566]` | anchors: root |
| 15 | Row 14 is observably a no-op except in the case it exists to close. With nothing staged today, the empty-input digest already mismatches any real marker, so `status` already prints `historical`; the only output that changes is a legacy empty-digest marker, which today reports `live` and afterwards does not. The `_status_reconciliation_flag` call is unaffected — it runs only on `live`, which that state never reached. | `[verified: claude/.claude/scripts/marker.sh:170-178,526-528]` | anchors: row14 |
| 16 | Row 14 adds **no** supplementary "could not verify" stderr line for these two labels. The cumulative arm earned one because it is the only line in the report making a network round trip; a local capped `git diff --cached` failing is a different likelihood class, and a second copy of that machinery is the compounding-layers shape CLAUDE.md warns about. | `[verified: claude/.claude/scripts/marker.sh:558-566,570-577]` | anchors: row14 |
| 17 | Row 14 needs no migration for legacy empty-digest markers already on disk. After the fix `status` never reports them live, and the commit gates never read them (see Out-of-scope: not changing `require-code-review.sh` / `require-skill-review.sh`'s empty-diff posture), so they are inert; `clear-stale` covers only active-bypass markers and is correctly left alone. | `[verified: claude/.claude/scripts/marker.sh:503-508,575-577]` | anchors: row14 |
| 18 | `_lib_reviewer_round_state_value` (`_lib.sh:2286`): `unknown` → `return 1` (callers already fail open, as its own header documents); `empty` and `content` both hash exactly as today. This site is the one sibling where the empty value must stay valid — an empty staged diff is a legitimate result here, since `test_require_architect_consult.py`'s `_repo_at_cap` deliberately drives a round-2 state with nothing staged and the round-3 consult gate's counting depends on that value existing (see Out-of-scope: not re-deciding whether an empty staged diff is a legitimate `_lib_reviewer_round_state_value` result) — so it is the one place emptiness is *not* the trigger. The defect here is that a failed or capped `git diff` is currently stored as though it were a real round state, where a later genuinely-empty diff at the same HEAD would `grep -qFx` match it. This adds the probe's cost to both the `content` and `empty` paths, where it only pays off on `unknown`; accepted because skipping it on those two paths would leave this function unable to tell its own `unknown` case from a real empty round, which is the defect this row exists to close. | `[verified: claude/.claude/hooks/_lib.sh:2257-2289; claude/.claude/hooks/require-architect-consult.sh:107]` | anchors: root |
| 19 | `_lib.sh:496` cites `docs/design-decisions.md §42` for the cumulative-hash recipe; §42 is `/code-review`'s Fix-route step and §44 is the correct entry. In scope: the file is scoped by row 18, the comment describes current behavior rather than recording an event, and the wrong pointer sits directly above the function this PR reasons about. | `[verified: claude/.claude/hooks/_lib.sh:491-496; docs/design-decisions.md:725,821]` | anchors: row18 |
| 20 | `test_marker_worktree_keying.py`'s parametrized refusal class depends on today's behavior: its docstring states "skill-review hashes cleanly even with no staged SKILL.md (the empty-diff hash is non-empty)." Row 10 makes that sentence false. The class's own claim survives (from the worktree the write still exits 0, so rc 2 from the main tree is still attributable to the refusal), but the docstring must be corrected and the skill-review parametrization must stage a real `SKILL.md`-matching path so "would otherwise succeed" keeps meaning "writes a marker." Neither test method in this class asserts marker-file existence, so both stay green with or without this repair — it fixes the docstring's accuracy, not any assertion's correctness. | `[verified: claude/.claude/hooks/tests/test_marker_worktree_keying.py:284-329]` | anchors: row10 |
| 21 | `test_write_aborts_when_diff_is_empty` stays green and untouched. Only the message text at `marker.sh:367` changes; the abort contract that test pins (rc 2, no marker) does not. | `[engineer-verified: Step 4 answer 1]` | anchors: row4 |
| 22 | No file under `plugins/` changes, so `require-plugin-version-bump.sh` does not fire. `ready-for-review/SKILL.md` does change, so `/skill-review` is hook-required before the commit. | `[verified: .claude/rules/review-pipeline-dispatch.md]` | anchors: root |

**Alternatives set aside at the top level.** Fixing the cumulative arm inside `marker.sh`/`_lib.sh` — by adding a third outcome to `_lib_cumulative_diff_hash` so the write arm could report "empty" distinctly — was considered and rejected per Step 4 answer 1: it widens a contract two call sites depend on in order to improve a message, when the skill layer can prevent the call from happening at all.

## Critical files

**One `code-writer` dispatch, not split.** The skill-layer fix and the sibling-guard fix both edit `claude/.claude/scripts/marker.sh` and `claude/.claude/hooks/tests/test_marker_script.py`, so their file sets do not partition. Splitting them would mean either sequencing two dispatches over the same two files for no parallelism gain, or running them in parallel in one shared worktree where overlapping edits clobber silently.

**Files to modify:**

- `claude/.claude/skills/ready-for-review/SKILL.md` — insert the halt paragraph between line 71 (end of the fenced `pr-diff-against-base.sh` block) and line 73 (`<!-- CACHE_RULE:… start -->`). Prescribed text:

  > **Empty or unresolved diff — halt before anything below.** If that command exits non-zero, it has already named the reason on stderr; resolve that first, because this step has no diff to review. If it exits 0 and prints nothing, the branch's cumulative diff against its base is empty — there is nothing to review, and the usual cause is that this branch's PR is already merged. Halt `/ready-for-review` and report the empty diff and the likely already-merged PR. The rest of this step, including the `~/.claude/scripts/marker.sh status` read below, assumes a non-empty diff.

  Two constraints on any rewording: the section must not gain the literal `git diff --cached` (pinned by `test_ready_for_review_step3_never_produces_a_staged_diff`), and any `marker.sh` path stays in the `~/.claude/scripts/...` literal form (`_STOWED_PATH_TEMPLATING_RE` forbids the `<config-dir>/scripts/` form).
- `claude/.claude/hooks/_lib.sh` — add `_lib_staged_diff_state` (row 6/7); apply row 18 to `_lib_reviewer_round_state_value`; fix the `§42` → `§44` citation at line 496. **Reuse:** the header comment on the new helper is the canonical home for the failed-vs-empty rule, alongside the existing empty-digest reasoning at lines 450-456 — do not restate it at the call sites. Scope the header comment's claim about `--quiet` vs. `diff` divergence to what row 9 actually verifies; do not assert the divergence is categorically unreachable.
- `claude/.claude/scripts/marker.sh` — rows 4, 10, 14. **Reuse:** call `_lib_staged_diff_state` at all four sites; keep `_guard_staged_vs_unstaged`, the post-hash `[ -n "$MARKER_VALUE" ]` guards, and both existing hashing pipelines byte-for-byte as they are.
- `claude/.claude/hooks/tests/test_marker_script.py` — update the verbatim stderr pin at lines 2074-2078; add the new write-arm and status-arm tests named in Verification.
- `claude/.claude/hooks/tests/test_marker_worktree_keying.py` — row 20.
- `claude/.claude/hooks/tests/test_lib.py` — direct coverage for `_lib_staged_diff_state`'s three outcomes (parallels `TestLibConfigDir`, `TestLibRealpathM` — small self-contained utilities already live here). Not the home for `_lib_reviewer_round_state_value` coverage — see the next bullet.
- `claude/.claude/hooks/tests/test_lib_reviewer_round_state.py` — add the `unknown` → rc 1 case to the existing `TestLibReviewerRoundStateValue` class (line 113), reusing its own `_state_value()` subprocess helper (row 18). This is `_lib_reviewer_round_state_value`'s established home (also the pattern `test_lib_append_line_locked.py` and `test_lib_worktree_collision_guard.py` follow); `test_lib.py` is not.
- `claude/.claude/tests/helpers.py` — `reviewer_round_state_value()`'s docstring (lines 929-930) says the helper returns `""` only when HEAD is unresolvable; after row 18 it also returns `""` on an unanswerable diff. Update the docstring to name both cases.
- `claude/.claude/skills/tests/test_skills.py` — one structural test pinning the halt sentence inside step 3. **Reuse:** `_section_between` with the existing `_READY_FOR_REVIEW_STEP3_HEADING`, following `test_ready_for_review_step3_never_produces_a_staged_diff`'s own shape. No anchor region is added, so `_EXPECTED_SCOPE_ANCHORS`, `_EXPECTED_CACHE_ANCHORS`, `_PINNED_SCOPE_CLAUSES`, and `_PINNED_CACHE_CLAUSES` are all untouched.

**Verification command for the dispatch:** `.venv/bin/python3 claude/.claude/scripts/select-tests.py`, then `.venv/bin/pytest claude/.claude/hooks/tests/test_marker_script.py -m timing -n0`.

## Verification

Run the repo's own scoped selector, per `CLAUDE.md`:

```bash
.venv/bin/python3 claude/.claude/scripts/select-tests.py
```

This diff selects **all three test domains** — `claude/.claude/hooks/tests`, `claude/.claude/scripts/tests`, and `claude/.claude/skills/tests` — plus `test_transcript_analysis*.py`, `test_ticket_reference_discipline.py`, and `test_select_tests.py`. `marker.sh` matches the `SCRIPTS_DIR` domain rule and `_is_scripts_dir_shell_script_change`; `_lib.sh` matches `HOOKS_DIR`, `_is_hooks_dir_shell_script_change`, and `_is_hooks_or_skills_change`; `ready-for-review/SKILL.md` matches `_is_skill_md_change` and `SKILL_FILES_READ_BY_HOOK_TESTS`. Shell linting needs no separate command: `test_shellcheck.py` lives in the selected `HOOKS_TESTS_DIR` and lints every tracked shell script.

Then re-run the timing-marked cumulative tests serially, because `addopts = ["-n", "auto", …]` runs them in parallel by default and the `timing` marker documents them as needing serial isolation:

```bash
.venv/bin/pytest claude/.claude/hooks/tests/test_marker_script.py -m timing -n0
```

**Existing tests that must be updated (not merely re-run):**

- `test_marker_script.py::TestMarkerScriptCumulativeReview::test_status_reports_could_not_verify_when_marker_exists_but_hash_fails` (lines 2074-2078) — the verbatim stderr pin, updated to the new `:576` text, keeping the `cumulative-review: could not verify (` prefix intact so `test_marker_script.py:2174`'s negative assertion still means what it means.
- `test_marker_worktree_keying.py::TestMarkerScriptRefusalCoversEveryWriteArm` (lines 284-329) — docstring correction plus a staged `SKILL.md`-matching path for the `skill-review` parametrization. This corrects the docstring's accuracy only: neither test method in this class asserts marker-file existence, so both stay green with or without the repair.
- `test_marker_script.py::test_write_aborts_when_diff_resolution_fails` (line 1935) asserts only `"cumulative" in result.stderr.lower()`; the new `:367` text keeps that word, so it needs no edit — confirm rather than assume.

**New tests required:**

- `test_marker_script.py` — `write code-review` on a clean tree exits 0, writes no marker, and names the empty staged diff on stderr.
- `test_marker_script.py` — `write skill-review` with a non-`SKILL.md` file staged exits 0 and writes no marker; with a `SKILL.md` staged it still writes a marker (the happy-path regression for row 10).
- `test_marker_script.py` — the row-9 divergence test: `write code-review` with a **mode-only** staged change (`chmod +x`), a staged **rename**, and a staged **binary file** each still write a marker (probe and hash agree, matching all three categories row 9's own prose names). A fourth case drives the known divergence directly: stage a real change with `GIT_EXTERNAL_DIFF` (or `diff.external`) set to a no-op script that exits 0 without writing to stdout, and assert `_lib_staged_diff_state` still returns `content` for it — matching row 9's documented exception, not reclassifying it as `empty`.
- `test_marker_script.py` — `write code-review` and `write skill-review` each abort exit 2, writing no marker, when `_lib_staged_diff_state` reports `unknown`, forced via a stub `git` binary on `PATH` that sleeps for the `diff --cached --quiet` argument shape (reusing `test_code_review_value_computation_times_out_gracefully`'s technique, `test_marker_script.py:1637-1674`, retargeted at the probe instead of the hash pipeline) combined with `_lib_capped`'s cap. This is also the row-6 cap-grounding test: it exercises the probe's cap firing on the write arm. Mark it `@pytest.mark.timing` per this file's own convention for stub-sleep tests (line 1636), so it runs in the serial `-m timing -n0` re-run above.
- `test_marker_script.py` — `status`'s `code-review` line and `skill-review` line each fall through to `absent`/`historical` (not `live`) and the report does not crash, when the same stub-`git` technique drives `_lib_staged_diff_state` to `unknown` for each. Also `@pytest.mark.timing`, for the same reason.
- `test_marker_script.py` — `status` with an empty-digest marker seeded via `write_marker(isolated_home, repo, hashlib.sha256(b"").hexdigest())` (`claude/.claude/tests/helpers.py:754`) and a clean tree reports the `code-review` line as **not** `live`, and the same for `skill-review`. (The seeding writes into the pytest `isolated_home` tmp fixture, not a real config dir, so CLAUDE.md's never-hand-write-a-marker rule is not in play.)
- `test_lib.py` — `_lib_staged_diff_state` returns `content`, `empty`, and `unknown` for the three inputs, with `unknown` covering a non-repo `REPO_ROOT`.
- `test_lib_reviewer_round_state.py::TestLibReviewerRoundStateValue` — add the `unknown` → rc 1 with no stdout case, reusing the class's existing `_state_value()` helper (row 18). The nothing-staged rc 0 case needs no new test: `test_deterministic_across_repeat_calls` (lines 114-122) already covers it.
- `test_skills.py` — step 3 of `ready-for-review/SKILL.md` contains the halt sentence; pin the substring `the branch's cumulative diff against its base is empty`.

**One manual end-to-end check no unit test covers:** in a scratch worktree whose branch is already merged (HEAD at the PR's merge-base), run `/ready-for-review` and confirm it halts at step 3 with the merged-branch message instead of running `/code-review` on an empty diff.

**Pipeline obligations:** `/skill-review` is hook-enforced for the `ready-for-review/SKILL.md` edit, and `/code-review` runs against the staged diff before the commit. No `plugins/**` file changes, so `require-plugin-version-bump.sh` does not fire.

## Out of scope

- **Not changing `pr-diff-against-base.sh`'s exit contract.** It exits 0 with empty stdout when the merge-base equals HEAD, and exits 1 with a stderr line naming the cause on gh/merge-base failure. It is the shared producer for the write arm and the status arm; changing its exit contract would ripple into both and into `_lib_cumulative_diff_hash`'s cap handling, so this plan leaves it as is. `[verified: claude/.claude/scripts/pr-diff-against-base.sh:5,24-38]`
- **Not changing `require-code-review.sh` / `require-skill-review.sh`'s empty-diff posture.** They already exit 0 on the same empty state a write arm would face today. `require-code-review.sh:93` is documented as one half of a named coupling with `deny-invisible-commit-content.sh` ("do not remove either half independently"), so this plan takes their posture as fixed and matches it rather than changing it. `[verified: claude/.claude/hooks/require-code-review.sh:89-95; plugins/skill-management/hooks/require-skill-review.sh:102-118]`
- **The `unknown`/`empty` split not extending to `require-code-review.sh` / `require-skill-review.sh`'s read-side checks.** After row 10, `marker.sh write code-review` / `write skill-review` fail closed on `unknown` (abort, no marker). `require-code-review.sh:93` and `require-skill-review.sh:116` keep a raw `[ -z "$(git … diff --cached 2>/dev/null)" ]` test that is true both when nothing is staged and when the `git diff` invocation itself fails, so they still conflate "can't tell" with "nothing to review" and fail open on both. This is pre-existing and not worsened by this plan — the read side behaved this way before this change too. It needs naming here because the bullet above ("already exit 0 on the same empty state") stops precisely describing reality for the `unknown` case the moment row 10 ships.
- **Not changing `enforce-marker-script-shape.sh`'s chain allowlist.** It explicitly allows `marker.sh write <skill> && git commit` chains. That allowlist is a separate security gate; rows 10 and 11's exit-code choices at the write arm must stay compatible with a shape the gate blesses, without this plan touching the gate itself. `[verified: claude/.claude/hooks/enforce-marker-script-shape.sh:607,619-628]`
- **Not re-deciding whether an empty staged diff is a legitimate `_lib_reviewer_round_state_value` result.** `test_require_architect_consult.py`'s `_repo_at_cap` deliberately drives a round-2 state with nothing staged, and the round-3 consult gate's counting depends on that value existing; re-deciding it belongs to that gate's own design, not here. `[verified: claude/.claude/hooks/tests/test_require_architect_consult.py:42-56 (per Step 3 evidence); claude/.claude/hooks/_lib.sh:2257-2263]`
- **The 0-byte plan-file sites** (`marker.sh:318`, `marker.sh:545`, `claude/.claude/hooks/require-plan-review.sh:111`). Deliberately excluded, not overlooked: those hash a real file's bytes, so the empty-input digest there is the *correct* content hash of a 0-byte file, and their `[ -z "$PLAN_HASH" ]` guards do fire (a missing or unreadable file makes `sha256sum` print nothing). The only residual is a same-session plan-mode target swapping between two distinct 0-byte plan files — a degenerate state, and a different bug shape from the failed-vs-empty conflation this change closes.
- **`_lib_active_plan_hash`** (`_lib.sh:458-484`) — its `[ -n "$active_files" ] || return 0` short-circuits before any hashing, so it never had the defect.
- **A new `docs/design-decisions.md` entry.** The rule this change establishes ("classify the hash input before hashing; an empty input follows the corresponding gate's own posture") gets exactly one canonical home: `_lib_staged_diff_state`'s header comment in `_lib.sh`, where the next contributor adding a hash site will be. `§44` is a dated decision-log entry recording a decision that this change refines rather than reverses, and CLAUDE.md Axis 3 makes it read-only.
- **`docs/hooks.md:89`.** Its claim — a completion marker's content is "a hash of exactly the state that was reviewed" — stays true after this change and is in fact strengthened by it, so it needs no edit.
- **Converting the two `require-*` hooks to a shared hashing helper.** Both currently recompute the staged-diff hash with their own inline `git diff --cached | sha256sum | awk` pipeline (`require-code-review.sh:108`) rather than sharing one. The right long-term shape is one recipe, one place, but converting changes the hashed bytes, invalidates every marker in the field, and requires a plugin version bump since `require-skill-review.sh` lives inside a versioned plugin. Raise it separately if marker-recipe drift ever actually bites.
- **Pre-pull poisoned entries in `.reviewer-round-state.d`.** Rows 15/17 analyze backward compatibility for completion markers only. A line recorded before this fix ships can hold `<head-sha> <empty-digest>` for a diff that was actually unanswerable, and `require-architect-consult.sh:107`'s `grep -qFx` still matches that line against a later, genuinely empty diff at the same HEAD after the fix ships. With `_LIB_REVIEWER_ROUND_STATE_CAP=2` (`_lib.sh:2229`), a poisoned line can occupy one of only two slots for the remaining life of that branch's ledger; the 30-day sweep (`log-reviewer-round.sh:114`) deletes whole files rather than lines and fires only as a side effect of a write to that same file, so an actively-worked branch keeps refreshing the file's mtime and the poisoned line never ages out. Accepted as a bounded residual: `require-architect-consult.sh` is documented as a cost-and-quality interruption, not a security control, so the effect is a suppressed consult nudge for that branch, not a bypass. Deleting that branch's `.reviewer-round-state.d` file resets it, if a clean slate matters.
- **`clear-stale` coverage for completion markers.** Legacy empty-digest markers become inert under row 14 rather than harmful, so no eviction path is needed here.
