# Redacted cross-account `pr-cost` ledger export

## Context

Make the cross-account `pr-cost` export reproducible by giving it a durable home in `transcript-analysis.py`. A prior session hand-rolled a one-off script that read every declared Claude Code account's `pr-cost-ledger.tsv` and emitted a redacted cross-account TSV to feed a cost/telemetry case study comparing per-PR cost before and after a handoff-threshold change. That script lived in `/tmp` and is gone; only its redaction contract survives. The follow-on step — running the actual cross-account/cross-repo analysis with a partner session on another machine — is blocked on this surface existing, and re-deriving a one-off each time both wastes the work and risks a redaction slip. Everything under `claude/` ships to every stow consumer via `./install.sh`, so the user surface and threat model here are "every stow consumer," not one machine.

## Approach

Add a fourth pr-cost-family surface, `transcript-analysis.py pr-cost-export`, that reads every declared account's `pr-cost-ledger.tsv`, collapses each account's rows to the current row per PR, re-redacts the four identity columns into account-namespaced tokens, truncates the two remaining free-precision timestamps to a date, and writes the union to a single operator-named TSV — never to stdout. The subcommand makes no `gh` call and scans no transcript corpus: it is a pure local file transform over ledger files, which is why it reuses `_resolve_cost_roots` / `_pr_cost_ledger_path` / `_parse_pr_cost_ledger_file_text` / `_latest_pr_cost_row` for input resolution and selection, but shares none of `_pr_cost_report`'s network-and-corpus preamble.

**Root problem.** The pr-cost ledger is per-account and its rows carry `host` and `repo` raw at rest (`docs/pr-cost.md:99-103`), so the cross-account, cross-repo cost analysis this data exists to support has no reproducible, publishable input — only a one-off script that has since been deleted. The threat the design must hold is that the artifact produced for that analysis must not carry a private project's host, repo, branch, PR number, or minute-precision activity trace, and must not be copied into the transcript corpus it analyzes.

**Givens** (fixed conditions the design does not reach):

- **G1.** Stored `head_branch` labels are not stable identifiers across rows. `branch_map` is per-run (`redaction.py:220`, "this label is stable only within one run, not across runs") and `_new_pr_cost_row` stores the label rather than the branch, so account A's ledger can hold two rows whose stored `account-1/branch-1` refer to different branches recorded in different runs. Dissolving this needs a durable per-account branch map — a change to the ledger's write path and an ownership decision outside this plan. Recorded also under **Out of scope**.
- **G2.** `host` and `repo` are raw at rest in every existing ledger row, deliberately, because both are part of the row key and must stay comparable across runs (`transcript-analysis.py:8218-8222`). The export redacts on the way out; it cannot retroactively change what is already on disk. Recorded also under **Out of scope**.
- **G3.** `account-K` ordinals, and every `account-K/<kind>-N` token, are comparable only among runs built from the same declared-roots file and the same corpus (`redaction.py:130-134`, and `_assign_root_scoped_redact_label`'s own "stable only within one run, not across runs" at `redaction.py:220-221`). `_redaction_ordinals` (`scope.py:198`) has no cross-run anchor to offer, and adding one would change a contract ~16 other call sites share. Recorded also under **Out of scope**.

**Assumption ledger.**

| # | Assumption | Tag |
|---|---|---|
| 1 | The surface is its own subcommand, not a third `pr-cost` mode and not a standalone script; it reuses `_resolve_cost_roots`, `_pr_cost_ledger_path`, `_parse_pr_cost_ledger_file_text` but keeps its own arg surface and output path. | `[engineer-verified]` |
| 2 | `--out PATH` is required; the export never writes rows to stdout, because stdout inside a Claude Code session is captured into that session's own JSONL transcript — the corpus this tool analyzes. Deliberate divergence from the 36-of-38 stdout convention. | `[engineer-verified]` |
| 3 | The export honors each account's existing `.pr-cost-enabled` file. That file is a **participation flag, not an egress consent artifact**: `docs/pr-cost.md:58` states its rationale as "a write-taking subcommand shipped to every stow user stays consent-gated" — it exists so stow consumers don't accumulate ledger files they never wanted. The export honors it because deleting it is the documented opt-out, and honoring an opt-out is the conservative direction for an artifact designed to leave the machine. | `[engineer-verified]` |
| 4 | The schema gains a leading `account` column carrying the account label, so a consumer partitions without string-parsing token prefixes. | `[engineer-verified]` |
| 5 | Full metric granularity is required in the export — the analysis distinguishes "cost fell" from "the PRs got smaller," which needs `additions`/`deletions`/`changed_files`/`commit_count`/`review_comment_count`. Column-scoping is rejected: there is no principled safe subset, because the six-decimal `*_usd` floats and per-class token counts are higher-entropy per-PR fingerprints than the gh-sourced integers. Joinability against data a third party holds, not entropy, is the whole mechanism, and a `--omit-columns` flag would advertise a safe mode that isn't one. | `[engineer-verified]` |
| 6 | Publishing export rows, or any per-row figure derived from them, to this public repo is prohibited. Aggregates are permitted with explicit human sign-off, which is granted for **aggregate PR counts, token counts, cost, and models used, reported in one bucket**; anything beyond that set returns to the engineer. The general rule in `CLAUDE.md` §"Also redact structural fingerprints and provenance" stays as written — it requires human judgment on aggregates by design, and this row records that judgment being exercised, not the rule being loosened. | `[engineer-verified]` |
| 7 | `pr-cost` reaches the network and the corpus before it ever opens a ledger: `_git_remote_origin_host_and_owner_repo()` → `_gh_auth_preflight_ok` → `_resolve_pinned_gh_repo` → `_gh_discover_merged_prs`, then a per-root `_resolve_project_scope` + `_compute_pr_cost_branch_totals` scan, all at `transcript-analysis.py:8397-8426`, before the `_pr_cost_ledger_path` call at :8429. An export mode inside `pr-cost` inherits every one of those or needs early returns threaded through them. | `[verified: transcript-analysis.py:8397-8429]` |
| 8 | `_assign_root_scoped_redact_label`'s counter is `n = sum(1 for k in redact_map if k[0] == ordinal) + 1` — no `kind` filter — so one map per kind is mandatory, not stylistic. Four kinds here (`host`, `repo`, `pr`, `branch`) means four separate dicts, matching every existing pr-cost call site's `repo_map`/`branch_map` split. | `[verified: redaction.py:226-230; call sites at transcript-analysis.py:7979, 8239, 8277, 8306, 8503]` |
| 9 | `rate_stamp` is already date-only — the parser validates it with `datetime.strptime(row["rate_stamp"], "%Y-%m-%d")`. A "date-only" transform on this column is a no-op and must not be written. | `[verified: transcript-analysis.py:7573-7576]` |
| 10 | `supersedes` has no parser validation of any kind: it is absent from the required-non-empty loop (:7563), the ISO8601 loop (:7568), and from `_PR_COST_FLOAT_COLUMNS` / `_PR_COST_INT_COLUMNS` / `_PR_COST_BOOL_COLUMNS`. It is `""` on an ordinary row and a copied prior `captured_at` on a `--force` row (:8608). **Consequence retagged by M13:** since the export ships only current rows and replaces this column with a derived count, no truncation or validation of `supersedes` is needed — `bool(row["supersedes"])` is safe for any string the parser admits. | `[verified: transcript-analysis.py:7536-7603, 8608]` |
| 11 | `_format_pr_cost_ledger_row` branches only on `_PR_COST_BOOL_COLUMNS` and `_PR_COST_FLOAT_COLUMNS`; `pr_number` falls to a bare `else: cell = str(value)`. The int requirement is the *parser*'s (`_parse_pr_cost_ledger_row_cells:7558`). The real constraint on reuse is that the formatter iterates `_PR_COST_LEDGER_COLUMNS` verbatim, which the export's column tuple is not. | `[verified: transcript-analysis.py:7646-7669 vs :7558]` |
| 12 | `_PR_COST_BOOL_COLUMNS` is a module constant (`:7405`), not a parameter of `_format_pr_cost_ledger_row`. A Python `bool` in a column outside that tuple renders `True`/`False` via `str()`, not `true`/`false`. This is why M13 chooses an integer `correction_count` over a boolean flag — an int renders correctly through the existing `str()` fallback, so M3's single-parameter change stays sufficient. | `[verified: transcript-analysis.py:7405, 7646-7669]` |
| 13 | Rows reaching the export are parser-typed: floats are `float`, ints are `int`, bools are `bool`, `machine` has already matched `^[a-z0-9]{1,8}$`. So the formatter's `f"{value:.6f}"` and `true`/`false` renderings reproduce the ledger's own metric cells byte-for-byte when handed a parsed row. | `[verified: transcript-analysis.py:7582-7601]` |
| 14 | Every stored `captured_at` is written by this tool as `datetime.now(UTC).isoformat(timespec="seconds").replace("+00:00", "Z")` — UTC, `Z`-suffixed, second precision — and hand-editing the ledger is prohibited (`docs/pr-cost.md:60`). Lexicographic ordering therefore equals chronological ordering by closed-world construction, which is what licenses `max()`-based selection. | `[verified: transcript-analysis.py:8603; docs/pr-cost.md:60]` |
| 15 | `_latest_pr_cost_row` selects via `max(matches, key=lambda r: r["captured_at"])` (`:7695`). Python's `max` returns the **first** maximal element on a tie, and `_append_pr_cost_ledger_row` returns `[*existing_rows, new_row]` so rows are parsed oldest-first — therefore an exact same-second tie returns the **older** row, inverting the ledger's last-write-wins correction model. Verified empirically this session: `max()` over two dicts sharing a `captured_at` returned the first. | `[verified: transcript-analysis.py:7695, 7715; executed check this session]` |
| 16 | Fixing that tie-break to last-wins is behavior-identical at both existing call sites: `:8303` uses the result only as an existence check, and `:8562`'s use feeds `supersedes=already["captured_at"]` (`:8608`), where both tied rows carry the same string. | `[verified: transcript-analysis.py:8303, 8562, 8608]` |
| 17 | `_resolve_cost_roots(args, "pr-cost-export")` needs only `args.extra_config_dirs` from the namespace: the `--summary` branch is gated on `subcommand == "cost"` (:523) and `no_redact` is read through `getattr(..., False)` (:553). No `--projects`/`--this-repo` is required. | `[verified: scope.py:488-561]` |
| 18 | Any `_resolve_cost_roots` caller must also join `_SUBCOMMANDS_WITH_OWN_CONFIG_DIR` (`scope.py:480-485`), or `main()` silently reassigns `scope.PROJECTS_DIR` from a top-level `--config-dir` the subcommand never reads. Adding the name automatically adds a case to the parametrized `test_top_level_config_dir_refused_for_subcommands_with_their_own`. | `[verified: transcript-analysis.py:12113-12132; test_transcript_analysis.py:245-282]` |
| 19 | That parametrized test invokes `main()` with a bare subcommand and asserts `SystemExit(2)`. An argparse-level `required=True` on `--out` would make argparse exit 2 during `parse_args` — the assertions still pass, but on a usage error rather than on `main()`'s refusal, silently voiding that row's coverage. Enforcing `--out` in the command body instead keeps the parse succeeding and the refusal genuine. | `[verified: test_transcript_analysis.py:265-282 read against main():12112-12133]` |
| 20 | `_redaction_ordinals` sorts by resolved path, so iterating `sorted(ordinals)` yields ordinal-ascending order independent of which profile is active. `_resolve_cost_roots` returns the active account first, which is a different order. | `[verified: scope.py:198-199 vs scope.py:530-535]` |
| 21 | An account skipped for a missing sentinel must not renumber the remaining accounts: ordinals come from `_redaction_ordinals(roots)` over the full resolved root set, and filtering happens after lookup. **Parity is with `--record`'s skip behavior specifically, not with `--all-accounts` read mode** — read mode returns at `:8442-8447` before ever reaching the sentinel check at `:8449`, so it skips nothing. The export's sentinel check is therefore a new, stricter gate on a read path, not an inherited one. | `[verified: transcript-analysis.py:8406-8414, 8442-8447, 8449-8461]` |
| 22 | `PR_COST_LEDGER_PATH` overrides `config_dir_override` unconditionally (`_pr_cost_ledger_path:7527-7532`), so with it set and more than one root resolved, every account resolves to the same file and one ledger's rows are emitted once per account under different tokens. `pr-cost` refuses this exact combination at :8356-8366; the refusal transfers verbatim. | `[verified: transcript-analysis.py:7522-7533, 8356-8366]` |
| 23 | `_ledger_path_is_git_tracked(path, subcommand)` walks up to the nearest existing ancestor (terminating safely at `/`), shells out to `git -C <ancestor> rev-parse --is-inside-work-tree` with a 10s timeout, and fails closed on a missing `git` binary, a timeout, or an ambiguous exit. Its `subcommand` parameter exists precisely so a second caller can attribute its own diagnostics. It is reusable against a not-yet-created `--out` path. | `[verified: transcript-analysis.py:6699-6751]` |
| 24 | `os`, `stat`, `Sequence` (`from collections.abc import Iterable, Sequence`), and `hashlib` are already imported in `transcript-analysis.py` at lines 16, 21, 28, and 13. No new import and no `requirements-dev.txt` change is needed. `hashlib`'s import carries `# noqa: F401 -- read only via _mod.hashlib from test files`, which becomes false once M14 gives it a production call site — the comment must be removed in the same edit. | `[verified: transcript-analysis.py:13, 16, 21, 28; requirements-dev.txt]` |
| 25 | `os.open(path, O_WRONLY|O_CREAT|O_EXCL, 0o600)` yields exactly `0o600` under umasks 022, 002, 077, and 027, because `0o600` sets no group or other bits for a normal umask to clear. CI is `ubuntu-24.04` only and `README.md:94` states native Windows is unsupported (WSL2 required), so every supported consumer is on a POSIX filesystem. **Residual:** an operator-chosen `--out` on a non-POSIX mount (SMB/CIFS without ACL mapping, exFAT) can silently ignore the mode argument. Accepted and documented, not re-checked at runtime. | `[verified: .github/workflows/tests.yml, README.md:94; empirical umask matrix run by staff-platform-engineer]` |
| 26 | Reading each ledger lock-free is correct, not a shortcut. `_write_pr_cost_ledger_file` writes to a `tempfile.mkstemp` sibling in the same directory (:7728) and publishes via `os.replace` (:7740), which is atomic on POSIX — a reader opening the path by name sees either the complete old or complete new file. `pr-cost` read mode already reads this exact file lock-free at `:8434-8440`, outside the `flock` block that wraps only the `--record` write loop (:8550-8622). | `[verified: transcript-analysis.py:7728, 7740, 8434-8440, 8550-8622]` |
| 27 | The legacy-header `host` backfill is a validated historical fact, not a guess: every row under `_PR_COST_LEDGER_LEGACY_HEADER_LINE` predates GHE support, so `github.com` is the only host that could have produced it. Tokenizing a backfilled `host` identically to a genuinely recorded `github.com` is therefore correct — both cells hold the same true fact and should collapse to one token. | `[verified: .claude/plans/pr-cost-ghe-auth-support.md; transcript-analysis.py:7392]` |
| 28 | No `select-tests.py` rule-table entry is needed. `DOMAIN_RULES` maps anything under `claude/.claude/scripts` → `claude/.claude/scripts/tests` (`select-tests.py:353`), `_is_under(p, DOCS_DIR)` covers both doc files, `_is_py_source_under_claude_or_plugins` and `_is_test_source_change` add the two audit test paths, and `CHANGELOG_MD` maps to no targets. Root `CLAUDE.md` is covered by its own pre-existing `CROSS_DOMAIN_EXCEPTIONS` entry (`ROOT_CLAUDE_MD = "CLAUDE.md"` at `:171`, mapped to `HOOKS_TESTS_DIR` at `:473`) and is not a `GLOBAL_TRIGGER_PATHS` member (`:234-237`), so it forces no full-suite run and its target is already selected by the two doc files. | `[verified: select-tests.py:171, 234-237, 351-360, 450-479]` |
| 29 | No test enforces that every subcommand appears in `docs/transcript-analysis.md`, and no test enumerates subparsers — `test_select_tests.py:781-785` asserts the opposite, that no test reads that doc by path. The doc update is a discipline obligation, not a CI-enforced one. | `[verified: test_select_tests.py:781-785]` |
| 30 | No new sentinel means `install.sh` and `claude/.claude/hooks/tests/test_install_sh_sentinel_inventory.py` are untouched. | `[verified: row 3 plus the sentinel inventory's scope]` |
| 31 | Reading the export back with the `Read` tool, or `cat`-ing it in-session, copies its rows into the session transcript — the identical leak row 2 exists to close. Load-bearing for M14's stderr line and the doc guidance. | `[unverified]` — an inference from row 2's own mechanism, not a measured capture path. |
| 32 | POSIX `open(2)` guarantees `O_CREAT|O_EXCL` fails `EEXIST` when the final path component is a symlink, *regardless of the link's target* — but only when the symlink itself is the path passed to `open()`. Opening a `.resolve()`-derived path throws that protection away: for a **dangling** symlink at `--out`, `Path.exists()` returns `False` (it follows the link) so an early existence check does not fire, and `os.open` on the resolved target either creates a file wherever the link pointed (exit 0, no diagnostic) or raises `FileNotFoundError`, which is not `FileExistsError`. Verified empirically by `staff-backend-engineer` this session. | `[verified: POSIX open(2); empirical check this session]` |
| 33 | `_parse_pr_cost_ledger_file_text` computes `is_legacy_header` internally (`:7629-7633`) purely to decide whether to prepend the host default, then discards it — its only return is `list[dict]`, and a parsed legacy row is structurally identical to a current-header one. There is therefore **no** path to a per-account legacy-header count through the reused parser, and the plan must supply one explicitly rather than leaving an implementer to widen that function's signature. | `[verified: transcript-analysis.py:7606-7643]` |
| 34 | The first data row of an account's ledger never changes or moves: the file is append-only through the tool (`docs/pr-cost.md:60`), `_append_pr_cost_ledger_row` only appends (`:7715`), and `_parse_pr_cost_ledger_file_text` walks `lines[1:]` in file order with no sort. Its `(captured_at, machine)` pair is therefore a stable per-account identity, and `captured_at` is second-precision over a multi-year span — high enough entropy that a digest over the sorted set is not enumerable the way a digest over filesystem paths is. | `[verified: transcript-analysis.py:7606-7643, 7715; docs/pr-cost.md:60]` |

**Mechanisms.**

**M1 — A new `pr-cost-export` subcommand rather than a `pr-cost` flag.** `anchors: row1` (engineer's call), grounded in `row7`. The over-powered-primitive check, enumerated for a reader who was not in the session:

- *Lighter primitive A — a `--export PATH` flag on `pr-cost`.* Set aside because `_pr_cost_report` requires a git origin remote, `gh` auth, a live network discovery call, and a full per-root transcript scan before the first ledger read (`row7`). A pure local file transform would inherit a network dependency, and suppressing it means early returns threaded through a 300-line function that already carries read mode, `--record`, two multi-root refusals, and a per-branch enrichment loop.
- *Lighter primitive B — parse `pr-cost --all-accounts` read-mode stdout.* Set aside because `_print_pr_cost_ledger_rows` emits 6 of the 39 columns (`Repo/PR/Machine/Status/Join/CapturedAt`, :8275) and every metric column — the analysis payload — is absent. It also lands on stdout, which is `row2`'s exact problem.
- *Lighter primitive C — no tool at all: concatenate the ledgers and post-process with shell/pandas.* Set aside because `host` and `repo` are raw at rest (`G2`), so an unredacted concatenation *is* the leak, and reproducing `_redaction_ordinals` plus `_assign_root_scoped_redact_label`'s per-account counters outside Python is not a one-liner. This is what the deleted one-off was, and its loss is why this plan exists.

**M2 — `--out PATH` required, enforced in the command body (exit 2), no stdout path at all.** `anchors: row2`, enforcement site per `row19`. The mechanism is *more restrictive* than the convention, so the enumeration is of the lighter, more permissive shapes:

- *Lighter primitive A — stdout by default with an optional `--out`, the `user-input` (:11161) and `judgment-pair` (:11404) shape.* Set aside because the default invocation is then the leaking one: a bare `pr-cost-export` inside a session copies every exported row into that session's transcript.
- *Lighter primitive B — stdout behind an explicit confirmation flag.* Set aside as strictly more surface for the same outcome, and against the module's own precedent: `_pr_cost_report`'s docstring states "There is deliberately no --no-redact escape hatch for this subcommand, unlike cost/subagents" (:8337-8338). The file already treats stdout as a channel needing protection elsewhere — `audit-routing-samples` routes its scope header to stderr because "its stdout is a JSON (or curation-markdown) data stream a header line would corrupt" (`scope.py:461-467`).

Argparse `required=True` is rejected on two grounds: `row19`'s coverage loss, and that argparse's "the following arguments are required: --out" cannot carry the *why*, which is the non-obvious part.

**M3 — Reuse `_format_pr_cost_ledger_row` by adding a keyword-only `columns` parameter defaulting to `_PR_COST_LEDGER_COLUMNS`.** `anchors: row11, row13`. The four properties the export needs — `true`/`false` bools, `:.6f` floats, `str()` fallback, and the refuse-on-embedded-tab/newline guard — are exactly the four that function already owns. Re-implementing them in a second function duplicates a format contract across two sites, which CLAUDE.md's single-source-of-truth rule exists to prevent, and drifts the export's metric cells from the ledger's own the first time either changes. The durable-write path passes no `columns` argument, so `_write_pr_cost_ledger_file` is unchanged by construction. The lighter alternative — a shared `_assert_pr_cost_cell_safe(col, cell)` helper called from two independent join loops — was set aside because it shares only one of the four properties and leaves the other three duplicated. Per `row12`, the parameter is *not* extended to `_PR_COST_BOOL_COLUMNS`: the export's own new columns are rendered as strings/ints by the caller instead.

**M4 — Four separate redact maps, one per kind, and `str(row["pr_number"])` before tokenizing.** `anchors: row8`. The stringify satisfies `_assign_root_scoped_redact_label`'s `value: str` parameter contract (`redaction.py:198`) — it is *not* fixing a mixed-type-key runtime hazard, since under this design `pr_map` holds only `pr_number` keys. The same `str()` coercion is what `_format_pr_cost_ledger_row` already applies to this column.

**M5 — Re-tokenize `head_branch` even though it arrives already scrubbed, and rename the exported column to `head_branch_label`.** `anchors: G1, G3`. The stored label was assigned under whichever run's ordinal scheme recorded it, so passing it through puts two disagreeing `account-K` numberings in one row and invites a mis-partition by account. Re-tokenizing an already-opaque value discloses nothing. This contradicts `_print_pr_cost_ledger_rows`' own "would double-redact it" comment (:8270) — correct for its single-account print path, not for a cross-account export. Because `G1` makes the value a per-run label rather than a branch identity, it is not a valid join or grouping key: two rows recorded in different runs can collapse to one token, and one branch can appear under two. The export therefore renames the column so the trap is visible on sight rather than only in a doc a consumer may never open. The ledger's own on-disk column name is untouched.

**M6 — Iterate accounts in ordinal order (`sorted(ordinals)`), not `_resolve_cost_roots` order.** `anchors: row20`. Two exports taken from the same declared-roots file under different active profiles then produce byte-identical row order, which makes the artifact diffable.

**M7 — Create the output with `os.open(path, O_WRONLY|O_CREAT|O_EXCL, 0o600)` on a path whose final component is *not* symlink-resolved, catching `OSError`.** `anchors: row2, row25, row32, G2`.

Per `row32`, the path handed to `os.open` must be `resolved_out.parent.resolve() / resolved_out.name` — the parent chain resolved (which the git-tree check needs), the final component left as the operator named it — so `O_EXCL`'s own POSIX symlink refusal actually fires. Catch `OSError` rather than `FileExistsError` alone: a dangling symlink whose resolved parent is missing raises `FileNotFoundError`, and letting that escape produces exactly the traceback the symlink coverage forbids. The early fast-fail check uses `os.path.lexists()`, not `Path.exists()`, so it tests the directory entry rather than following the link and agrees with what `O_EXCL` will do. One primitive covers three needs: `O_EXCL` refuses to clobber an existing file (a `--out` pointed at a `pr-cost-ledger.tsv` would otherwise destroy an append-only, irreplaceable record whose source transcripts have aged out), it does so without a TOCTOU window, and `0o600` at creation avoids the umask-dependent world-readable window a `write_text`-then-`chmod` sequence leaves open. The heavier alternative — the ledger's own `mkstemp` + read-back + `os.replace` pattern (:7718-7744) — was set aside because `os.replace` clobbers, which is the behavior being refused. No `O_EXCL` precedent exists elsewhere in this file, so the collision message is authored net-new rather than reused; it must be caught and converted, never allowed to propagate as a traceback.

**M8 — Refuse an `--out` path inside a git working tree, via `_ledger_path_is_git_tracked(resolved_out, "pr-cost-export")`.** `anchors: row23`. The natural cwd for these sessions is a repo worktree, and this repo is public; the helper already exists, already fails closed, and already takes a subcommand label for exactly this reuse. Distinct failure mode from M7's (accidental commit vs. destroying the ledger), not a second layer over one gap. This is the tool's entire enforceable contribution to `row6`'s publication boundary — see M15.

**M9 — Refuse `PR_COST_LEDGER_PATH` when more than one root resolves; permit it at one root.** `anchors: row22`. Mirrors `pr-cost`'s own refusal, and keeps the env var usable as the test seam existing assertions rely on.

**M10 — Fail loud (exit 1) on a malformed ledger, naming `account-K` and never a path or a raw cell value.** `anchors: row21`. **Correction to a prior draft's citation:** `transcript-analysis.py:8434-8440` prints exactly `f"pr-cost: {exc}"` — it names neither a path nor an account. The `account-{ordinal}` discipline is demonstrated only at `:8452-8457` and `:8465-8469`, which cover a different failure mode (missing sentinel). The export's handler is therefore **net-new code**, composing `f"pr-cost-export: account-{ordinal}: {exc}"`, not a pattern copied from the malformed-ledger site. The no-raw-value discipline follows `_parse_pr_cost_ledger_row_cells`' own host/repo errors, which deliberately omit the offending value.

**M11 — Code lands in `transcript-analysis.py` beside the pr-cost block, not in a new `transcript_analysis/` module.** `anchors: row1`. Every helper it reuses is a private module-level function in the shim; `cmd_pr_cost` is itself still shim-resident (:8312) while `cmd_cost` has migrated, so landing beside the sibling matches the pr-cost family's actual state. `docs/transcript-analysis-architecture.md:6-12` frames the migration as deliberately leafward-first and incremental, and a new package module would require updating that doc's module list and one-directional import rule.

**M12 — No `--all-accounts` flag; scanning every resolved root is the default.** `anchors: row7, M1`. `pr-cost`'s multi-root refusal (:8340-8355) is justified by durable per-account writes and by read mode conflating two accounts' data in one listing. Neither applies: the export writes one file, every identity column is tokenized, and the `account` column makes the partition explicit rather than conflated.

**M13 — Collapse to one current row per `(host, repo, pr_number, machine)`, and replace `supersedes` with a derived `correction_count`.** `anchors: row10, row12, row14, row15`. The ledger is append-only, so a `--force` correction appends a row carrying `supersedes` = the prior row's `captured_at` rather than replacing it; passing every historical row through hands a consumer an artifact where `groupby([...]).sum()` silently double-counts every corrected PR.

The append-only rationale does not transfer to this artifact, and `docs/pr-cost.md:72` says why in its own words: append-only exists because "this ledger is the sole surviving record once transcripts age out." The export is not the sole record — every source ledger stays byte-identical on its own machine and the export is regenerable. What *is* at stake is grain integrity of a single-purpose extract, and that is protected by fixing the grain, not by mirroring the source's retention policy.

- *Alternative — keep every row and document it.* Not available, rather than merely weaker: making it workable requires `supersedes` and `captured_at` at full precision so a consumer can order same-day corrections, which is exactly the minute-precision trace this plan's root problem excludes.
- *Alternative — collapse plus an `is_current` column.* Set aside because the shipped history is still unusable — superseded rows' timestamps are still truncated, so they can only be filtered out, which the collapse already did — while roughly doubling the row-granularity metric tuples in an artifact whose handling constraint is "fewer joinable rows is better."

Collapse runs **per account, on raw untokenized rows, before redaction and before truncation**, by calling `_latest_pr_cost_row` once per distinct key in that account's parsed rows. Call it rather than re-deriving a `max()` inline: at this scale the cost is nothing and it keeps one definition of "latest."

Both halves of that ordering are mechanically required, not tidiness. **Before redaction:** `_latest_pr_cost_row`'s signature is typed (`host: str, repo: str, pr_number: int`) and it compares `r["pr_number"] == pr_number` as an `int` (`:7690`); redacting first would replace those cells with opaque token *strings* (M4's `str()` step), breaking the helper's contract and forcing a reimplementation — the duplication M13's own "one definition of latest" rationale exists to avoid. **Before truncation:** truncating `captured_at` to a date first would convert today's rare exact-second tie into a routine same-calendar-day collision for any PR corrected twice in one day, making M16's tie-break carry correctness it was never meant to carry.

After the collapse a shipped row's `supersedes` points at a row the file no longer contains — a dangling pointer inviting a join it cannot support, whose truncated form means nothing. Replace it with `correction_count`, an integer computed from the **full pre-collapse row group** for that key (`len(group) - 1`) — the dependency is row-membership completeness, not timestamp precision. An integer rather than a boolean per `row12`: a `bool` would render `True`/`False` through the formatter's `str()` fallback, since `_PR_COST_BOOL_COLUMNS` is a module constant M3's parameter does not reach. Renaming rather than retyping in place is deliberate — a same-named column with a changed type is worse than a renamed one.

**M14 — One `#`-prefixed provenance line above the TSV header.** `anchors: row6, row31, row33, row34, G3`. It makes four otherwise-stdout-only or undetectable facts travel with the artifact: whether two exports' `account-K` ordinals are comparable at all (`G3`), that a fully-skipped run produced a header-only file, the legacy-header provenance (`row27`), and the publication prohibition.

**Format is pinned, because the corpus digest is meant to be compared programmatically rather than read.** A single space-delimited `key=value` token sequence, not prose:

`# pr-cost-export DO-NOT-PUBLISH-no-tooling-enforces-this exported_at=<ISO8601 UTC> declared=<N> opted_in=<N> skipped_no_sentinel=<N> legacy_header_accounts=<N> corpus=<12 hex chars> corpus_override=<0|1>`

`corpus_override` was added during the testability-fixes pass: `1` flags a run against a `TRANSCRIPT_CONFIG_DIRS_FILE`-overridden (synthetic) root set rather than this machine's real declared accounts — see `docs/transcript-analysis.md`'s "Testing against a synthetic corpus" section. `0` and `1` are both real, expected values, not an error state.

**The digest is over corpus-derived values, not filesystem paths.** `hashlib.sha256("\n".join(sorted(<per-account identities>)).encode()).hexdigest()[:12]`, mirroring `_corpus_fingerprint`'s own construction (`redaction.py:167-176`) — naming the primitive explicitly because Python's built-in `hash()` is per-process salted, which would emit a different value on every real invocation while passing an in-process test vacuously. Each account contributes the `(captured_at, machine)` pair of its ledger's **first data row**, which `row34` establishes never changes or moves under append-only writes. This identifies the participating account set exactly as well as the paths would, discloses nothing the export does not already contain, and — unlike a digest over a small, structurally guessable set of config-dir paths — is not enumerable, since second-precision timestamps over a multi-year span carry real entropy. Hashing the resolved root paths was the first design and was rejected on that ground.

The digest certifies **ordinal/account comparability only**. It is orthogonal to schema drift, which the column-name-driven join handles independently — two exports of the same accounts across a ledger-schema change share a digest despite differing columns. Like `_corpus_fingerprint`, it is a same-corpus indicator, not a security boundary, and the docs say so.

`scope._DO_NOT_PUBLISH_BANNER` (`scope.py:471-473`) is **not** reused: its text names "real project names and session IDs," false for this artifact, and it is gated on `--no-redact` and printed to stderr rather than written into a file. Its 30+ existing occurrences all sit behind a genuine runtime gate, so the export's marker states inline that nothing enforces it — otherwise a reader who has met the phrase elsewhere in this file may take its presence as evidence the artifact was already vetted, and skip the manual review M15 depends on.

`legacy_header_accounts` needs a source the reused parser cannot give: `row33` establishes `_parse_pr_cost_ledger_file_text` discards `is_legacy_header`. Rather than widen that function's signature — which would drag in its two other call sites (`:7735`, `:8437`) under the structural-siblings rule — the export peeks at `lines[0]` of each account's raw text and compares it against `_PR_COST_LEDGER_LEGACY_HEADER_LINE` before handing the text to the parser. One comparison, no shared-contract change.

A sidecar file was set aside as the heavier of two equally effective options — it would need its own `O_EXCL`/`0o600` treatment for no gain over one line in the file already being written. Nothing in this codebase reads the export back, so a leading comment line creates no internal parsing-contract conflict; it does create one for the artifact's actual consumer, which the docs address.

**M15 — The publication boundary is documented, not enforced beyond M8, and the plan says so.** `anchors: row5, row6`. M8 makes it impossible for the tool to write the export into any git working tree — genuine enforcement. Everything after the file exists is outside the tool's reach: `docs/pr-cost.md:105-112` already documents this helper's blind spots (cloud-sync folders, bare-repo dotfile managers) for the ledger; a `cp` into the repo is a third, and a hand-written case-study table of row figures is a fourth no file-path check could ever see.

Extending `deny-private-project-refs.sh` with a detector was considered and rejected: a regex on `account-[0-9]+/(repo|pr|branch|host)-[0-9]+` collides with the documentation that explains the format — this plan's own test coverage asserts `account-1/repo-1` appears, and the CHANGELOG and docs carry examples — so it would block this very PR and need an allowlist, which is the compounding-defensive-layer shape CLAUDE.md warns against. A header-line detector fires only on a verbatim TSV paste, the accident least likely to happen and most obvious on review. Neither earns its place.

The rule therefore lives in prose, split to avoid restatement: the **prohibition** in root `CLAUDE.md`'s "Also redact structural fingerprints and provenance" subsection (which already covers it generically — what is missing is the named instance, and it earns one because the export's cells *are* tokenized, so the artifact looks redacted and an agent writing a case study will not spontaneously classify tokenized figures as private-corpus-derived); the **mechanism and safe-aggregate guidance** in `docs/pr-cost.md`, deferring to `CLAUDE.md` for the prohibition itself.

**M16 — Fix `_latest_pr_cost_row`'s tie-break to last-wins.** `anchors: row15, row16`. In scope under Axis 1's "required to make the ticket's change correct" carve-out, where *correct* means **M13's own correctness claim is honest** — not that anything crashes without it. M13's entire value to a consumer is "one row per key, correctly current"; shipping it over a selection that resolves ties backwards makes that guarantee false in exactly the case a corrected PR creates. The asymmetry that justifies touching a live write path: on `--record` a tie is a one-off rare event, but once a tie exists in a ledger it resolves wrong on *every* future export run, indefinitely.

Break ties by append position — `max(enumerate(matches), key=lambda p: (p[1]["captured_at"], p[0]))[1]` — in the shared helper rather than duplicating a corrected selection beside the existing one, per CLAUDE.md's structural-siblings rule. Behavior-identical at both existing call sites (`row16`), independently verified by two reviewers, so it carries no migration risk, and it also corrects the live `--force` path.

**Open decision left to the engineer:** none. Two micro-choices were settled by default: the `account` cell emits the full label `account-1` rather than the bare integer, because the label is the prefix of every token in its own row so a consumer can check row-internal consistency by string equality; and `correction_count` counts prior captures, so an uncorrected row carries `0`.

## Critical files

**Single `code-writer` dispatch.** The doc prose describes the same redaction and grain contract the code implements, so a code/docs split would restate identical background in both prompts and risks the two resolving `G1`'s caveat differently — `plan-it`'s own "do not split when the same shared-state background would have to be restated" case.

**Modify — `claude/.claude/scripts/transcript-analysis.py`**

- `_latest_pr_cost_row` (:7672-7695) — apply M16's tie-break fix. One line.
- `_format_pr_cost_ledger_row` (:7646) — add keyword-only `columns: Sequence[str] = _PR_COST_LEDGER_COLUMNS`, iterate it instead of the module constant. Docstring gains one line: the tab/newline guard and the bool/float renderings are shared with the export's wider column tuple.
- New, immediately after the pr-cost block (after :8640): `_PR_COST_EXPORT_COLUMNS` (`_PR_COST_LEDGER_COLUMNS` with a leading `account`, `supersedes` replaced by `correction_count`, and `head_branch` renamed `head_branch_label`), `_PR_COST_EXPORT_HEADER_LINE`, `_pr_cost_export_date_only(value)`, `_collapse_pr_cost_rows_to_current(rows)`, `_redact_pr_cost_row_for_export(row, ordinal, correction_count, host_map, repo_map, pr_map, branch_map)`, `_pr_cost_export_rows(roots)`, `_pr_cost_export_provenance_line(...)`, `cmd_pr_cost_export(args)`.
- `_pr_cost_export_rows(roots)` returns a **fully materialized** `(rows, declared, opted_in, skipped_no_sentinel, legacy_header_accounts)` tuple — never a generator, and it performs no filesystem writes. This is load-bearing: it is what guarantees a mid-loop malformed ledger exits before `--out` is ever created, so no partial file can exist. `legacy_header_accounts` comes from M14's own `lines[0]` peek, not from the reused parser (`row33`).
- `_redact_pr_cost_row_for_export` returns a dict keyed by **`_PR_COST_EXPORT_COLUMNS`' own names** (`head_branch_label`, `correction_count`), not the ledger's original field names — `_format_pr_cost_ledger_row` does `value = row[col]` over the tuple it is passed (`:7656-7657`), so a row still keyed `head_branch`/`supersedes` raises `KeyError`. The rename is in the row dict, not only the header.
- Remove `hashlib`'s `# noqa: F401` comment (`:13`), which stops being true once M14 gives it a production call site (`row24`).
- `build_parser()` — register `p_pr_cost_export = sub.add_parser("pr-cost-export", ...)` immediately after `p_pr_cost.set_defaults` (:11828), with `--out PATH` (help text states it is required and why) and the repeatable `--config-dir` / `dest="extra_config_dirs"`. No `_add_project_scope_args` call.
- Command body order, load-bearing: refuse missing `--out` (exit 2) → resolve the out path → **early `os.path.lexists()` check, refuse fast** → `_ledger_path_is_git_tracked` refusal (on the fully-resolved path) → `_resolve_cost_roots` → `PR_COST_LEDGER_PATH` multi-root refusal → `_redaction_ordinals` → `_pr_cost_export_rows` (per-account loop in `sorted(ordinals)` order: **collapse, redact, truncate, format**; per-account stderr skip notices) → **one** `os.open(<parent-resolved, final-component-unresolved path>, O_EXCL, 0o600)` and **one** write of the provenance line plus header plus rows → stdout summary line plus the stderr inspect-outside-a-session reminder.
- Two path forms are used deliberately and must not be collapsed into one: the fully-resolved path for the `lexists` and git-tree checks, and `resolved_out.parent.resolve() / resolved_out.name` for the terminal open (`M7`, `row32`).
- Refusal messages follow this file's `"<subcommand>: <reason> -- <what to do next>"` convention and exit 2 for every path-shape refusal, exit 1 for the malformed ledger. Every `--out`-touching diagnostic echoes at most the operator's own literal `args.out` string, never a `.resolve()`-derived absolute path — the same discipline `_config_dir.py:64-133` applies ("the path identifies an engagement"). The terminal open catches `OSError`, never letting `FileExistsError` or `FileNotFoundError` propagate.
- Reuse, do not reimplement: `_resolve_cost_roots`, `_redaction_ordinals`, `_pr_cost_ledger_path(config_dir_override=...)`, `_parse_pr_cost_ledger_file_text`, `_latest_pr_cost_row`, `_assign_root_scoped_redact_label`, `_ledger_path_is_git_tracked`, `_format_pr_cost_ledger_row`, `_PrCostLedgerParseError`.

**Modify — `claude/.claude/scripts/transcript_analysis/scope.py`**

- Add `"pr-cost-export"` to `_SUBCOMMANDS_WITH_OWN_CONFIG_DIR` (:480-485). Required by `row18`; this also grows the parametrized refusal test by one case for free.

**Modify — `claude/.claude/scripts/tests/test_transcript_analysis.py`**

New classes after the pr-cost block (ends :22267), in the file's dominant style — `_mod.cmd_pr_cost_export(args)` against a `type("A", (), {...})()` namespace, asserting on `tmp_path` file content and `capsys`. Reuse `_two_declared_roots` (:17196) and `_sample_pr_cost_row` (:19620); add `_pr_cost_export_args(*, out, extra_config_dirs=None)` beside `_pr_cost_args` (:19467). Extract `_legacy_row_line` (:20538) from its current private position on `TestPrCostLedgerLegacyHostColumnMigration` to module scope before Group 7 needs a second copy.

**Assertion shape.** Every redaction-relevant assertion uses absence *plus* positive-token presence — absence alone passes vacuously on empty output. The correct model is `test_no_raw_branch_name_in_existing_rows_table_or_uncaptured_listing` (:21762-21783) or `test_no_raw_branch_name_in_already_captured_refusal` (:21787-21814). **Not** :21855-21883, which is presence-only.

**All five refusal tests monkeypatch `subprocess.run`** (reusing `_fake_pr_cost_subprocess_run` or an equivalently narrow fake for the `rev-parse` call), with `git_tracked=True` for the git-tree case and the default for the others — matching the file's existing discipline rather than depending on `tmp_path`'s ambient location relative to a git checkout. The git-tree check runs second in the command body, so *every* refusal test depends on it resolving.

Coverage:

1. **Redaction — host, repo, branch.** Seed two accounts with a distinctive raw `repo`/`host` and a distinctive stored branch label; assert none appears in the written file or in `captured.out + captured.err`, *and* that `account-1/repo-1` and `account-2/repo-1` both appear.
2. **Redaction — PR number.** Seed a distinctive `pr_number` (not the `42` fixture default, which recurs everywhere and cannot distinguish redacted from coincidentally rare); assert it is absent and an `account-1/pr-1`-shaped token is present. `_print_pr_cost_ledger_rows` prints `pr_number` raw 150 lines away, so this is the closest wrong precedent an implementer could follow.
3. **Redaction — the `head_branch` collision M5 exists for.** Two accounts whose stored `head_branch` is the *identical* literal `"account-1/branch-1"` (also `_sample_pr_cost_row`'s own default at :19626) must export to *different* tokens, proving re-tokenization ran rather than being skipped because the input already looked redacted.
4. **Schema.** Header equals `_PR_COST_EXPORT_HEADER_LINE`, with `account` leading, `head_branch_label` in place of `head_branch`, and `correction_count` in place of `supersedes`. Every metric cell is byte-identical to `_format_pr_cost_ledger_row`'s rendering of the same source row — scoped explicitly to the 35 non-identity columns, since the four tokenized columns are *supposed* to differ.
5. **Grain (M13).** Two same-key rows where the later `captured_at` wins and the superseded row is absent. A second sub-case with the two `captured_at` values differing **only by seconds within one calendar day** — its own docstring must state the invariant it guards, that collapse runs on full-precision timestamps *before* truncation, so it is not mistaken for a duplicate of the first and deleted. Plus: `correction_count` is `1` on a row with one prior capture and `0` otherwise; two rows differing only by `machine` both survive, since `machine` is part of the key.
6. **Tie-break (M16).** The fixture order is the whole test, so name it: two rows sharing `(host, repo, pr_number, machine)` **and an identical `captured_at`**, differing in one other observable field (e.g. `additions`), written to the ledger in a stated order — row-A first, row-B second. Assert the exported row carries **row-B's** value *and* that row-A's value is absent. Written the other way round (expected winner first) the test passes against the unfixed `max()` and provides zero coverage of the bug it exists to pin.
7. **Collapse × tie-break (M13 × M16).** Three rows under one key: row-A strictly older, then row-B and row-C sharing one identical later `captured_at`, appended in that order. Assert row-C survives and both row-A's and row-B's markers are absent — a fix that special-cases exactly two elements passes item 6 and fails here.
8. **Timestamps.** `merged_at`/`captured_at` date-only; `rate_stamp` unchanged (`row9`).
9. **Ordinals and order.** Ordinals match `_redaction_ordinals` over the same roots; an account skipped for a missing sentinel does not renumber the others (`row21`). **The ordering case runs against `cmd_pr_cost_export` itself** — export once with account A active, once with account B active (swapping which config dir is `CLAUDE_CONFIG_DIR`/`PROJECTS_DIR`), and assert the two files' *row order* is identical, e.g. by comparing the sequence of `account` cells. Mirroring `test_same_two_roots_different_scan_order_yield_identical_ordinals` (:17954) instead would only re-test an unchanged helper and would still pass against a bug that reverted M6.
10. **Refusals.** Missing `--out` exits 2; an existing `--out` is refused fast, before any ledger read, with its original bytes intact and a message naming the path and the fix; an `--out` inside a git working tree is refused; `PR_COST_LEDGER_PATH` with two roots exits 2; a malformed ledger exits 1 naming `account-K`, with no filesystem path and no raw cell value in the message, **and with `--out` asserted not to exist afterwards** (the no-partial-file property rides on this same fixture rather than a duplicate one). Every message is asserted to contain none of a distinctively-named tmp cwd's path components.
11. **Symlinks (`row32`).** Three distinct cases, since they exercise different layers: a **live** symlink at `--out`'s final component (caught by both the early check and `O_EXCL`); a **dangling** symlink at `--out`'s final component (skips the early check, so this is the one that proves the terminal open received an unresolved final component) — both refuse cleanly, no traceback, no leaked resolved path; and `--out` whose *parent* is a symlink into a git working tree, still triggering the git-tree refusal.
12. **`O_EXCL` backstop, independent of the early check.** Make the target path come into existence only *after* the early check passes (a monkeypatch or fixture creating it between the two points) and assert the terminal open still refuses. Without this, a later refactor that drops the atomic open in favour of the early check alone passes every other test here.
13. **Opt-in.** An account without `.pr-cost-enabled` contributes zero rows and is counted in the summary line and the provenance line.
14. **Legacy header.** A ledger written under `_PR_COST_LEDGER_LEGACY_HEADER_LINE` exports correctly and the provenance line records `legacy_header_accounts`. One ledger file carries exactly one header format, so the "collapses to the same token as an explicitly-recorded `github.com`" property (`row27`) cannot be shown by comparing two accounts — their tokens are account-namespaced and never equal. Assert it instead by calling `_redact_pr_cost_row_for_export` directly, twice with the same `ordinal` and `host_map`: once on a row parsed from legacy text, once on an ordinary current-schema row with a raw `github.com`, and assert identical `host` tokens.
15. **Provenance line.** Present, `#`-prefixed, above the header, parsing as `key=value` tokens; carries the DO-NOT-PUBLISH marker. Two runs over the same accounts produce the same `corpus=` digest and two runs over different account sets do not — **compared between two values computed within the test, never against a hardcoded literal**, since the digest is corpus-derived and not stable across runs or machines.
16. **File mode.** `stat.S_IMODE(out.stat().st_mode) == 0o600` — asserted on an already-passing successful-export test (item 1 or 4) rather than standing up its own fixture.
17. **M3 regression.** A **golden literal** tab-separated string, captured once from `_format_pr_cost_ledger_row(_sample_pr_cost_row())`'s current output and hardcoded in the test, asserted equal to the live call's result. The literal is the frozen "before" state and the call is the live "after" state, so no in-test before/after comparison is needed — a single test run cannot invoke two versions of the function. Do **not** derive the expectation from `_PR_COST_LEDGER_HEADER_LINE` plus a per-column type loop: that re-implements the formatter's own branching inside the test and would drift with it. The existing `test_format_then_parse_round_trip_is_lossless` (:20444-20449) is not a substitute — a consistent reorder of both format and parse still round-trips clean.
18. **M16 at its own call site.** One direct regression test on `_latest_pr_cost_row` (or on `--record --force`) asserting the corrected tie-break, independent of the export's suite — the live write path should not have its only coverage inside a new, unrelated subcommand's tests.
19. **Argparse wiring.** Mirroring `TestPlanBoundaryArgparseWiring` (:19448): `parse_args(["pr-cost-export"])` yields `out is None`, `extra_config_dirs is None`, `func == _mod.cmd_pr_cost_export`.

No change to `test_transcript_cli_bootstrap.py`: it exists to prove `from transcript_analysis import X` resolves under a real subprocess (its docstring, :1-11), and this code is shim-resident with no package re-export to break.

**Modify — `CLAUDE.md` (repo root)**

One sentence in the "Also redact structural fingerprints and provenance" subsection naming `pr-cost-export` rows, and any per-row figure derived from them, as private-corpus-derived and barred from this repo, pointing to `docs/pr-cost.md`. In scope under Axis 1's "required to make the ticket's change correct" carve-out: this PR creates the artifact class the sentence governs. The existing human-judgment requirement for aggregates stays as written (`row6`).

**Word it as a clarifying application of the existing universal test, not as a separately enumerated prohibition** — e.g. "for the avoidance of doubt, this covers tokenized output such as a `pr-cost-export` file." That subsection is deliberately datatype-agnostic ("whatever the datatype, and whether you quoted it, computed it, or recalled it"), and a named carve-out inside a universal rule invites the reading that the rule binds only where it enumerates. The sentence earns its place because the trap is genuinely non-obvious in one specific way: the export's identity cells *are* tokenized, so the artifact looks redacted and a future agent could in good faith classify it under the rule's own synthetic-fixture exception. Removing the sentence must not read as removing the bar.

**Modify — `docs/pr-cost.md`**

New `## Redacted cross-account export` section placed **after** `## Redaction: head_branch is opaque, host/repo are raw at rest` (not after `## Refusals`), so it builds on the mechanism it cites rather than preceding it. Also update that existing section's sentence at :101 — "`host` has no equivalent terminal exposure to cover in the first place" goes stale once the export writes a redacted `host` token into a durable file.

Section content. **Every `G<n>` / `M<n>` / `row<n>` token below is a plan-local anchor naming where the requirement comes from — none of them may appear in the shipped doc, which has no way to resolve them.**

- **Parsing first, because nothing else works without it.** The file opens with a `#`-prefixed provenance line above the TSV header. Name the recipe: pass `comment="#"` to a tab reader, or `tail -n +2`. Handles it as-is: `pandas.read_csv`. Does not: `cut -f`, bare `awk -F'\t'`, spreadsheet imports — each silently shifts every column by one row. The source ledger format has no preamble convention, so this is new and must be stated.
- The column shape and the four tokenized columns; the two date-only columns and `rate_stamp`'s no-op.
- **The grain: one row per `(host, repo, pr_number, machine)`, current state only.** State all four key parts — the shorthand `(repo, PR, machine)` is wrong now that GHE hosts are supported, and a consumer grouping without `host_token` would merge two distinct PRs sharing an `owner/repo` string across hosts. Summing one PR across two machines is correct, not a duplicate.
- **`correction_count`:** the number of other rows sharing this row's key in the account's raw ledger, counted regardless of `status` (total captures minus one). `0` means this is the only capture ever recorded under that key. A corrected row is exactly as current and authoritative as an uncorrected one. The count conflates rate-table refreshes, data-quality fixes, and operator iteration with no reason code, so a nonzero value is **not** a trust or quality signal on its own.
- That `head_branch_label` is a per-run label and not a join key (`G1`, `M5`); that the same repo in two accounts yields two unrelated tokens; that a `pr` token is unique only within `(account, repo)`.
- The `machine`-plus-dates correlation residual on the same accepted terms as :88; the re-identification mechanism and why no column subset is safe (`row5`), deferring to `CLAUDE.md` for the prohibition itself (`M15`). **Carry `row6`'s granted aggregate scope verbatim** — aggregate PR counts, token counts, cost, and models used, reported in one bucket — in the same sentence as the prohibition. This is the durable home for it: the plan file becomes inert history once merged, and a future session writing the case study will read this file, not the plan.
- A cross-reference to the existing `## Comparing rows across rate stamps` (:74) — this export's whole purpose is that caveat's scenario, so dollars must be re-derived from the retained token columns under one rate table when `rate_stamp` differs.
- That `opus_dollar_share_pct` must be recomputed from summed numerator and denominator, never row-averaged, mirroring `mean_context_at_turn`'s existing caution at :26.
- That `status != "ok"` rows carry missing-not-zero `gh` columns and must be filtered before any size/rework aggregation. Separately, and more sharply: **under this grain a key whose latest capture is degraded contributes no row at all to a `gh`-sourced aggregate, not merely an excluded one.** An earlier complete capture, if one exists, is recoverable only from the source account's ledger. Collapse stays purely latest-by-`captured_at` and never prefers an older `ok` row, because inverting an operator's own explicit correction would be worse.
- That two exports are comparable only by column *name*, never by position or count — this is what handles schema drift, independently of the digest. The `corpus=` digest separately certifies that two exports saw the same account set, so their `account-K` ordinals line up (`G3`, `M14`); it says nothing about schema. Like `_corpus_fingerprint`, it is a same-corpus indicator, not a security boundary.
- That a PR merged on the change's own deploy day needs manual resolution against the source ledger's full-precision `merged_at`.
- Why the legacy-header `host` backfill is a validated historical fact rather than a guess (`row27`), so a reader does not treat the tokenized value as lower-confidence than a recorded one.
- The re-run convention (a timestamped `--out` per run) since `O_EXCL` refuses to overwrite; the non-POSIX-mount mode residual (`row25`); and that inspecting the export inside a Claude Code session copies it into that session's transcript (`row31`).

**Modify — `docs/transcript-analysis.md`**

New `## pr-cost-export` section after `## pr-cost` (which ends :1001), matching the established per-subcommand shape (Purpose / Flags / Default output / When to reach for it) and deferring to `docs/pr-cost.md` for the redaction and grain contract, exactly as `## pr-cost` does at :982. Do not restate the column contract here.

**Modify — `CHANGELOG.md`**

One bullet under the existing `### Added` heading in `[Unreleased]` (:48), plus one under `### Fixed` (:84) for M16's tie-break. A new subcommand ships to every stow consumer on `git pull`, so it is user-facing. The Added bullet names the required `--out` and its reason, the current-row-only grain, the reuse of `.pr-cost-enabled`, and that no new sentinel or re-install is needed.

The Fixed bullet must name `_latest_pr_cost_row` explicitly and state that the corrected tie-break affects **`pr-cost` read mode and `--record`'s existing selection**, not only the new subcommand — this is the one surface that tells an existing user their already-recorded ledger now reads differently on a row where two captures landed in the same UTC second. Framing it as an export-internal fix would under-communicate a live-path change.

**Comment discipline.** Durable one-line facts only, no rationale paragraphs: on the `columns` parameter (shared with the export's wider tuple); on the ordinal-ordered iteration (row order stays stable across active profiles); on not renumbering after a sentinel skip (parity is with `--record`'s skip, not read mode's); on the terminal open (the final path component stays unresolved so `O_EXCL` refuses a symlink); on `_latest_pr_cost_row`'s tie-break (last appended wins on an equal `captured_at`); on collapse running before redaction (the helper compares a typed `int` `pr_number`); and on `_pr_cost_export_rows` returning a materialized list so no partial file can exist. Everything about why the one-off was lost, or what an earlier draft got wrong, belongs in the commit message and PR body.

## Verification

From inside the worktree, per README.md:516's worktree-relative substitution:

```bash
../../../.venv/bin/python3 claude/.claude/scripts/select-tests.py
../../../.venv/bin/ruff check claude/.claude/ claude-skills/
```

`select-tests.py` needs no new rule-table entry for this diff (`row28`). Per CLAUDE.md's Commands section, agents run `select-tests.py`, not the full suite; CI runs the full suite on push.

An automated subprocess test (`test_transcript_analysis_pr_cost_export_subprocess_writes_synthetic_two_account_rows` in `claude/.claude/scripts/tests/test_transcript_cli_bootstrap.py`) covers what the plan originally scoped as a manual smoke check: it seeds two synthetic accounts (an opt-in sentinel plus a one-row ledger each) under `tmp_path`, points `CLAUDE_CONFIG_DIR`/`TRANSCRIPT_CONFIG_DIRS_FILE` at them, invokes `pr-cost-export --out PATH` as a real subprocess, and asserts exit code 0, `corpus_override=1` in the provenance line, both accounts' rows in the output file, and the absence of raw identifiers (`"github.com"`, `"owner/repo"`, `"branch-1"`) from the output. Unlike a manual check run in a separate terminal, this leaves no ambiguity about whether the run actually scanned synthetic roots rather than this workstation's real accounts, and the file is never read back by hand.

## Out of scope

- **Running the analysis.** The cross-account/cross-repo before/after comparison and any update to `docs/case-studies/handoff-threshold-impact.md` are the follow-on step this plan unblocks. It inherits `row6`'s constraint explicitly: the analysis runs across all accounts privately, and only aggregate PR counts, token counts, cost, and models used may be published in one bucket without returning to the engineer.
- **Closing the `host`/`repo` raw-at-rest gap** in the ledger itself (`G2`, `docs/pr-cost.md:99-103`). The export redacts on the way out; changing what is stored is a ledger-format migration with a documented backward-compat contract (:34).
- **Making stored `head_branch` labels stable across runs** (`G1`). Requires a durable per-account branch map on the write path; the export cannot reconstruct one from labels alone.
- **A cross-run anchor for `account-K` ordinals** (`G3`). `_redaction_ordinals` is shared by ~16 call sites; M14's provenance hash lets a consumer *detect* incomparability, which is the part this plan owns.
- **Rescoping `.pr-cost-enabled` from per-account to per-machine.** The engineer flagged this as worth revisiting; it changes `--record`'s gate and would pull in `install.sh` and `test_install_sh_sentinel_inventory.py`, which `row30` asserts are untouched. Separate change.
- **A keyed or salted provenance digest.** `row34`'s corpus-derived input removes the enumeration risk that motivated one, and introducing a machine-local secret would add provisioning, storage, and rotation surface to a tool that has none — a layer closing a gap the previous layer opened.
- **Migrating `cmd_pr_cost` or the export into `transcript_analysis/` package modules** (`M11`).
- **A `--no-redact` or disclose mode**, on the same grounds `_pr_cost_report` refuses one (:8337-8338).
- **A `pr-cost-export` CLI bootstrap test proving *package re-export* wiring** — no package re-export exists for it to prove; see Verification above for the subprocess test that exists instead, which proves the synthetic-corpus test recipe end-to-end rather than package-import wiring.
- **A `deny-private-project-refs.sh` detector for the export's token or header shape** (`M15`) — rejected on the merits, not deferred.
- **Runtime re-checking of the created file's mode** against a non-POSIX mount (`row25`).
- **Refactoring `_pr_cost_report`'s preamble** to make an in-`pr-cost` export mode viable (`row7`). Evaluated as lighter primitive A under `M1` and set aside; nothing here should touch that function.
